---
phase: 02.1-side-panel-ux
verified: 2026-05-19T00:00:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
  gaps_closed: []
  gaps_remaining: []
  regressions: []
---

# Phase 02.1: Side-Panel UX Closure — Verification Report

**Phase Goal:** Close the 5 UX gaps surfaced by Phase 02 human UAT so the side
panel is ergonomically usable in day-to-day work (grouped hierarchy, clean
labels, active-workspace cue, in-panel env switcher, browser-link shortcuts)
**Verified:** 2026-05-19
**Status:** PASSED
**Re-verification:** No — initial verification

> **MVP-mode note:** ROADMAP marks this phase `mode: mvp`, but the goal is not a
> User Story (`As a …, I want …, so that …`). Verifier proceeded under standard
> goal-backward methodology against the 5 explicit success criteria + PANEL-07..PANEL-11
> requirements, because the success criteria are unambiguous and observable. This
> deviation from MVP guard is informational, not blocking.

## Goal Achievement

### Observable Truths

| # | Truth (Success Criterion / Requirement) | Status | Evidence |
|---|------------------------------------------|--------|----------|
| 1 | **PANEL-07** — Expanding a workspace shows `Projects (n)` + `Scripts (n)` category nodes, each lazy-loading children | ✓ VERIFIED | `src/sidePanel.ts:119-136` constructs both category TreeItems with `folder-library` icon and `(${n.count})` label; `loadWorkspaceChildren` (`src/sidePanel.ts:238-287`) fetches projects + scripts in parallel, populates `projectsCache`/`scriptsCache`, and returns ONLY the two category nodes; `getChildren` returns cached arrays when expanding category nodes (`sidePanel.ts:188-193`). Context values `projectsCategoryNode` / `scriptsCategoryNode` defined at `sidePanel.ts:21-22` per D-02. |
| 2 | **PANEL-08** — Labels exclude GUIDs; `Copy ID` context menu exposes the GUID on workspace, project, script | ✓ VERIFIED | Labels: `sidePanel.ts:97` (`n.info.name`), `:139` (`n.project.name`), `:148` (`n.script.name`) — plain names only, no GUID interpolation. Command: `altium365.tree.copyId` registered in `src/treeCommands.ts:18-64`, reads `node.info.workspaceId` / `node.project.id` / `node.script.scriptId`, writes via `vscode.env.clipboard.writeText`, shows confirmation toast. Menu wired: `package.json:300-313` adds 3 `view/item/context` entries (workspaceNode, projectNode, scriptNode) under group `9_clipboard@1`. Command declared `package.json:113-116`. |
| 3 | **PANEL-09** — Active workspace visually distinguishable from inactive | ✓ VERIFIED | `getActiveWorkspaceId(ctx)` exported from `src/auth.ts:373-377`, reads `globalState['altium365.activeWorkspaceId']`. Writer at `auth.ts:296` updates key inside `ensureWorkspaceToken`; cleared on sign-out at `auth.ts:343`. Renderer at `sidePanel.ts:106-116`: active workspace → `ThemeIcon('cloud')` + `description = '(active)'`; inactive → `ThemeIcon('cloud', ThemeColor('descriptionForeground'))`. Distinction is both icon-tinted and screen-reader visible. |
| 4 | **PANEL-10** — View title exposes globe-icon action that runs `altium365.selectEnvironment` | ✓ VERIFIED | `package.json:265-277` `menus.view/title` contains entry: `command: altium365.selectEnvironment`, `when: view == altium365.tree && altium365.signedIn`, `group: navigation`, `icon: $(globe)`. Existing refresh entry preserved. No new command handler needed — reuses existing `altium365.selectEnvironment` declared at `package.json:71-75`. |
| 5 | **PANEL-11** — Workspace and project context menus include `Open in Browser` opening the node's A365 URL | ✓ VERIFIED | GraphQL queries extended with `url` field: `src/workspace.ts:58` (`desWorkspaceInfos { … url }`), `:120` (`desProjects { nodes { id name url } }`); types include `url?: string` at `:17` and `:110`. Node payloads carry url: `sidePanel.ts:232` (workspace), `:261` (project). Commands `altium365.workspace.openInBrowser` + `altium365.project.openInBrowser` registered in `src/treeCommands.ts:65-126` — call `vscode.env.openExternal(vscode.Uri.parse(url))` with fallback toast `"This {workspace\|project} does not expose a browser URL"` per D-10. Menu wired: `package.json:314-323` (`group: 9_open@1`, gated by viewItem). Commands declared `package.json:117-126`. |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/sidePanel.ts` | Category nodes, label cleanup, active-workspace icon/description | ✓ VERIFIED | 288 lines; substantive logic for D-01..D-04, D-07. Wired via `registerTreeDataProvider` in extension.ts (pre-existing). |
| `src/treeCommands.ts` | New module: copyId + openInBrowser handlers (D-11) | ✓ VERIFIED | 128 lines; exports `registerTreeCommands`. Imported `src/extension.ts:19`, invoked `:48`. |
| `src/auth.ts` | `getActiveWorkspaceId` accessor + setter inside `ensureWorkspaceToken` (D-06) | ✓ VERIFIED | Key constant `:30`, setter `:296`, clear-on-sign-out `:343`, getter `:373-377`. Used by sidePanel.ts:106. |
| `src/workspace.ts` | `url` field on `desWorkspaceInfos` + `desProjects` queries, types updated (D-09) | ✓ VERIFIED | Types `:17`, `:110`; queries `:58`, `:120`. |
| `package.json` | 3 new commands, 5 new view/item/context entries, 1 new view/title entry | ✓ VERIFIED | Commands `:113-126`; view/title `:271-276`; copy-id menus `:300-313`; open-in-browser menus `:314-323`. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `sidePanel.ts` workspace TreeItem | active workspace state | `getActiveWorkspaceId(ctx)` import + call | ✓ WIRED | Imported `sidePanel.ts:5`, called `:106`; state populated in `ensureWorkspaceToken` (auth.ts:296). |
| Category node expand | cached children | `projectsCache.get(workspaceId)` / `scriptsCache.get(workspaceId)` | ✓ WIRED | Populated in `loadWorkspaceChildren` (sidePanel.ts:270-271), read in `getChildren` (`:189`, `:192`). |
| `Copy ID` menu entry | `altium365.tree.copyId` handler | command registration | ✓ WIRED | Menu `package.json:300-313` → command declared `:113` → handler `treeCommands.ts:19` → factory called `extension.ts:48`. |
| `Open in Browser` menu | `altium365.{workspace,project}.openInBrowser` handlers | command registration | ✓ WIRED | Menu `package.json:314-323` → commands declared `:117-126` → handlers `treeCommands.ts:65,96` → factory called `extension.ts:48`. |
| View title globe → env switcher | `altium365.selectEnvironment` | reuses existing command | ✓ WIRED | Menu `package.json:271-276` references pre-existing command `:71-75`. No new handler needed. |
| Node payload `url` | Open-in-browser handler | `node.url` read | ✓ WIRED | Set in `loadWorkspaces` (`sidePanel.ts:232`) and `loadWorkspaceChildren` (`:261`); read at `treeCommands.ts:75,106`. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| workspace TreeItem `description="(active)"` | `activeId` | `globalState['altium365.activeWorkspaceId']` written by `ensureWorkspaceToken` on every workspace-token exchange | Yes (real workspaceId from GraphQL exchange) | ✓ FLOWING |
| category node count | `projects.length` / `scripts.length` | `listProjects` / `listScripts` GraphQL via `graphqlRequest` | Yes (live API; unchanged from Phase 02) | ✓ FLOWING |
| Open in Browser URL | `node.url` | `desWorkspaceInfos { url }` / `desProjects { nodes { url } }` | Conditional — depends on backend exposing `url`; handler defends with fallback toast when undefined (treeCommands.ts:76-81, 107-112) | ✓ FLOWING (with documented fallback) |
| Copy ID clipboard write | `node.info.workspaceId` / `node.project.id` / `node.script.scriptId` | GraphQL response | Yes (GUIDs preserved on payload even though stripped from labels — D-04) | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| TypeScript compiles cleanly with all new wiring | `npx tsc -p . --noEmit` | exit 0, no diagnostics | ✓ PASS |
| `Copy ID` command registered | grep `altium365.tree.copyId` in package.json + treeCommands.ts | declared + handler + 3 menu entries | ✓ PASS |
| `Open in Browser` commands registered | grep `openInBrowser` | 2 commands declared + handlers + 2 menus | ✓ PASS |
| `getActiveWorkspaceId` exported and consumed | grep across src/ | exported `auth.ts:373`, consumed `sidePanel.ts:5,106` | ✓ PASS |

### Probe Execution

No probes declared in PLAN/SUMMARY artifacts and no conventional `scripts/*/tests/probe-*.sh` exist. Skipped.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| PANEL-07 | 02.1-01-PLAN.md | Category parent nodes Projects(n)/Scripts(n) | ✓ SATISFIED | Truth #1 evidence |
| PANEL-08 | 02.1-03-PLAN.md | GUID-free labels + Copy ID context menu | ✓ SATISFIED | Truth #2 evidence |
| PANEL-09 | 02.1-04-PLAN.md | Active workspace visually distinguished | ✓ SATISFIED | Truth #3 evidence |
| PANEL-10 | 02.1-02-PLAN.md | View-title globe action → selectEnvironment | ✓ SATISFIED | Truth #4 evidence |
| PANEL-11 | 02.1-05-PLAN.md | Open in Browser on workspace + project | ✓ SATISFIED | Truth #5 evidence |

No orphaned requirements: REQUIREMENTS.md traceability table maps PANEL-07..11 exclusively to Phase 02.1, all five are covered by the five plans.

### Anti-Patterns Found

Scanned `src/sidePanel.ts`, `src/treeCommands.ts`, `src/workspace.ts`, `src/auth.ts`, `package.json` for debt markers (TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER), stub returns, hardcoded empty data flowing to UI, and console.log-only handlers.

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| _none_ | — | — | — | No blocking or warning patterns found in modified files. |

Notes:
- `void cfg;` at `sidePanel.ts:213` is an intentional lint-friendly discard of an unused result, not a stub.
- Empty `[]` returns at `sidePanel.ts:194,216` are correct fallbacks (no children for unknown node kinds; no workspaces when unauthenticated) and are NOT stubs hiding behind hardcoded data — the populated branches flow real GraphQL results.
- Fallback toasts in `treeCommands.ts` (workspace/project without `url`) are explicit UX choices per D-10, not stubs.

### Human Verification Required

None. All five truths are observable in code and require no live UI smoke test for verification — the wiring chain (menu → command → handler → API → tree render) is fully traceable via static analysis, and the original UAT gaps G-01..G-05 are already addressed by clear implementation evidence above. Live UAT against a real A365 workspace is *recommended* as routine smoke-test practice but is not required to confirm goal achievement.

### Gaps Summary

No gaps. All five PANEL-07..PANEL-11 requirements and all five ROADMAP success criteria are satisfied by substantive, wired, data-flowing implementations across `src/sidePanel.ts`, `src/treeCommands.ts`, `src/auth.ts`, `src/workspace.ts`, and `package.json`. TypeScript compiles cleanly. The phase delivered exactly what was promised in CONTEXT.md decisions D-01..D-11.

---

_Verified: 2026-05-19_
_Verifier: gsd-verifier_
