---
phase: 02.1-side-panel-ux
plan: 03
type: execute
wave: 2
depends_on:
  - 02.1-01
files_modified:
  - src/sidePanel.ts
  - src/treeCommands.ts
  - src/extension.ts
  - package.json
autonomous: true
requirements:
  - PANEL-08
tags:
  - vscode-tree
  - clipboard
  - labels
must_haves:
  truths:
    - "Workspace, project, and script tree labels no longer display backend GUIDs in either the label or the description"
    - "Right-clicking a workspace, project, or script node shows a 'Copy ID' context menu item"
    - "Invoking 'Copy ID' writes the node's backend identifier to the system clipboard (vscode.env.clipboard.writeText)"
    - "After copying, a short info toast confirms the action"
    - "The GUID is still carried on the A365Node payload so other commands (Open in Browser, future SCRIPT-* operations) can read it"
  artifacts:
    - path: "src/sidePanel.ts"
      provides: "TreeItem labels without GUIDs (D-04)"
    - path: "src/treeCommands.ts"
      provides: "registerTreeCommands factory exporting Copy ID handler (D-11)"
      contains: "registerTreeCommands"
    - path: "package.json"
      provides: "altium365.tree.copyId command + view/item/context entries for workspace/project/script (D-05)"
      contains: "altium365.tree.copyId"
  key_links:
    - from: "package.json view/item/context entries (3 entries, group 9_clipboard)"
      to: "altium365.tree.copyId handler in src/treeCommands.ts"
      via: "viewItem == workspaceNode|projectNode|scriptNode"
      pattern: "altium365.tree.copyId"
    - from: "src/extension.ts activate()"
      to: "registerTreeCommands(ctx, output)"
      via: "ctx.subscriptions.push(...registerTreeCommands(...))"
      pattern: "registerTreeCommands"
---

<objective>
G-02 closure: drop backend GUIDs from displayed tree labels (and from the `description` field where it currently leaks the GUID) and expose the GUID instead via a `Copy ID` context menu item on workspace, project, and script nodes. Introduces a new shared module `src/treeCommands.ts` per D-11 that mirrors the existing `registerScriptCommands` factory pattern.

Purpose: removes visual noise (GUIDs in every row) while keeping the GUID one click away when developers actually need it.
Output: cleaner tree labels, one new command (`altium365.tree.copyId`), three new view/item/context entries, and a new `src/treeCommands.ts` module wired into activate().

Depends on 02.1-01 because both plans modify `src/sidePanel.ts`. Wave 1 runs the hierarchy restructure first; this plan then edits the post-restructure TreeItem construction sites in getTreeItem.
</objective>

<execution_context>
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-side-panel-ux/02.1-CONTEXT.md
@.planning/phases/02-side-panel/02-HUMAN-UAT.md
@AGENTS.md
@src/sidePanel.ts
@src/scriptCommands.ts
@src/extension.ts
@package.json

<interfaces>
<!-- Existing pattern to mirror — src/scriptCommands.ts:
       export function registerScriptCommands(context: vscode.ExtensionContext, output: vscode.OutputChannel): vscode.Disposable[]
     Wired from src/extension.ts:46:
       const scriptCommandDisposables = registerScriptCommands(context, outputChannel);
       ...ctx.subscriptions.push(...scriptCommandDisposables);
     New module should follow the same shape exactly.

     Existing A365Node payloads carry the GUIDs we need:
       - workspace: n.info.workspaceId
       - project:   n.project.id
       - script:    n.script.scriptId
     These remain on the node after this plan (labels change, payloads do not). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Strip GUIDs from displayed tree labels for workspace/project/script</name>
  <read_first>
    - src/sidePanel.ts (POST-02.1-01 state — getTreeItem switch; current state lines 64-119 sets `item.description = workspaceId | project.id | script.scriptId` on workspace/project/script branches)
    - .planning/phases/02.1-side-panel-ux/02.1-CONTEXT.md (D-04: label = human name only, GUID stays on payload)
  </read_first>
  <action>
    In src/sidePanel.ts getTreeItem, for the three branches `case 'workspace'`, `case 'project'`, `case 'script'`:
    - Keep the constructor argument `n.info.name` / `n.project.name` / `n.script.name` (these are already human names — verify they don't include the GUID; the current TreeItem label is the name, but the `description` field is where the GUID currently appears).
    - REMOVE the three lines that currently set `item.description = n.info.workspaceId` / `item.description = n.project.id` / `item.description = n.script.scriptId`. Per D-04, the displayed description must not carry the GUID.
    - Keep `item.tooltip` on the script branch unchanged (`n.script.description ?? n.script.name`); the tooltip is fine because it's not a primary label.
    - Do NOT modify any A365Node payload — workspaceId / project.id / scriptId stay on the node objects so Copy ID and future commands can read them.

    Leave Plan 02.1-04's "(active)" description for the active workspace as a Plan 02.1-04 concern — do NOT pre-empt it here. This task just removes the GUID; Plan 02.1-04 will conditionally set description to `"(active)"` on the active workspace only.
  </action>
  <verify>
    <automated>npm run compile</automated>
  </verify>
  <acceptance_criteria>
    - `npm run compile` exits 0
    - `grep -n "item.description = n.info.workspaceId" src/sidePanel.ts` returns nothing
    - `grep -n "item.description = n.project.id" src/sidePanel.ts` returns nothing
    - `grep -n "item.description = n.script.scriptId" src/sidePanel.ts` returns nothing
    - `grep -c "n.info.workspaceId\\|n.project.id\\|n.script.scriptId" src/sidePanel.ts` is still &gt;= 1 (payload references survive on the node objects; e.g. in loadWorkspaceChildren / category node construction)
  </acceptance_criteria>
  <done>
    Workspace, project, and script TreeItems no longer carry the GUID in their `description` field. Payload still carries the GUIDs for downstream commands.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create src/treeCommands.ts with registerTreeCommands factory + Copy ID handler</name>
  <read_first>
    - src/scriptCommands.ts (full file — the EXACT factory pattern to mirror per D-11: signature, return type, internal closure helpers, JSDoc header)
    - src/sidePanel.ts (A365Node union shape — to safely narrow on `kind` inside the handler)
    - .planning/phases/02.1-side-panel-ux/02.1-CONTEXT.md (D-05: command id `altium365.tree.copyId`, reads node.id, writes via `vscode.env.clipboard.writeText`; D-11: new module name; the agent's discretion note allows the post-copy info toast)
  </read_first>
  <action>
    Create a NEW file `src/treeCommands.ts` exporting `registerTreeCommands(context: vscode.ExtensionContext, output: vscode.OutputChannel): vscode.Disposable[]` mirroring the signature and structure of `registerScriptCommands` exactly.

    The factory registers ONE command in this task (additional commands come from Plan 02.1-05):
      - Command id: `altium365.tree.copyId`
      - Handler receives an `A365Node | undefined` (VS Code passes the right-clicked tree item as the first argument for view/item/context commands).
      - Handler must narrow on `node.kind` and read the GUID from the correct payload field:
          * workspace → `node.info.workspaceId`
          * project   → `node.project.id`
          * script    → `node.script.scriptId`
          * any other kind (info, error, projectsCategory, scriptsCategory) or undefined → no-op with a single line appended to `output` for diagnosis (`'[Altium 365] tree.copyId: ignored kind=' + node?.kind`); do NOT show a toast in this case (silent ignore is correct because contributes-menu `when` clauses scope the command to the three valid kinds anyway).
      - On the three valid kinds: call `await vscode.env.clipboard.writeText(id)`, then show `vscode.window.showInformationMessage('Altium 365: ID copied to clipboard')` (per the agent's discretion default in CONTEXT.md).
      - Wrap clipboard write in try/catch; on failure surface `vscode.window.showErrorMessage('Altium 365: Copy ID failed: ' + e.message)` and append the stack to `output` (CONVENTIONS.md: errors surface at command boundary).

    Import `A365Node` (and the relevant per-kind context-value constants if useful for narrowing) from `./sidePanel`. Do NOT introduce a circular import — sidePanel does not import from treeCommands.

    Do NOT register Copy ID in `package.json` here — that's Task 3. Do NOT wire the factory into `extension.ts` here — that's Task 4.

    JSDoc header: short paragraph noting this module hosts tree-generic (non-script-scoped) handlers per D-11 of `.planning/phases/02.1-side-panel-ux/02.1-CONTEXT.md`, mirroring the `registerScriptCommands` pattern.
  </action>
  <verify>
    <automated>npm run compile</automated>
  </verify>
  <acceptance_criteria>
    - `src/treeCommands.ts` exists
    - `grep -n "export function registerTreeCommands" src/treeCommands.ts` matches exactly once
    - `grep -n "altium365.tree.copyId" src/treeCommands.ts` matches at least once
    - `grep -n "vscode.env.clipboard.writeText" src/treeCommands.ts` matches at least once
    - `grep -n "import.*A365Node.*from './sidePanel'" src/treeCommands.ts` matches (import path correct)
    - `npm run compile` exits 0
  </acceptance_criteria>
  <done>
    src/treeCommands.ts exists with the registerTreeCommands factory and a working Copy ID handler. Not yet wired or declared in package.json.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Declare altium365.tree.copyId in package.json + add 3 view/item/context entries</name>
  <read_first>
    - package.json (full contributes.commands array around lines 45-112; full contributes.menus["view/item/context"] array around lines 257-278)
    - .planning/phases/02.1-side-panel-ux/02.1-CONTEXT.md (D-05; &lt;specifics&gt; "Context menu groupings" — group name `9_clipboard`)
  </read_first>
  <action>
    Edit package.json:

    1. Append a new entry to `contributes.commands`:
       - `command: "altium365.tree.copyId"`
       - `title: "Altium 365: Copy ID"`
       - `category: "Altium 365"`
       - No `icon` field (it's a context-menu-only action; surfacing in the Command Palette without an icon is fine and consistent with `altium365.tree.retryNode`).

    2. Append THREE new entries to `contributes.menus["view/item/context"]` — one per supported node kind:
       - `{ command: "altium365.tree.copyId", when: "view == altium365.tree && viewItem == workspaceNode", group: "9_clipboard@1" }`
       - `{ command: "altium365.tree.copyId", when: "view == altium365.tree && viewItem == projectNode",   group: "9_clipboard@1" }`
       - `{ command: "altium365.tree.copyId", when: "view == altium365.tree && viewItem == scriptNode",    group: "9_clipboard@1" }`

       Use group `9_clipboard` (high group number ⇒ renders below the existing `1_run` / `2_edit` script groups, per the CONTEXT.md groupings note). Do NOT add an entry for `projectsCategoryNode` or `scriptsCategoryNode` — category rows have no copyable identity.

    3. Do NOT modify the existing four `altium365.script.*` view/item/context entries.

    JSON must remain well-formed and the file must keep its 2-space indentation.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.contributes.commands.some(c=>c.command==='altium365.tree.copyId')) throw new Error('command missing'); const m=p.contributes.menus['view/item/context'].filter(x=>x.command==='altium365.tree.copyId'); if(m.length!==3) throw new Error('expected 3 menu entries, got '+m.length); const ctx=new Set(m.map(x=>x.when)); ['workspaceNode','projectNode','scriptNode'].forEach(k=>{if(![...ctx].some(w=>w.includes('viewItem == '+k))) throw new Error('missing '+k);}); if(!m.every(x=>x.group&&x.group.startsWith('9_clipboard'))) throw new Error('wrong group'); console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - The Node assertion above prints `ok`
    - `npm run compile` exits 0
    - JSON parses cleanly: `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` exits 0
    - No duplicate `altium365.tree.copyId` command declaration (only one entry in contributes.commands)
  </acceptance_criteria>
  <done>
    package.json declares the Copy ID command and exposes it via context menu on the three node kinds.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Wire registerTreeCommands into extension.ts activate()</name>
  <read_first>
    - src/extension.ts (lines 1-82 — imports, activate signature, current ctx.subscriptions.push pattern, especially line 46-78 where scriptCommandDisposables is constructed and spread into subscriptions)
  </read_first>
  <action>
    Edit src/extension.ts:

    1. Add an import: `import { registerTreeCommands } from './treeCommands';` next to the existing `import { registerScriptCommands } from './scriptCommands';`.

    2. Inside `activate()`, immediately after the existing `const scriptCommandDisposables = registerScriptCommands(context, outputChannel);` line, add a parallel line:
       `const treeCommandDisposables = registerTreeCommands(context, outputChannel);`

    3. Inside the existing `context.subscriptions.push(...)` call, add `...treeCommandDisposables` next to the existing `...scriptCommandDisposables` (so both spread arrays end up in subscriptions).

    Do NOT change any other ordering or any other registered command.
  </action>
  <verify>
    <automated>npm run compile</automated>
  </verify>
  <acceptance_criteria>
    - `npm run compile` exits 0
    - `grep -n "registerTreeCommands" src/extension.ts` matches in BOTH the import line and inside activate()
    - `grep -n "...treeCommandDisposables" src/extension.ts` matches inside the ctx.subscriptions.push spread
    - Activating the extension (manual smoke deferred to phase-end) does not log a "command not found" error for `altium365.tree.copyId`
  </acceptance_criteria>
  <done>
    registerTreeCommands runs on activation; altium365.tree.copyId has a live handler.
  </done>
</task>

</tasks>

<verification>
- `npm run compile` clean
- All node assertions / greps in task acceptance criteria pass
- Manual smoke (deferred): right-click any workspace/project/script → `Copy ID` appears below the existing script actions, copies the correct GUID, info toast confirms.
</verification>

<success_criteria>
PANEL-08 satisfied: tree labels are GUID-free; Copy ID is available on the three node kinds and writes the correct identifier to the clipboard.
</success_criteria>

<output>
Create `.planning/phases/02.1-side-panel-ux/02.1-03-SUMMARY.md` when done.
</output>
