---
phase: 02.1-side-panel-ux
plan: 04
type: execute
wave: 2
depends_on:
  - 02.1-01
files_modified:
  - src/auth.ts
  - src/sidePanel.ts
autonomous: true
requirements:
  - PANEL-09
tags:
  - vscode-tree
  - auth
  - active-workspace
must_haves:
  truths:
    - "The workspace whose token was most recently exchanged via ensureWorkspaceToken is recorded as the 'active workspace'"
    - "auth.ts exports getActiveWorkspaceId(context): string | undefined returning the most-recently-exchanged workspaceId or undefined"
    - "In the tree, the active workspace node uses a distinct ThemeIcon variant from inactive workspaces"
    - "In the tree, the active workspace node's description reads '(active)' for screen-reader clarity"
    - "When no workspace has ever been exchanged, every workspace node renders with the inactive icon and no '(active)' description"
    - "When the user signs out (clearAllTokens), the active-workspace memory is also cleared so post-sign-out tree renders show no active cue"
  artifacts:
    - path: "src/auth.ts"
      provides: "getActiveWorkspaceId accessor + active-id tracking inside ensureWorkspaceToken (D-06)"
      contains: "getActiveWorkspaceId"
    - path: "src/sidePanel.ts"
      provides: "Workspace TreeItem icon + description vary by active state (D-07)"
  key_links:
    - from: "src/auth.ts ensureWorkspaceToken"
      to: "globalState 'altium365.activeWorkspaceId'"
      via: "context.globalState.update on successful exchange"
      pattern: "altium365.activeWorkspaceId"
    - from: "src/sidePanel.ts getTreeItem case 'workspace'"
      to: "getActiveWorkspaceId(ctx)"
      via: "import + sync check (active id cached per-render or read on each getTreeItem call)"
      pattern: "getActiveWorkspaceId"
---

<objective>
G-03 closure: visually distinguish the "active workspace" — the one whose workspace-scoped token was most recently exchanged via `ensureWorkspaceToken` — from inactive workspace nodes in the tree.

Purpose: when a user has access to several workspaces, they can tell at a glance which one the panel is currently scoped to.
Output: a new `getActiveWorkspaceId` accessor in `src/auth.ts` and a conditional icon/description in the workspace TreeItem branch of `src/sidePanel.ts`.

Depends on 02.1-01 because both plans modify the workspace branch in `src/sidePanel.ts.getTreeItem`. Wave 1's hierarchy restructure ships first; this plan layers the active cue on top.

Per D-06 the accessor lives in `src/auth.ts` (not `src/workspace.ts`) because `ensureWorkspaceToken` — the function that defines "active" — already lives in `src/auth.ts:269` and is the natural write site.
</objective>

<execution_context>
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-side-panel-ux/02.1-CONTEXT.md
@.planning/phases/02-side-panel/02-HUMAN-UAT.md
@AGENTS.md
@src/auth.ts
@src/sidePanel.ts

<interfaces>
<!-- Existing src/auth.ts conventions (lines 26-29):
       const SECRET_TOKENS = 'altium365.tokens';
       const GLOBAL_WS_TOKEN_INDEX_KEY = 'altium365.workspaceTokenIds';
     New globalState key for this plan should follow the same 'altium365.*' prefix convention.

     Existing ensureWorkspaceToken signature (auth.ts:269-294):
       export async function ensureWorkspaceToken(context, cfg, workspace: { workspaceId; authId }): Promise<string>
     This is the single write site for the active-workspace id (called from sidePanel.ts:180 on workspace expansion).

     Existing clearAllTokens (auth.ts:331-344) drains all secret tokens and the workspace-token index on sign-out;
     it MUST also delete the active-workspace-id key so post-sign-out renders don't keep marking a stale workspace as active. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Track and expose the active workspace id in src/auth.ts</name>
  <read_first>
    - src/auth.ts (entire file — especially the SECRET_/GLOBAL_ constant block at lines 26-29, ensureWorkspaceToken at 269-294, clearAllTokens at 331-344, signIn at 182-236)
    - .planning/phases/02.1-side-panel-ux/02.1-CONTEXT.md (D-06 — exact definition of "active workspace")
  </read_first>
  <action>
    In src/auth.ts:

    1. Add a new module-level constant next to the existing index key:
       `const GLOBAL_ACTIVE_WORKSPACE_KEY = 'altium365.activeWorkspaceId';`

    2. In `ensureWorkspaceToken`, AFTER the successful exchange path (i.e. after the call to `exchangeWorkspaceToken` and the cache store on line 287, alongside the existing index update on lines 288-292), persist the active workspace id:
       `await context.globalState.update(GLOBAL_ACTIVE_WORKSPACE_KEY, workspace.workspaceId);`
       Do this on every successful exchange — including the cache-hit path? NO. Cache-hit means the token was already valid, no exchange happened, the previously-active workspace remains active. Only update on the exchange path (after `exchangeWorkspaceToken` returns). However, IF you find that on cache-hit the workspace also represents the user's intent to be "active" (it's being expanded/used), you may also write it on cache-hit — the cleaner reading of D-06 ("most recently exchanged") is exchange-path-only. Choose exchange-path-only and document the choice in the SUMMARY.

    3. In `clearAllTokens`, ADD a line to drop the active key alongside the existing globalState wipe (line 338):
       `await context.globalState.update(GLOBAL_ACTIVE_WORKSPACE_KEY, undefined);`
       This ensures sign-out also clears the active-workspace cue.

    4. Export a new sync accessor:
       `export function getActiveWorkspaceId(context: vscode.ExtensionContext): string | undefined`
       Implementation: `return context.globalState.get<string>(GLOBAL_ACTIVE_WORKSPACE_KEY);`
       Place it near the other token accessors (e.g. after `getActiveUserLabel` around line 361). Sync (not async) because globalState.get is sync.

    Do NOT modify any other behavior of ensureWorkspaceToken, exchangeWorkspaceToken, refreshTokens, or signIn. Do NOT fire an authStateEmitter event for the active-workspace change (the existing tree refresh via onAuthStateChanged is not triggered by workspace-token exchange, and we don't want to add a refresh storm here — Plan 02.1-04 Task 2 reads the value on every getTreeItem render, which is fine because tree refresh is already triggered by the existing refresh button + on-expand flow).
  </action>
  <verify>
    <automated>npm run compile</automated>
  </verify>
  <acceptance_criteria>
    - `npm run compile` exits 0
    - `grep -n "GLOBAL_ACTIVE_WORKSPACE_KEY" src/auth.ts` matches at least 4 times (declaration, ensureWorkspaceToken write, clearAllTokens delete, getActiveWorkspaceId read)
    - `grep -n "altium365.activeWorkspaceId" src/auth.ts` matches exactly once (the constant declaration string literal)
    - `grep -n "export function getActiveWorkspaceId" src/auth.ts` matches exactly once
    - The active-id write line in ensureWorkspaceToken appears AFTER the existing index update lines (line ordering matters — if exchange throws, we don't want to mark a failed exchange as active; placing the write after the index update guarantees the exchange succeeded)
  </acceptance_criteria>
  <done>
    src/auth.ts records the active workspace id on every successful workspace-token exchange, clears it on sign-out, and exposes a sync accessor.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Render active-workspace cue in src/sidePanel.ts workspace TreeItem</name>
  <read_first>
    - src/sidePanel.ts (POST-02.1-01 state — getTreeItem `case 'workspace'` branch; POST-02.1-03 if that plan happens to merge first, in which case `item.description = n.info.workspaceId` will already be removed)
    - src/auth.ts (the new getActiveWorkspaceId export from Task 1 above)
    - .planning/phases/02.1-side-panel-ux/02.1-CONTEXT.md (D-07 — icon and description spec)
  </read_first>
  <action>
    In src/sidePanel.ts:

    1. Add `getActiveWorkspaceId` to the existing import block from `./auth` (line 2-7 currently imports `OAuthConfig, ensureWorkspaceToken, getStoredTokens, readOAuthConfig` — append `getActiveWorkspaceId`).

    2. In `getTreeItem` `case 'workspace'`:
       - Compute `const activeId = getActiveWorkspaceId(this.ctx);` once at the top of the case block.
       - Compute `const isActive = activeId === n.info.workspaceId;`.
       - Replace the current `item.iconPath = new vscode.ThemeIcon('cloud');` with a conditional:
            * active   → `new vscode.ThemeIcon('cloud')` (per D-07: filled cloud for active)
            * inactive → `new vscode.ThemeIcon('cloud', new vscode.ThemeColor('descriptionForeground'))` (per D-07 fallback: dimmed via ThemeColor since `cloud-outline` is not a standard VS Code codicon)

         The agent's discretion: if `cloud-outline` IS a valid codicon at https://code.visualstudio.com/api/references/icons-in-labels at the time of implementation, prefer the icon-variant approach (`cloud` vs `cloud-outline`) over the ThemeColor approach. Document the choice in the SUMMARY.
       - When `isActive` is true, additionally set `item.description = '(active)';` (per D-07; provides screen-reader-friendly text). When false, do NOT set description (leave it undefined so the row stays clean — Plan 02.1-03 Task 1 already removed the GUID-from-description leak; this task must not re-introduce any non-active description content).

    3. Do NOT change the label, contextValue, or collapsibleState. Do NOT touch the project/script/category/info/error branches.
  </action>
  <verify>
    <automated>npm run compile</automated>
  </verify>
  <acceptance_criteria>
    - `npm run compile` exits 0
    - `grep -n "getActiveWorkspaceId" src/sidePanel.ts` matches in BOTH the import line and inside getTreeItem
    - `grep -n "'(active)'" src/sidePanel.ts` matches at least once (the description literal)
    - `grep -n "ThemeIcon('cloud'" src/sidePanel.ts` matches at least once (active branch)
    - For the inactive branch, EITHER `grep -n "cloud-outline" src/sidePanel.ts` matches OR `grep -n "ThemeColor('descriptionForeground')" src/sidePanel.ts` matches (the agent's discretion documented in SUMMARY)
    - The TreeItem.description for an inactive workspace is NOT set to the workspaceId (i.e. the line removed in Plan 02.1-03 Task 1 is not re-introduced) — verify by `grep -n "item.description = n.info.workspaceId" src/sidePanel.ts` returning nothing
  </acceptance_criteria>
  <done>
    Workspace rows render with a distinct icon (and `(active)` description) for the most-recently-exchanged workspace; all other workspace rows render with the inactive variant and no description.
  </done>
</task>

</tasks>

<verification>
- `npm run compile` clean
- Required greps from both task acceptance blocks pass
- Manual smoke (deferred): sign in with access to multiple workspaces → first one expanded after sign-in renders with active cue + `(active)` description; expanding a different workspace updates the active cue after the next tree refresh.
</verification>

<success_criteria>
PANEL-09 satisfied: the tree visually distinguishes the active workspace from inactive ones, with both an iconic and a textual cue.
</success_criteria>

<output>
Create `.planning/phases/02.1-side-panel-ux/02.1-04-SUMMARY.md` when done.
</output>
