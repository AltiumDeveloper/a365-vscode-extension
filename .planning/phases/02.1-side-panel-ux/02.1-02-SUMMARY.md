---
phase: 02.1-side-panel-ux
plan: 02
subsystem: ui
tags: [vscode-menus, environment-switcher, package-json]

requires:
  - phase: 02-side-panel
    provides: altium365.selectEnvironment command + altium365.signedIn context key managed by updateSignedInContext
provides:
  - Globe-icon action on A365 view title bar bound to existing environment-switch command, gated by signed-in state
affects: [phase-02.1-remaining-plans]

tech-stack:
  added: []
  patterns:
    - "Reuse-existing-command via view/title contributes — no new handler when an existing command already encapsulates the behaviour"

key-files:
  created: []
  modified:
    - package.json

key-decisions:
  - "Bound the new view/title entry to the real existing command id `altium365.selectEnvironment` rather than D-08's typo `altium365.switchEnvironment`"
  - "Kept the `$(globe)` icon on the menu entry only (not on the command declaration) to avoid an icon appearing in the Command Palette listing"

patterns-established:
  - "Title-bar shortcuts for existing commands are pure package.json changes — zero TS code, zero new commands"

requirements-completed: [PANEL-10]

duration: 1 min
completed: 2026-05-19
---

# Phase 02.1 Plan 02: Globe-Icon Environment Switcher Summary

**Globe-icon action on the A365 view title bar that runs the existing `altium365.selectEnvironment` command — pure package.json contributes change, gated by `altium365.signedIn`.**

## Performance

- **Duration:** 1 min
- **Started:** 2026-05-19T22:48:00Z
- **Completed:** 2026-05-19T22:49:00Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Added second entry under `contributes.menus["view/title"]` binding `$(globe)` icon to `altium365.selectEnvironment`
- `when` clause gates the icon on signed-in state (`view == altium365.tree && altium365.signedIn`)
- Existing `altium365.tree.refresh` title-bar action preserved unchanged
- Zero new TypeScript code, zero new command registrations

## Task Commits

1. **Task 1: Add globe-icon view/title entry for environment switch** — `feat(02.1-02)` (see git log)

**Plan metadata:** `docs(02.1-02): complete globe-icon environment switcher plan`

## Files Created/Modified
- `package.json` — Added second `view/title` menu entry (globe icon → `altium365.selectEnvironment`, gated by `altium365.signedIn`)

## Decisions Made
- **Command id correction:** CONTEXT.md D-08 names the target `altium365.switchEnvironment`, but the command that actually exists (declared at package.json:72, registered at src/extension.ts:61, invoked by the status-bar QuickPick at extension.ts:89) is `altium365.selectEnvironment`. Plan and implementation use the real id; D-08's name is treated as a typo. Behaviour matches D-08's intent.
- **Icon placement:** Icon set on the menu entry only, not on the command declaration in `contributes.commands`, so the Command Palette listing stays icon-free.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- PANEL-10 satisfied. Manual smoke test deferred to phase-end human-verify (G-04): visit A365 view title bar signed in → globe icon visible → click → Select Environment QuickPick opens → sign out → icon disappears.
- Ready for next plan: 02.1-03.

## Self-Check: PASSED
- `package.json` exists and contains the new `altium365.selectEnvironment` view/title entry (verified via node)
- All four acceptance-criteria commands exit clean (node assertion `ok`, refresh entry present `true`, JSON well-formed, `npm run compile` exit 0)

---
*Phase: 02.1-side-panel-ux*
*Completed: 2026-05-19*
