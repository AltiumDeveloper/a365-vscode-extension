---
status: complete
phase: 02.1-side-panel-ux
source: 02.1-01-SUMMARY.md, 02.1-02-SUMMARY.md, 02.1-03-SUMMARY.md, 02.1-04-SUMMARY.md, 02.1-05-SUMMARY.md
started: 2026-05-20T00:00:00Z
updated: 2026-05-20T00:40:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start — Extension Reloads Cleanly
expected: Reload the Extension Development Host window. A365 view appears in the Activity Bar with no error toasts and no Extension Host errors in the Debug Console. Workspaces list loads.
result: pass

### 2. Category Nodes — Projects (n) / Scripts (n)
expected: Expand a workspace in the A365 tree. Instead of projects and scripts appearing as flat siblings, you see exactly two child nodes — `Projects (n)` and `Scripts (n)` — with the count in parentheses. Expanding `Projects (n)` lists the real projects; expanding `Scripts (n)` lists the real scripts. Collapsing/re-expanding does not re-fetch (cached).
result: pass

### 3. Globe Icon — Switch Environment from View Title
expected: A globe icon appears in the A365 view's title bar (top-right of the tree). Clicking it opens the environment QuickPick — same picker as the status-bar entry. Selecting a different environment switches and the tree refreshes. The refresh button is still next to the globe.
result: issue
reported: "The picker works as expected, but there is no globe icon - instead there is a long text button 'Altium 365: Select Environment (Dev / UAT / Prod)'. I'd rather have a compact globe icon and a short name of the current environment, similar to the status bar"
severity: major

### 4. Labels Show Plain Names — No GUIDs Visible
expected: All workspace, project, and script labels in the tree show plain human-readable names only. No 8-4-4-4-12 GUIDs visible anywhere in labels or in the secondary description text (where GUIDs previously leaked).
result: pass

### 5. Copy ID — Context Menu
expected: Right-click a workspace, a project, and a script. Each shows a `Copy ID` entry in the context menu. Clicking it copies the backend GUID to the clipboard and shows a brief confirmation toast (or status message). Pasting elsewhere yields the GUID.
result: pass

### 6. Active Workspace — Visual Cue
expected: After signing into a workspace (or running any command that ensures a workspace token), that workspace node shows a visually distinct state — filled cloud icon and an `(active)` description suffix — while other workspaces show a dimmed/outline cloud and no suffix. Switching to a different workspace transfers the cue.
result: pass

### 7. Open in Browser — Workspace
expected: Right-click a workspace node → `Open in Browser`. Your default browser opens the correct Altium 365 workspace URL. If the workspace has no URL yet (just signed in), a friendly toast appears instead of a crash.
result: pass

### 8. Open in Browser — Project
expected: Expand `Projects (n)`, right-click a project → `Open in Browser`. Your default browser opens that specific project's URL on Altium 365. No GUID leakage in the URL handling; the URL comes from the GraphQL `url` field.
result: pass

## Summary

total: 8
passed: 7
issues: 1
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Globe icon appears in A365 view title bar; clicking it opens the environment QuickPick"
  status: failed
  reason: "User reported: no globe icon — entry rendered as long text button 'Altium 365: Select Environment (Dev / UAT / Prod)'. User wants compact globe + short current-env name (status-bar style)."
  severity: major
  test: 3
  root_cause: ""
  artifacts: []
  missing: []
  debug_session: ""
