---
phase: 02.1-side-panel-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: true
requirements:
  - PANEL-10
tags:
  - vscode-menus
  - environment-switcher
must_haves:
  truths:
    - "The A365 view title bar exposes a globe-icon action when signed in"
    - "Clicking the globe icon runs altium365.selectEnvironment (the existing environment-switch command)"
    - "The action is hidden when the user is signed out (when: altium365.signedIn is false)"
    - "The existing altium365.tree.refresh title-bar action continues to render alongside the new one"
  artifacts:
    - path: "package.json"
      provides: "view/title entry binding the globe icon to the existing environment-switch command"
      contains: "altium365.selectEnvironment"
  key_links:
    - from: "package.json contributes.menus['view/title']"
      to: "altium365.selectEnvironment handler (src/extension.ts:61)"
      via: "command id reuse — no new handler"
      pattern: "altium365.selectEnvironment"
---

<objective>
G-04 closure: add a globe-icon action to the A365 view title bar that runs the existing environment-switch command, so users no longer have to go through the status-bar QuickPick to change environments. Pure package.json contributes change — zero new code, zero new commands.

Purpose: surfaces environment switching one click away from the panel the user is already looking at.
Output: one new entry under `contributes.menus.view/title` in package.json.

**IMPORTANT — naming reality check:** CONTEXT.md D-08 names the target command `altium365.switchEnvironment`, but the command that actually exists in the codebase is `altium365.selectEnvironment` (declared at package.json:72, registered at src/extension.ts:61, also referenced by the status-bar QuickPick at extension.ts:89). This plan uses the real existing command id and treats D-08's name as a typo. The behaviour is what D-08 intends; the identifier is the one that compiles.
</objective>

<execution_context>
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-side-panel-ux/02.1-CONTEXT.md
@.planning/phases/02-side-panel/02-HUMAN-UAT.md
@AGENTS.md
@package.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add globe-icon view/title entry for environment switch</name>
  <read_first>
    - package.json (entire contributes block — note current `view/title` entry at lines 250-256 with refresh-only action; note the existing `altium365.selectEnvironment` command declaration at lines 71-75; note the activitybar view id is `altium365.tree` per line 33)
    - .planning/phases/02.1-side-panel-ux/02.1-CONTEXT.md (D-08 and &lt;specifics&gt; "View title entry" snippet)
    - .planning/phases/02-side-panel/02-VERIFICATION.md (confirms altium365.signedIn context key is flipped by updateSignedInContext in extension.ts on activate/signIn/signOut)
  </read_first>
  <action>
    Edit `package.json` to add a second entry to `contributes.menus["view/title"]` (currently has one entry for `altium365.tree.refresh`).

    The new entry must:
    - Bind command `altium365.selectEnvironment` (NOT `altium365.switchEnvironment` — see Objective note; D-08 has a typo)
    - Use `when: "view == altium365.tree && altium365.signedIn"` (matches the view id declared at package.json line 33 and gates on the existing signed-in context key already managed by updateSignedInContext in src/extension.ts:22-29)
    - Set `group: "navigation"` (so it renders as an inline icon next to the refresh button, not in the overflow `...` menu)
    - Set `icon: "$(globe)"` (per D-08)

    Keep the existing `altium365.tree.refresh` entry intact and unchanged.

    Do NOT register a new command, do NOT modify the existing `altium365.selectEnvironment` handler. The handler already exists and works (it is the same handler invoked from the status-bar QuickPick "Switch Environment" entry).

    Do NOT add an `icon` field to the existing command declaration in `contributes.commands` for `altium365.selectEnvironment` (the icon on the menu entry is sufficient and avoids polluting the Command Palette listing with an icon).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const e=p.contributes.menus['view/title'].find(x=>x.command==='altium365.selectEnvironment'); if(!e) throw new Error('missing view/title entry'); if(e.when!=='view == altium365.tree && altium365.signedIn') throw new Error('wrong when: '+e.when); if(e.icon!=='\$(globe)') throw new Error('wrong icon: '+e.icon); if(e.group!=='navigation') throw new Error('wrong group: '+e.group); console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - The Node check above prints `ok` (entry present with correct command, when, icon, group)
    - `npm run compile` exits 0 (no TS impact — pure manifest change — but compile sanity check still required)
    - Existing `altium365.tree.refresh` entry is still present: `node -e "console.log(require('./package.json').contributes.menus['view/title'].some(x=>x.command==='altium365.tree.refresh'))"` prints `true`
    - JSON is well-formed: `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` exits 0
  </acceptance_criteria>
  <done>
    package.json declares the globe-icon view/title action bound to the existing environment-switch command, gated by signed-in state. No code changes, no new commands.
  </done>
</task>

</tasks>

<verification>
- `npm run compile` clean
- Node assertion on package.json structure passes
- Manual smoke (deferred to phase-end human-verify): A365 view title bar shows globe icon when signed in; clicking it opens the same Select Environment QuickPick the status-bar entry opens; icon disappears when signed out.
</verification>

<success_criteria>
PANEL-10 satisfied: globe-icon shortcut for environment switching exists on the view title bar, gated by signed-in state.
</success_criteria>

<output>
Create `.planning/phases/02.1-side-panel-ux/02.1-02-SUMMARY.md` when done.
</output>
