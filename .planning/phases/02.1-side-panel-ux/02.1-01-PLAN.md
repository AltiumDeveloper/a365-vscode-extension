---
phase: 02.1-side-panel-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sidePanel.ts
autonomous: true
requirements:
  - PANEL-07
tags:
  - vscode-tree
  - hierarchy
must_haves:
  truths:
    - "Expanding a workspace shows exactly two child nodes: 'Projects (n)' and 'Scripts (n)' where n is the count"
    - "Expanding 'Projects (n)' lazy-loads and renders the workspace's project nodes"
    - "Expanding 'Scripts (n)' lazy-loads and renders the workspace's script nodes"
    - "Empty categories still render as 'Projects (0)' / 'Scripts (0)' so structure is predictable"
    - "Category nodes carry contextValue 'projectsCategoryNode' / 'scriptsCategoryNode' so they never inherit project/script context menus"
  artifacts:
    - path: "src/sidePanel.ts"
      provides: "A365TreeDataProvider with category-grouped workspace children (D-01..D-03)"
      contains: "projectsCategoryNode"
    - path: "src/sidePanel.ts"
      provides: "Scripts category context value"
      contains: "scriptsCategoryNode"
  key_links:
    - from: "A365TreeDataProvider.getChildren"
      to: "loadWorkspaceChildren (returns category nodes) + new loadCategoryChildren (returns real children)"
      via: "discriminated union on A365Node.kind"
      pattern: "projectsCategory|scriptsCategory"
---

<objective>
G-01 closure: restructure A365TreeDataProvider so a workspace expands to two synthetic category nodes — `Projects (n)` and `Scripts (n)` — each lazy-loading its real children on expand. Reverses Phase 02 D-05 (flat siblings) per Phase 02 HUMAN-UAT.md Gap G-01.

Purpose: makes the tree navigable at scale — users with 10+ projects and scripts can collapse one branch while exploring the other.
Output: refactored `src/sidePanel.ts` whose workspace children are exactly the two category rows, with lazy expansion preserving the existing per-workspace child cache strategy.
</objective>

<execution_context>
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-side-panel-ux/02.1-CONTEXT.md
@.planning/phases/02-side-panel/02-HUMAN-UAT.md
@.planning/phases/02-side-panel/02-VERIFICATION.md
@AGENTS.md
@src/sidePanel.ts

<interfaces>
<!-- Existing A365Node discriminated union shape (src/sidePanel.ts:23-34) executor extends.
     Existing constants: CTX_WORKSPACE='workspaceNode', CTX_PROJECT='projectNode', CTX_SCRIPT='scriptNode' (lines 17-19).
     Existing data fetchers (consumed unchanged): listProjects, listScripts from src/workspace.ts.
     Existing cache: childrenCache: Map<string, A365Node[]> keyed by workspaceId (line 41).
     Existing inline-error wrapping path: catch block in getChildren wraps errors as { kind: 'error', parent: element } (lines 130-140) — must continue to work for category nodes too. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend A365Node union with category kinds and update getTreeItem</name>
  <read_first>
    - src/sidePanel.ts (entire file — current A365Node union, CTX_* constants, getTreeItem switch, loadWorkspaceChildren)
    - .planning/phases/02.1-side-panel-ux/02.1-CONTEXT.md (D-01..D-03, "Hierarchy shape after D-01" diagram in &lt;specifics&gt;)
  </read_first>
  <action>
    Add two new variants to the A365Node discriminated union in src/sidePanel.ts:
      - `{ kind: 'projectsCategory'; workspaceId: string; workspaceAuthId: string; count: number }`
      - `{ kind: 'scriptsCategory'; workspaceId: string; workspaceAuthId: string; workspaceUrl: string; count: number }`
    The `scriptsCategory` variant carries workspaceUrl because individual `script` nodes downstream still need it (existing script-node payload field).

    Export two new contextValue constants alongside the existing CTX_* trio (per D-02):
      - `CTX_PROJECTS_CATEGORY = 'projectsCategoryNode'`
      - `CTX_SCRIPTS_CATEGORY = 'scriptsCategoryNode'`

    Add two `case` branches to `getTreeItem` for the new kinds:
      - Label: `Projects (n)` / `Scripts (n)` using the variant's `count`
      - CollapsibleState: `Collapsed`
      - contextValue: the matching CTX_PROJECTS_CATEGORY / CTX_SCRIPTS_CATEGORY constant
      - iconPath: `new vscode.ThemeIcon('folder-library')` (D-03; the agent's discretion permits substituting `database` or `symbol-folder` if `folder-library` is not a valid codicon — confirm at https://code.visualstudio.com/api/references/icons-in-labels before substituting)
      - No `description` set on category nodes (keep them visually quiet)

    Do not touch the existing workspace/project/script/info/error branches in this task.
  </action>
  <verify>
    <automated>npm run compile</automated>
  </verify>
  <acceptance_criteria>
    - `npm run compile` exits 0
    - `grep -n "projectsCategoryNode" src/sidePanel.ts` matches at least once
    - `grep -n "scriptsCategoryNode" src/sidePanel.ts` matches at least once
    - `grep -n "kind: 'projectsCategory'" src/sidePanel.ts` matches the union declaration
    - `grep -n "kind: 'scriptsCategory'" src/sidePanel.ts` matches the union declaration
    - getTreeItem switch is exhaustive over all seven kinds (no implicit-fallthrough or missing-case TypeScript error)
  </acceptance_criteria>
  <done>
    A365Node union + getTreeItem support the two category kinds. Workspace/project/script/info/error branches still compile and behave identically. No new GraphQL calls, no behavioural change yet (category nodes are not produced by getChildren until Task 2).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace flat sibling fetch with category-parent fetch + lazy category expansion</name>
  <read_first>
    - src/sidePanel.ts (current loadWorkspaceChildren at lines 172-210, getChildren at 121-141, refresh at 49-62, childrenCache field at line 41)
    - .planning/phases/02.1-side-panel-ux/02.1-CONTEXT.md (D-01 — "Empty categories still render (showing `(0)`) so the structure is predictable")
  </read_first>
  <action>
    Restructure data loading so a workspace expansion yields the two category nodes (eagerly counting their children), and each category expansion lazy-loads its real children. Specifically (per D-01):

    1. Rename the existing `childrenCache: Map<string, A365Node[]>` to something kind-specific or replace it with two caches:
       - `projectsCache: Map<string, A365Node[]>` keyed by workspaceId — holds project A365Nodes
       - `scriptsCache: Map<string, A365Node[]>` keyed by workspaceId — holds script A365Nodes
       Update `refresh(node?)` to clear the matching cache(s):
         - `refresh()` with no arg: clear workspacesCache and BOTH per-workspace caches
         - `refresh(node)` for kind 'workspace': delete both projectsCache.get(workspaceId) and scriptsCache.get(workspaceId), then fire(node)
         - `refresh(node)` for kind 'projectsCategory' / 'scriptsCategory': delete only the matching cache entry, then fire(node)

    2. Rewrite `loadWorkspaceChildren(element)` to (a) call `ensureWorkspaceToken` once, (b) run `Promise.all([listProjects, listScripts])` once to count children, (c) populate both per-workspace caches with sorted project/script nodes (sort logic preserved from current code), and (d) return exactly two A365Nodes — a `projectsCategory` and a `scriptsCategory` — each carrying the corresponding `count` plus the workspace's `workspaceAuthId` (and workspaceUrl for scripts). This keeps lazy expansion cheap (no second network round-trip) while still ensuring counts are accurate on first expand.

    3. Add a new branch to `getChildren(element)`:
       - if `element.kind === 'projectsCategory'`: return `projectsCache.get(element.workspaceId) ?? []` (cache MUST have been populated by the parent workspace expansion; if missing, treat as empty array and let the next manual refresh repopulate)
       - if `element.kind === 'scriptsCategory'`: same for scriptsCache

    4. Preserve existing inline-error wrapping (catch block in getChildren) so a fetch failure during workspace expansion still surfaces as an `{ kind: 'error', parent: element }` node attached to the workspace (not silently swallowed). Category expansion does not fetch, so no new error path is needed there.

    Do NOT introduce a separate second fetch on category expansion — the cache is hydrated by the parent workspace expansion (per "lazy-loads its real children on expand" — the load happens once, the render happens lazily on category expand). This matches Phase 02's existing "expand workspace = one round-trip" performance contract.
  </action>
  <verify>
    <automated>npm run compile</automated>
  </verify>
  <acceptance_criteria>
    - `npm run compile` exits 0
    - `grep -n "projectsCache" src/sidePanel.ts` matches at least once (or equivalent kind-keyed cache shape)
    - `grep -n "scriptsCache" src/sidePanel.ts` matches at least once
    - `grep -n "kind: 'projectsCategory'" src/sidePanel.ts | grep -v "type "` matches in loadWorkspaceChildren (node construction site)
    - `grep -n "kind: 'scriptsCategory'" src/sidePanel.ts | grep -v "type "` matches in loadWorkspaceChildren (node construction site)
    - `grep -n "Promise.all" src/sidePanel.ts` still matches (listProjects + listScripts still run in parallel)
    - The string `[...projectNodes, ...scriptNodes]` (the old flat-sibling concat from line 207) no longer appears anywhere in src/sidePanel.ts
    - refresh(node) for the new category kinds compiles (TypeScript exhaustiveness check passes)
  </acceptance_criteria>
  <done>
    Expanding a workspace yields two category rows labelled `Projects (n)` and `Scripts (n)`. Expanding either category reveals its real children from the per-workspace cache populated during the workspace fetch. Empty categories render as `(0)`. Inline error path on workspace expansion still works.
  </done>
</task>

</tasks>

<verification>
- `npm run compile` clean
- All five required greps from Task 2 acceptance pass
- Manual smoke (deferred to phase-end human-verify, not blocking this plan): Activity Bar → expand workspace shows two category rows; expand `Projects (n)` reveals project rows; expand `Scripts (n)` reveals script rows; both with correct counts.
</verification>

<success_criteria>
PANEL-07 satisfied at the code level: A365TreeDataProvider produces category-grouped children for workspaces, with lazy expansion preserving the single-round-trip contract.
</success_criteria>

<output>
Create `.planning/phases/02.1-side-panel-ux/02.1-01-SUMMARY.md` when done.
</output>
