---
phase: 03-remote-script-ops
plan: 03
slug: open-publish
wave: 3
depends_on:
  - 03-01-files-foundation
  - 03-02-fsp-and-wiring
type: execute
autonomous: true
requirements:
  - SCRIPT-02
  - SCRIPT-03
files_modified:
  - src/remoteScriptFs.ts
  - src/workspace.ts          # GraphQL helpers: getScript, updateScript
must_haves:
  truths:
    - "Right-click → Edit Script opens an editor showing the live published source of the remote script (UTF-8, line endings preserved as returned by the Files Service)."
    - "Editing + Ctrl+S pushes the new content via Files Service upload + gloScrUpdateScript mutation; a subsequent close+reopen of the same script reads back the new content."
    - "Save failure (network, auth, GraphQL error) surfaces via VS Code's standard save-failure indicator; the document stays dirty for retry (D-03)."
    - "Right-click → Publish Script on an open dirty `altium365:` document equates to doc.save() (= writeFile)."
  artifacts:
    - path: src/remoteScriptFs.ts
      provides: "readFile + writeFile real implementations (replace the 03-02 stubs)"
    - path: src/workspace.ts
      provides: "getScript(endpoint, token, scriptId) + updateScript(endpoint, token, scriptId, fileToken, comment?)"
      exports: ["getScript", "updateScript"]
  key_links:
    - from: "remoteScriptFs.readFile"
      to: "filesService.downloadByToken"
      via: "two-step: GraphQL getScript → fileToken → Files Service GET"
      pattern: "downloadByToken"
    - from: "remoteScriptFs.writeFile"
      to: "workspace.updateScript"
      via: "two-step: Files Service PUT/POST → updateScript mutation pins token"
      pattern: "updateScript"
---

<objective>
Replace the 03-02 stubs with real readFile and writeFile implementations and add the two GraphQL helpers they need. After this commit SCRIPT-02 and SCRIPT-03 are functionally complete.

Purpose: Deliver the round-trip — open a remote script in VS Code, edit it, save it, see the change reflected on next open.
Output: Single atomic commit. No new modules.
</objective>

<context>
@AGENTS.md
@.planning/phases/03-remote-script-ops/03-CONTEXT.md
@.planning/phases/03-remote-script-ops/03-RESEARCH.md
@.planning/phases/03-remote-script-ops/03-01-PLAN.md
@.planning/phases/03-remote-script-ops/03-02-PLAN.md
@src/remoteScriptFs.ts
@src/workspace.ts
@src/filesService.ts
@src/auth.ts
@src/sidePanel.ts
</context>

<interfaces>
New exports in `src/workspace.ts`:
```ts
export interface ScriptDetail {
    scriptId: string;
    name: string;
    description?: string;
    latestFileToken: string;  // package.fileToken of versions[0]
    latestVersionId: string;
}

export async function getScript(
    endpoint: string,
    workspaceToken: string,
    scriptId: string
): Promise<ScriptDetail>;

export async function updateScript(
    endpoint: string,
    workspaceToken: string,
    scriptId: string,
    fileToken: string,
    comment?: string
): Promise<{ scriptVersionId: string; timestamp: string }>;
```

GraphQL strings (RESEARCH §Code Examples):
```graphql
query GetScriptSource($scriptId: String!) {
  gloScrScript(scriptId: $scriptId) {
    scriptId name description
    versions(first: 1, order: [{ timestamp: DESC }]) {
      nodes { scriptVersionId timestamp comment package { fileToken } }
    }
  }
}

mutation UpdateScript($input: GloScrUpdateScriptInput!) {
  gloScrUpdateScript(input: $input) {
    gloScrScriptVersion { scriptVersionId timestamp }
  }
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add getScript + updateScript helpers to src/workspace.ts</name>
  <files>src/workspace.ts</files>
  <action>
    Append two new exported functions per `<interfaces>` above. Use existing `graphqlRequest` (now throws `GraphQLError` with `code` per Plan 03-01 Task 4). Helper bodies:

    - `getScript`: send `GET_SCRIPT_QUERY` with `{ scriptId }`. Read `data.gloScrScript`; if absent throw `new Error('Script not found: ' + scriptId)`. Read `versions.nodes[0]`; if absent throw `new Error('Script has no published versions: ' + scriptId)`. Return `{ scriptId, name, description, latestFileToken: node.package.fileToken, latestVersionId: node.scriptVersionId }`.

    - `updateScript`: send `UPDATE_SCRIPT_MUTATION` with `{ input: { scriptId, package: { fileToken }, comment } }`. Read `data.gloScrUpdateScript.gloScrScriptVersion`; return `{ scriptVersionId, timestamp }`.

    Both helpers respect 02.3 D-19 by accepting an `endpoint` arg — callers resolve via `getWorkspaceApiUrl` (FSP code in Task 2 does this).

    Place queries as `const` strings at the top of the new code block, with a comment citing the schema introspection file (`.planning/phases/03-remote-script-ops/schema-introspection.json` — referenced by 03-RESEARCH.md).
  </action>
  <verify>
    <automated>npm run compile</automated>
    grep -n "export async function getScript" src/workspace.ts
    grep -n "export async function updateScript" src/workspace.ts
    grep -n "gloScrUpdateScriptInput" src/workspace.ts -i  # mutation input type
  </verify>
  <done>
    Both helpers exported; compile clean; queries cite schema source.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement readFile in src/remoteScriptFs.ts</name>
  <files>src/remoteScriptFs.ts</files>
  <action>
    Replace the stub `readFile` body. Flow:

    1. `const { workspaceId, scriptId } = parseScriptUri(uri);` (already throws FileNotFound on malformed URI).
    2. Resolve `WorkspaceInfo`:
       - Try the cached selected workspace via `getSelectedWorkspace(ctx)`; if its `workspaceId` matches, use it.
       - Else fetch fresh: `const baseToken = await getBaseAccessToken(ctx, cfg); const list = await listWorkspaces(envGlobal, baseToken); const ws = list.find(w => w.workspaceId === workspaceId);` — if not found throw `FileSystemError.FileNotFound(uri)`.
    3. `const cfg = readOAuthConfig();`
    4. `const wsToken = await ensureWorkspaceToken(ctx, cfg, { workspaceId, authId: ws.authId });` (D-08 — no local token cache).
    5. `const apiUrl = getWorkspaceApiUrl(ws, this.getEnvGlobalEndpoint());` (D-07 / D-19).
    6. `const detail = await getScript(apiUrl, wsToken, scriptId);` — wrap in try/catch, map `GraphQLError.code === 'AUTH_NOT_AUTHENTICATED'` to `FileSystemError.NoPermissions(uri)`; everything else → `FileSystemError.Unavailable('Open Script failed: ' + e.message)`; full body to `this.output.appendLine(...)`.
    7. `const filesUrl = getWorkspaceFilesUrl(ws);` (throws actionable on missing per 03-01).
    8. `const bytes = await downloadByToken(filesUrl, detail.latestFileToken, wsToken);`
    9. Return `bytes` (Uint8Array). VS Code decodes per file encoding setting; `.py` extension triggers Python language services automatically (RESEARCH §Don't Hand-Roll).

    Imports needed: `readOAuthConfig`, `getBaseAccessToken`, `ensureWorkspaceToken` from `./auth`; `listWorkspaces`, `getWorkspaceApiUrl`, `getWorkspaceFilesUrl`, `getScript`, `getSelectedWorkspace` from `./workspace`; `downloadByToken` from `./filesService`.

    Logging: one `this.output.appendLine('[Altium 365] readFile <uri>: <bytes> bytes via fileToken <prefix>...')` on success (truncate token to 8-char prefix — never log full token).
  </action>
  <verify>
    <automated>npm run compile</automated>
    # Ensure stub is gone
    ! grep -n "readFile not yet implemented" src/remoteScriptFs.ts
    grep -n "downloadByToken" src/remoteScriptFs.ts
    grep -n "getScript" src/remoteScriptFs.ts
    grep -n "ensureWorkspaceToken" src/remoteScriptFs.ts
    # Token-hygiene: never log full bearer / fileToken
    ! grep -nE "appendLine.*\\\$\\{wsToken" src/remoteScriptFs.ts
  </verify>
  <done>
    `readFile` performs the two-step (GraphQL→fileToken→Files GET); errors map to documented FileSystemError variants; compile clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement writeFile in src/remoteScriptFs.ts</name>
  <files>src/remoteScriptFs.ts</files>
  <action>
    Replace the stub `writeFile` body. Signature: `writeFile(uri, content, options): Promise<void>` where `content: Uint8Array`.

    Flow (D-03 save = publish, D-02 blind overwrite — no precondition):
    1. Parse + resolve workspace + token + `apiUrl` + `filesUrl` exactly as in readFile.
    2. `const fileToken = await uploadAndGetToken(filesUrl, wsToken, content);` — bytes-first per RESEARCH Pattern 1.
    3. `const version = await updateScript(apiUrl, wsToken, scriptId, fileToken, 'Updated via VS Code extension');` — comment string is fixed v1 (no UI for it; D-02 says no etag-style versioning).
    4. Fire `this._onDidChangeFile.fire([{ type: vscode.FileChangeType.Changed, uri }])` so any other open editors of the same URI re-pull. (Defensive — v1 has no realistic multi-editor flow but free to do.)
    5. Log success: `this.output.appendLine('[Altium 365] writeFile <uri>: published <bytes> bytes → scriptVersionId=<...>')`.

    Error mapping (D-11):
    - `GraphQLError.code === 'AUTH_NOT_AUTHENTICATED' || 'UNAUTHORIZED'` → `throw FileSystemError.NoPermissions(uri)` (VS Code shows "Failed to save: Permission denied" — actionable: user re-signs in).
    - GraphQLError.code === 'BAD_USER_INPUT' (RESEARCH Pitfall 2) → `throw FileSystemError.Unavailable('Publish failed: server rejected input (' + (err.message || code) + ')')`.
    - Network / unmapped → `throw FileSystemError.Unavailable('Publish failed: ' + e.message)`.
    - In all error branches: full body (incl. `err.rawErrors` if present) to `this.output.appendLine(...)`.

    NOTE: `options.create` / `options.overwrite` are ignored — A365 has no "create new" semantic in this URI form (SCRIPT-V2-01 deferred). Document this in the function JSDoc.

    Reachability check: a) GraphQLError surfaces from `updateScript` because Plan 03-01 widened `graphqlRequest`. b) Files Service errors surface from `uploadAndGetToken` as plain `Error`. Both branches are exercisable.
  </action>
  <verify>
    <automated>npm run compile</automated>
    ! grep -n "writeFile not yet implemented" src/remoteScriptFs.ts
    grep -n "uploadAndGetToken" src/remoteScriptFs.ts
    grep -n "updateScript" src/remoteScriptFs.ts
    grep -n "FileSystemError.NoPermissions" src/remoteScriptFs.ts
    grep -n "_onDidChangeFile.fire" src/remoteScriptFs.ts
  </verify>
  <done>
    `writeFile` performs two-step publish (Files PUT → updateScript mutation); error mapping covers auth/input/network; compile clean.
  </done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-03-03-01 | Spoofing | Workspace token used against wrong workspace's filesServiceUrl after env switch | mitigate | Each FSP call re-resolves workspace from URI's authority via `listWorkspaces` if not cached; token via `ensureWorkspaceToken` (mutex; per-workspace). |
| T-03-03-02 | Information Disclosure | Bearer token / fileToken logged | mitigate | Logging truncates fileToken to 8-char prefix; never includes `wsToken`; grep-gated. |
| T-03-03-03 | Tampering | Blind overwrite races between two clients | accept | D-02: documented limitation; README caveat in 03-05. Server is ultimate source of truth — last save wins. |
| T-03-03-04 | Repudiation | A5: stale "latest version" if A365 has a separate "current" marker | mitigate (UAT) | Verify in 03-05 UAT step "open → check version" against a script with multiple published versions. Fix-forward in this file if needed. |
| T-03-03-SC | Tampering | New packages | n/a | None added. |
</threat_model>

<invariants_checklist>
- [ ] D-07 / D-19 — `getWorkspaceApiUrl` used for every GraphQL call (`getScript`, `updateScript`); `getWorkspaceFilesUrl` used for every Files Service call
- [ ] D-08 — every call resolves token via `ensureWorkspaceToken`; zero local token state
- [ ] D-09 — single `outputChannel` for diagnostic lines; no new channels
- [ ] D-17 — ActionWait long-poll path NOT touched
- [ ] D-18 — env-switch ordering NOT touched
- [ ] 02.2 auth surface — NOT touched
- [ ] No new commands / no `package.json` change (wiring landed in 03-02)
</invariants_checklist>

<manual_uat>
Lives in 03-05 fully; smoke check after this commit:
1. Sign in. Pick a workspace that contains at least one script. Tree → script → Edit Script. Expect real source content in the editor (verify against the portal).
2. Add a one-line change. Save (`Ctrl+S`). Expect no error toast. Output channel shows "writeFile ... published ... bytes → scriptVersionId=...".
3. Close the editor and re-open via Edit Script. Expect the changed content.
4. Tree → second different script → Edit Script. Then `Ctrl+W` to close. Tree → original script → Edit Script. Expect ORIGINAL script content (not the second one — URI keying works).
5. Disconnect network → `Ctrl+S` → expect VS Code's "Failed to save" indicator + dirty state preserved.
</manual_uat>

<success_criteria>
- `npm run compile` clean.
- SCRIPT-02: open a remote script → see live source ✅
- SCRIPT-03: edit + save → mutation lands → next open shows new content ✅
- Save failures preserve dirty state for retry (D-03).
- No regression in 02.x flows.
</success_criteria>

<output>
Create `.planning/phases/03-remote-script-ops/03-03-SUMMARY.md` recording any deviations from RESEARCH (e.g. if A5 stale-version assumption proved wrong and the query had to change).
</output>
