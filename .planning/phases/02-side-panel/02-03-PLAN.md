---
phase: 02-side-panel
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: true
requirements: [PANEL-01, PANEL-05]
must_haves:
  truths:
    - "Activity Bar shows an Altium 365 container icon"
    - "Container opens a 'Workspaces' tree view (id `altium365.tree`)"
    - "When signed out, the tree view shows a 'Sign In' welcome button that runs `altium365.signIn`"
    - "Right-click on a script node shows four context menu actions in stable group order"
    - "Tree view title shows a refresh icon that runs `altium365.tree.refresh`"
    - "Seven new command IDs are declared in contributes.commands"
  artifacts:
    - path: "package.json"
      provides: "contributes.viewsContainers.activitybar.altium365"
      contains: "viewsContainers"
    - path: "package.json"
      provides: "contributes.views.altium365 with id `altium365.tree`"
      contains: "altium365.tree"
    - path: "package.json"
      provides: "contributes.viewsWelcome gated by `!altium365.signedIn`"
      contains: "viewsWelcome"
    - path: "package.json"
      provides: "7 new commands: tree.refresh, tree.retryNode, statusBar.click, script.runLocal, script.edit, script.executeRemote, script.publish"
      contains: "altium365.script.runLocal"
    - path: "package.json"
      provides: "menus.view/title (refresh) + menus.view/item/context (4 script actions gated by viewItem == scriptNode)"
      contains: "view/item/context"
  key_links:
    - from: "package.json contributes.viewsContainers.activitybar[0].id (`altium365`)"
      to: "package.json contributes.views.altium365 key"
      via: "string id must match exactly"
      pattern: "\"altium365\""
    - from: "package.json contributes.menus.view/item/context[].when"
      to: "TreeItem.contextValue `scriptNode` set in Plan 04"
      via: "viewItem == scriptNode literal"
      pattern: "viewItem == scriptNode"
    - from: "package.json contributes.viewsWelcome[].when (`!altium365.signedIn`)"
      to: "setContext('altium365.signedIn', ...) calls in Plan 04 / Plan 05"
      via: "VS Code context key"
      pattern: "altium365.signedIn"
---

<objective>
Add all `contributes` manifest entries for Phase 2 to `package.json`: Activity Bar container, tree view, signed-out welcome, view-title refresh action, item-context menus, and seven new command declarations. Pure declarative work — no TypeScript changes. Plans 04, 05, 06 register the matching handlers and TreeDataProvider.

Purpose: PANEL-01 (Activity Bar entry point) + PANEL-05 declarative half (commands + menus). Per D-05 viewsWelcome handles signed-out state; per D-08 all four script commands must be declared NOW so Phase 3 can swap handlers without re-touching `package.json`. Per RESEARCH.md Pitfall 1 the three IDs (`viewsContainers[].id`, `views.<key>`, the string `registerTreeDataProvider` will receive in Plan 04) MUST match — they all equal `altium365.tree` for the view and `altium365` for the container.
Output: Extended `contributes` block in `package.json`. No TS files touched.
</objective>

<execution_context>
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-side-panel/02-CONTEXT.md
@.planning/phases/02-side-panel/02-RESEARCH.md
@.planning/phases/02-side-panel/02-PATTERNS.md
@AGENTS.md
@package.json

<interfaces>
Existing package.json structure (DO NOT remove or rename any existing key):
- engines.vscode "^1.85.0" — satisfies viewsWelcome (≥1.45) ✓
- contributes.commands: 6 commands (signIn, signOut, selectWorkspace, runScript, debugScript, selectEnvironment) — keep all
- contributes.configuration.properties: 14 settings — keep all
- contributes.menus: editor/title/run, explorer/context — keep all, add `view/title` + `view/item/context`

Commands to declare (7 new):
  altium365.tree.refresh           — title "Altium 365: Refresh Workspaces", category "Altium 365", icon $(refresh)
  altium365.tree.retryNode         — title "Altium 365: Retry", category "Altium 365" (not user-facing in command palette but must be declared; handler in Plan 04)
  altium365.statusBar.click        — title "Altium 365: Status Bar Menu", category "Altium 365"
  altium365.script.runLocal        — title "Altium 365: Run Script (Local)", category "Altium 365"
  altium365.script.edit            — title "Altium 365: Edit Script", category "Altium 365"
  altium365.script.executeRemote   — title "Altium 365: Execute Script Remotely", category "Altium 365"
  altium365.script.publish         — title "Altium 365: Publish Script", category "Altium 365"

Context key contract (set by Plan 04 + Plan 05; consumed by this plan's viewsWelcome `when`):
  altium365.signedIn (boolean)

Context value contract (set by Plan 04 TreeItems; consumed by this plan's menu `when` clauses):
  scriptNode    — script leaf
  workspaceNode — workspace node (no menus in v1 but reserved)
  projectNode   — project leaf (no menus in v1)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add viewsContainers + views + viewsWelcome blocks</name>
  <files>package.json</files>
  <read_first>
    - package.json (entire file, 204 lines)
    - .planning/phases/02-side-panel/02-RESEARCH.md §Code Examples §"Activity Bar contribution" (lines 420-439) and §Pattern 5 (viewsWelcome, lines 330-343)
    - .planning/phases/02-side-panel/02-PATTERNS.md §`package.json` — order convention
  </read_first>
  <behavior>
    - Activity Bar in VS Code shows a new entry titled "Altium 365"
    - Clicking it opens an empty container with one view "Workspaces"
    - When `altium365.signedIn` context key is unset/false, the Workspaces view body shows a welcome panel containing the link `[Sign In](command:altium365.signIn)` (rendered as a button)
    - When `altium365.signedIn` becomes true, the welcome panel hides automatically
  </behavior>
  <action>
    Edit `package.json`. Inside the existing `contributes` object (currently has `commands`, `configuration`, `menus`), insert three new top-level keys. Place them BEFORE the existing `commands` key for diff readability (matching RESEARCH.md Code Examples ordering):

    1. `viewsContainers`:
       - Add `activitybar` array containing one entry: `{ "id": "altium365", "title": "Altium 365", "icon": "$(circuit-board)" }`.
       - Per D-discretion + RESEARCH.md Open Question 3, use the Codicon `$(circuit-board)` initially — no shipped SVG asset required, auto-themes.

    2. `views`:
       - Key `altium365` (matches the container id literally — critical per Pitfall 1).
       - Value: an array with one entry: `{ "id": "altium365.tree", "name": "Workspaces" }`.
       - The id `altium365.tree` is the canonical view id used by `registerTreeDataProvider`/`createTreeView` in Plan 04 and by every `when: view == altium365.tree` clause in this plan.

    3. `viewsWelcome` (per D-05):
       - Single entry: `{ "view": "altium365.tree", "contents": "You are not signed in to Altium 365.\n\n[Sign In](command:altium365.signIn)\n\nLearn more about [Altium 365](https://www.altium.com/altium-365).", "when": "!altium365.signedIn" }`.
       - The command link syntax `[label](command:altium365.signIn)` is intentional — VS Code parses it as an executable command link.

    Use the Edit tool to insert these into the existing `contributes` object. Keep all existing commands/configuration/menus blocks untouched. Maintain valid JSON throughout (no trailing commas, proper quote escaping inside the welcome `contents` string).
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('package.json','utf-8'))" && npm run compile</automated>
    Plus:
      node -e "const p = JSON.parse(require('fs').readFileSync('package.json','utf-8')); const c = p.contributes; if(!c.viewsContainers?.activitybar?.find(x => x.id === 'altium365')) throw new Error('missing activitybar entry'); if(!c.views?.altium365?.find(x => x.id === 'altium365.tree')) throw new Error('missing view'); if(!c.viewsWelcome?.find(x => x.view === 'altium365.tree' && x.when === '!altium365.signedIn')) throw new Error('missing welcome'); console.log('ok');"
    Must print `ok`.
  </verify>
  <acceptance_criteria>
    - `package.json` is valid JSON (no parse error)
    - `contributes.viewsContainers.activitybar[0].id` === `"altium365"`
    - `contributes.views.altium365[0].id` === `"altium365.tree"`
    - `contributes.views.altium365[0].name` === `"Workspaces"`
    - `contributes.viewsWelcome[0].view` === `"altium365.tree"`, `.when` === `"!altium365.signedIn"`, `.contents` contains the substring `[Sign In](command:altium365.signIn)`
    - No existing key removed or renamed
  </acceptance_criteria>
  <done>Activity Bar container, tree view, and viewsWelcome declared; JSON valid; compile clean</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Declare 7 new commands and add view/title + view/item/context menus</name>
  <files>package.json</files>
  <read_first>
    - package.json (especially contributes.commands lines 21-52 and contributes.menus lines 165-190 for style)
    - .planning/phases/02-side-panel/02-RESEARCH.md §Pattern 2 (lines 248-277) — exact menus shape with group ordering
    - .planning/phases/02-side-panel/02-PATTERNS.md §`package.json` — commands + menus patterns
  </read_first>
  <behavior>
    - `Ctrl+Shift+P` lists all 7 new commands under the "Altium 365" category
    - The Workspaces tree title bar shows a refresh icon (`$(refresh)`) when the view is visible
    - Right-clicking a TreeItem whose contextValue equals `scriptNode` shows four context menu items in groups: `1_run@1` Run Local, `1_run@2` Execute Remotely, `2_edit@1` Edit, `2_edit@2` Publish
    - Right-clicking a TreeItem with contextValue `workspaceNode` or `projectNode` shows none of the four script actions
  </behavior>
  <action>
    Continue editing `package.json`:

    1. Append 7 new entries to the existing `contributes.commands` array. Each follows the existing 3-field shape `{ "command": "...", "title": "Altium 365: ...", "category": "Altium 365" }`. For `altium365.tree.refresh` ALSO include `"icon": "$(refresh)"` so the view-title menu can render it as an icon button. Exact entries:
       - `altium365.tree.refresh` — title `"Altium 365: Refresh Workspaces"`, icon `"$(refresh)"`
       - `altium365.tree.retryNode` — title `"Altium 365: Retry Tree Node"` (declared so the TreeItem.command binding in Plan 04 resolves; NOT meant for the Command Palette, but VS Code still requires the declaration)
       - `altium365.statusBar.click` — title `"Altium 365: Status Bar Menu"`
       - `altium365.script.runLocal` — title `"Altium 365: Run Script (Local)"`
       - `altium365.script.edit` — title `"Altium 365: Edit Script"`
       - `altium365.script.executeRemote` — title `"Altium 365: Execute Script Remotely"`
       - `altium365.script.publish` — title `"Altium 365: Publish Script"`

    2. Extend `contributes.menus` (currently has `editor/title/run` and `explorer/context`). Add TWO new top-level menu keys:

       a. `view/title` — array with one entry binding refresh to the tree's title bar:
          `{ "command": "altium365.tree.refresh", "when": "view == altium365.tree", "group": "navigation" }`

       b. `view/item/context` — array with FOUR entries, one per script action. Per RESEARCH.md Pattern 2 the group naming convention is `<groupPrefix>@<orderInGroup>`. Use:
          - `{ "command": "altium365.script.runLocal", "when": "view == altium365.tree && viewItem == scriptNode", "group": "1_run@1" }`
          - `{ "command": "altium365.script.executeRemote", "when": "view == altium365.tree && viewItem == scriptNode", "group": "1_run@2" }`
          - `{ "command": "altium365.script.edit", "when": "view == altium365.tree && viewItem == scriptNode", "group": "2_edit@1" }`
          - `{ "command": "altium365.script.publish", "when": "view == altium365.tree && viewItem == scriptNode", "group": "2_edit@2" }`

       This places "Run Locally" and "Execute Remotely" in the first menu group and "Edit" / "Publish" in a second group — matches the natural mental model (run vs author) and is the order RESEARCH.md Pattern 2 prescribes.

    3. Maintain valid JSON. Do NOT add `commandPalette` exclusions for `altium365.tree.retryNode` in v1 (RESEARCH.md does not call this out; deferred polish).

    Do not modify `activationEvents` — the new view contribution implicitly triggers `onView:altium365.tree` per RESEARCH.md Anti-Patterns; the existing empty `activationEvents: []` is correct.
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('package.json','utf-8'))" && npm run compile</automated>
    Plus:
      node -e "const p = JSON.parse(require('fs').readFileSync('package.json','utf-8')); const c = p.contributes; const cmds = c.commands.map(x => x.command); const need = ['altium365.tree.refresh','altium365.tree.retryNode','altium365.statusBar.click','altium365.script.runLocal','altium365.script.edit','altium365.script.executeRemote','altium365.script.publish']; for (const n of need) if (!cmds.includes(n)) throw new Error('missing command ' + n); if (!c.menus['view/title']?.find(x => x.command === 'altium365.tree.refresh')) throw new Error('missing view/title refresh'); const ctxMenus = c.menus['view/item/context'] || []; if (ctxMenus.length < 4) throw new Error('view/item/context needs 4 entries'); for (const cmd of ['altium365.script.runLocal','altium365.script.edit','altium365.script.executeRemote','altium365.script.publish']) if (!ctxMenus.find(x => x.command === cmd && x.when.includes('viewItem == scriptNode'))) throw new Error('missing item-context entry for ' + cmd); console.log('ok');"
    Must print `ok`.
  </verify>
  <acceptance_criteria>
    - `package.json` is valid JSON
    - All 7 new command IDs present in `contributes.commands` with category `"Altium 365"`
    - `altium365.tree.refresh` command entry has `"icon": "$(refresh)"`
    - `contributes.menus["view/title"]` contains one refresh entry gated by `view == altium365.tree` in group `navigation`
    - `contributes.menus["view/item/context"]` contains exactly the 4 script entries, each gated by `view == altium365.tree && viewItem == scriptNode`, with groups `1_run@1`, `1_run@2`, `2_edit@1`, `2_edit@2` in some order
    - Existing 6 commands and existing menus (editor/title/run, explorer/context) still present unchanged
    - `npm run compile` exits 0 (no TS regressions — config-only change)
  </acceptance_criteria>
  <done>All commands declared, all menus wired; node JSON-validation script prints ok; compile clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VS Code manifest → extension host | declarative contributes drive UI surface; no code execution from manifest itself |
| user click → command id resolution | menu `when` clauses scope which nodes expose script actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering (context-menu leak on wrong node) | menu `when` clauses | mitigate | All four script-menu entries gate on `viewItem == scriptNode` AND `view == altium365.tree`; Plan 04's TreeItem.contextValue strings are the matching tokens. RESEARCH.md Pitfall 3 directly addressed. |
| T-02-03-02 | Spoofing (welcome view persists after sign-in) | viewsWelcome `when` clause | mitigate | `!altium365.signedIn` requires Plan 04/05 to call `setContext('altium365.signedIn', boolean)` on every auth state change. RESEARCH.md Pitfall 2 documented; cross-plan contract verified in Plan 04 Task 1. |
| T-02-03-03 | DoS (command not found) | declared commands without registered handlers | mitigate | All 7 commands get `registerCommand` calls in Plans 04 (`tree.refresh`, `tree.retryNode`), 05 (`statusBar.click`), 06 (4 × `script.*`). RESEARCH.md Pitfall 6 directly addressed. |
| T-02-03-04 | Information Disclosure (command in palette user shouldn't invoke) | `altium365.tree.retryNode` exposed via palette | accept | Low risk: invoking with no/invalid argument logs an error via outputChannel; commandPalette exclusion deferred to polish. |
| T-02-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages added; engines.vscode unchanged. RESEARCH.md Package Legitimacy Audit N/A. |
</threat_model>

<verification>
- `node -e "JSON.parse(require('fs').readFileSync('package.json','utf-8'))"` exits 0
- `npm run compile` exits 0
- The two Node validation scripts in Task 1 and Task 2 verify blocks both print `ok`
- Install the freshly built extension in an Extension Development Host (manual smoke check, not automated): Activity Bar shows the new icon; tree shows the Sign In welcome view (because `altium365.signedIn` is not yet set anywhere)
</verification>

<success_criteria>
- PANEL-01 declarative half complete: Activity Bar container + view registered
- PANEL-05 declarative half complete: 4 script commands + their context-menu wiring declared
- All command IDs Phase 2 will register (7 total) declared in the manifest, satisfying RESEARCH.md Pitfall 6 prerequisite
- Pitfall 1 satisfied: `viewsContainers[].id` (`altium365`), `views.<key>` (`altium365`), and the view id (`altium365.tree`) are consistent
</success_criteria>

<output>
Create `.planning/phases/02-side-panel/02-03-SUMMARY.md` when done.
</output>
