---
phase: 02-side-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/workspace.ts
autonomous: true
requirements: [SCRIPT-01]
must_haves:
  truths:
    - "Calling listScripts(endpoint, workspaceToken) returns ScriptInfo[] for the workspace"
    - "GraphQL or HTTP errors propagate as thrown Error (caller catches at boundary)"
    - "Empty workspace returns [] (not undefined, not throw)"
  artifacts:
    - path: "src/workspace.ts"
      provides: "listScripts() function + ScriptInfo interface"
      contains: "export async function listScripts"
    - path: "src/workspace.ts"
      provides: "ScriptInfo type with scriptId, name, description?"
      contains: "export interface ScriptInfo"
  key_links:
    - from: "src/workspace.ts:listScripts"
      to: "src/workspace.ts:graphqlRequest"
      via: "reuse existing GraphQL helper"
      pattern: "graphqlRequest\\(endpoint, .*workspaceToken.*, LIST_SCRIPTS"
---

<objective>
Add the `listScripts(endpoint, workspaceToken)` helper to `src/workspace.ts` that calls the workspace-scoped `gloScrScripts` GraphQL query and returns a typed `ScriptInfo[]`. This is the GraphQL plumbing SCRIPT-01 needs; the tree provider in Plan 04 will consume it.

Purpose: SCRIPT-01 — list all scripts in a workspace via `gloScrScripts`. Per RESEARCH.md §Schema Finding, the query is workspace-scoped (no projectId arg), confirming PANEL-04's "scripts as sibling to projects" tree shape.
Output: New exported `ScriptInfo` interface and `listScripts` function in `src/workspace.ts`.
</objective>

<execution_context>
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-side-panel/02-CONTEXT.md
@.planning/phases/02-side-panel/02-RESEARCH.md
@.planning/phases/02-side-panel/02-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@AGENTS.md
@src/workspace.ts

<interfaces>
Existing exports in src/workspace.ts (already imported by sidePanel.ts/extension.ts in later plans):

  export interface WorkspaceInfo { name: string; workspaceId: string; authId: string; }
  export interface ProjectInfo { id: string; name: string; }
  export async function graphqlRequest(endpoint: string, accessToken: string, query: string, variables?: Record<string, unknown>): Promise<any>
  export async function listWorkspaces(endpoint: string, accessToken: string): Promise<WorkspaceInfo[]>
  export async function listProjects(endpoint: string, accessToken: string): Promise<ProjectInfo[]>

New exports this plan adds (consumed by Plan 04 sidePanel.ts and Plan 06 runLocal):

  export interface ScriptInfo { scriptId: string; name: string; description?: string; }
  export async function listScripts(endpoint: string, workspaceToken: string): Promise<ScriptInfo[]>

The `endpoint` argument is the workspace-scoped GraphQL URL (e.g. `${workspaceUrl}/api` per RESEARCH.md Code Examples). Caller is responsible for selecting the correct endpoint; this function does not do endpoint resolution.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add ScriptInfo interface and listScripts() to src/workspace.ts</name>
  <files>src/workspace.ts</files>
  <read_first>
    - src/workspace.ts (entire file, 122 lines) — model after `listProjects` at lines 106-122
    - .planning/phases/02-side-panel/02-PATTERNS.md §`src/workspace.ts` — exact template
    - .planning/phases/02-side-panel/02-RESEARCH.md §Code Examples §"Script-list GraphQL query" — query shape
  </read_first>
  <behavior>
    - listScripts(endpoint, "BAD_TOKEN") rejects with Error containing "GraphQL HTTP" or "GraphQL errors" (propagated from graphqlRequest)
    - listScripts(endpoint, validToken) on workspace with zero scripts resolves to []
    - listScripts return value is always an Array (defensive null-coalesce when data.gloScrScripts.nodes is missing)
    - ScriptInfo includes scriptId (string), name (string), description (string | undefined)
  </behavior>
  <action>
    Append a new section to `src/workspace.ts` after the existing `listProjects` function (currently line 122). Per D-01/D-04/D-09 the function is workspace-scoped (no projectId argument); per RESEARCH.md Schema Finding §1 the query is `gloScrScripts(first, after)` returning `{ pageInfo, nodes { scriptId name description createdAt } }`.

    Mirror the structure of `listProjects` exactly:
      1. Define `export interface ScriptInfo { scriptId: string; name: string; description?: string; }` immediately above the function.
      2. Define a module-private constant for the query string (multi-line template literal) named `LIST_SCRIPTS_QUERY` — select fields `scriptId`, `name`, `description` (omit `createdAt` — not used by tree). Query takes `$first: Int = 100` only; no `after` variable in v1 per RESEARCH.md Assumption A3.
      3. Export `async function listScripts(endpoint: string, workspaceToken: string): Promise<ScriptInfo[]>` that calls `graphqlRequest(endpoint, workspaceToken, LIST_SCRIPTS_QUERY, { first: 100 })`.
      4. Defensive return: `const nodes = data?.gloScrScripts?.nodes; return Array.isArray(nodes) ? (nodes as ScriptInfo[]) : [];` — matches the null-coalesce style of `listProjects`.
      5. Do NOT wrap graphqlRequest in try/catch — let errors propagate so callers (sidePanel.ts in Plan 04, runLocal in Plan 06) can convert them to tree error nodes or showErrorMessage per D-07 / S1.
      6. Do NOT introduce module-level state. Do NOT add pagination handling in v1 — per RESEARCH.md Assumption A3 a 100-item page is sufficient; leave a `// TODO: paginate if hasNextPage` comment referencing the assumption.
      7. Add the new symbols to the file's export surface naturally (named exports, no barrel file).

    Naming MUST match: function `listScripts`, interface `ScriptInfo`, constant `LIST_SCRIPTS_QUERY`. Plan 04 and Plan 06 import these names by string.
  </action>
  <verify>
    <automated>npm run compile</automated>
    Also verify symbols exist:
      grep -nE "^export async function listScripts" src/workspace.ts
      grep -nE "^export interface ScriptInfo" src/workspace.ts
      grep -n "gloScrScripts" src/workspace.ts
    All three greps must return exactly one match.
  </verify>
  <acceptance_criteria>
    - `npm run compile` exits 0 (no TS errors)
    - `src/workspace.ts` exports `listScripts` (named export, async, two parameters: endpoint, workspaceToken)
    - `src/workspace.ts` exports `ScriptInfo` interface with `scriptId: string`, `name: string`, `description?: string`
    - Query string contains literal `gloScrScripts` and `first: Int`
    - Function signature does NOT include a `projectId` parameter (workspace-scoped per RESEARCH.md Schema Finding §1)
    - No new npm dependencies added (package.json unchanged)
    - No module-level mutable state added (CONVENTIONS.md §Module Design)
  </acceptance_criteria>
  <done>Compile passes; grep confirms exports; function delegates to existing `graphqlRequest` and returns ScriptInfo[]</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extension host → A365 GraphQL endpoint | workspace-scoped access token crosses HTTPS boundary in Authorization header |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Information Disclosure | `listScripts` token argument | mitigate | Token is passed in-memory only; never logged. `graphqlRequest` sends as `Authorization: Bearer` header per workspace.ts:28 — no change to existing pattern. |
| T-02-01-02 | Tampering (GraphQL injection) | LIST_SCRIPTS_QUERY string | accept | Query is a hard-coded template literal; only variable is `first: 100` (typed Int). No string interpolation of user input into the query body. |
| T-02-01-03 | Denial of Service | unbounded result list | accept | Capped at `first: 100` per RESEARCH.md Assumption A3; pagination deferred to v2 with TODO comment in code. |
| T-02-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages added in this plan; Package Legitimacy Audit N/A per RESEARCH.md. |
</threat_model>

<verification>
- `npm run compile` passes
- `grep -c "^export async function listScripts" src/workspace.ts | grep -v '^#'` returns 1
- `grep -c "^export interface ScriptInfo" src/workspace.ts | grep -v '^#'` returns 1
- No regressions: existing `listProjects`, `listWorkspaces`, `graphqlRequest` exports still present (grep each, expect 1)
</verification>

<success_criteria>
- SCRIPT-01 GraphQL plumbing exists: `listScripts(endpoint, workspaceToken)` is callable from any module
- Function signature is workspace-scoped (no projectId), aligning the tree with D-04 / RESEARCH.md Schema Finding §1
- Errors propagate (do not get swallowed), enabling D-07 tree-error-item handling downstream
</success_criteria>

<output>
Create `.planning/phases/02-side-panel/02-01-SUMMARY.md` when done.
</output>
