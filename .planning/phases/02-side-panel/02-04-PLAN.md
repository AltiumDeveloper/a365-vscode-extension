---
phase: 02-side-panel
plan: 04
type: execute
wave: 2
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - src/sidePanel.ts
  - src/extension.ts
autonomous: true
requirements: [PANEL-01, PANEL-02, PANEL-03, PANEL-04]
must_haves:
  truths:
    - "Opening the A365 view fires getChildren(undefined) and renders one TreeItem per workspace from listWorkspaces"
    - "Expanding a workspace fires getChildren(workspaceNode) and renders its projects AND scripts as siblings"
    - "Refreshing via title-bar icon clears the in-memory cache and re-fetches the visible level"
    - "On fetch failure, an error TreeItem appears at that level with the message; clicking it re-runs the failed fetch"
    - "On signed-in account with zero workspaces, a single info TreeItem 'No workspaces available for this account' is rendered (D-06)"
    - "Auth state flips `altium365.signedIn` context key so viewsWelcome hides on sign-in and reappears on sign-out"
    - "Script TreeItems have contextValue == 'scriptNode' so the four context-menu commands appear on right-click"
  artifacts:
    - path: "src/sidePanel.ts"
      provides: "A365TreeDataProvider class, A365Node union, OUTPUT_PREFIX context-value constants"
      contains: "implements vscode.TreeDataProvider<A365Node>"
    - path: "src/extension.ts"
      provides: "TreeView registration + setContext('altium365.signedIn', ...) wiring on signIn/signOut/env-switch"
      contains: "createTreeView('altium365.tree'"
  key_links:
    - from: "src/sidePanel.ts:A365TreeDataProvider.getChildren(workspace)"
      to: "src/auth.ts:ensureWorkspaceToken"
      via: "lazy per-workspace token exchange"
      pattern: "ensureWorkspaceToken\\("
    - from: "src/sidePanel.ts:A365TreeDataProvider.getChildren(workspace)"
      to: "src/workspace.ts:listProjects + src/workspace.ts:listScripts"
      via: "parallel fetch of project + script siblings"
      pattern: "Promise\\.all|listScripts\\(|listProjects\\("
    - from: "src/extension.ts activate() onAuthStateChanged subscription"
      to: "setContext('altium365.signedIn', boolean) + provider.refresh()"
      via: "react to sign-in/out events emitted by auth.ts"
      pattern: "onAuthStateChanged\\("
    - from: "TreeItem contextValue 'scriptNode'"
      to: "package.json menus['view/item/context'] when clauses"
      via: "literal string match (declared in Plan 03)"
      pattern: "scriptNode"
---

<objective>
Implement the side-panel TreeDataProvider in a new file `src/sidePanel.ts` and wire it into `activate()` in `src/extension.ts`. Provider renders workspaces lazily, expands each into projects + scripts as siblings (PANEL-04), uses inline error TreeItems on failure (D-07), an info item on empty (D-06), and refreshes on auth-state events.

Purpose: PANEL-01 (registered view), PANEL-02 (workspaces at root), PANEL-03 (projects under workspace), PANEL-04 (scripts as siblings of projects, per D-decision + RESEARCH.md Schema Finding §1). D-08 contextValue strings expose the four script actions declared in Plan 03. RESEARCH.md Pitfall 2 mandates `setContext('altium365.signedIn', ...)` on every auth-state change so the viewsWelcome (Plan 03) toggles correctly.
Output: New `src/sidePanel.ts` file, edits to `src/extension.ts` activate() + signIn/signOut/env-switch helpers.
</objective>

<execution_context>
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-side-panel/02-CONTEXT.md
@.planning/phases/02-side-panel/02-RESEARCH.md
@.planning/phases/02-side-panel/02-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@AGENTS.md
@src/extension.ts
@src/auth.ts
@src/workspace.ts

<interfaces>
Consumed (from Plans 01, 02, 03, and existing code):

  // From src/workspace.ts (Plan 01 + existing)
  export interface WorkspaceInfo { name: string; workspaceId: string; authId: string; }
  export interface ProjectInfo { id: string; name: string; }
  export interface ScriptInfo { scriptId: string; name: string; description?: string; }
  export async function listWorkspaces(endpoint: string, accessToken: string): Promise<WorkspaceInfo[]>
  export async function listProjects(endpoint: string, accessToken: string): Promise<ProjectInfo[]>
  export async function listScripts(endpoint: string, workspaceToken: string): Promise<ScriptInfo[]>

  // From src/auth.ts (Plan 02 + existing)
  export interface OAuthConfig { ... }
  export interface AuthState { user?: string; environment?: string; signedIn: boolean; }
  export const onAuthStateChanged: vscode.Event<AuthState>;
  export function readOAuthConfig(): OAuthConfig
  export async function getStoredTokens(context): Promise<TokenSet | undefined>
  export async function ensureWorkspaceToken(context, cfg, { workspaceId, authId }): Promise<string>

  // From src/extension.ts (existing)
  let outputChannel: vscode.OutputChannel;  // module-level singleton; pass into provider

  // From package.json contributes (Plan 03)
  view id: "altium365.tree"
  context key: "altium365.signedIn"
  contextValue strings: "workspaceNode", "projectNode", "scriptNode"

New exports this plan adds (consumed by Plan 06 runLocal handler):

  // From src/sidePanel.ts
  export type A365Node =
    | { kind: 'workspace'; info: WorkspaceInfo; workspaceUrl: string }
    | { kind: 'project';   workspaceId: string; project: ProjectInfo }
    | { kind: 'script';    workspaceId: string; workspaceAuthId: string; workspaceUrl: string; script: ScriptInfo }
    | { kind: 'info';      label: string }
    | { kind: 'error';     label: string; parent: A365Node | undefined };
  export const CTX_WORKSPACE = 'workspaceNode';
  export const CTX_PROJECT = 'projectNode';
  export const CTX_SCRIPT = 'scriptNode';
  export class A365TreeDataProvider implements vscode.TreeDataProvider<A365Node> { ... refresh(node?: A365Node): void }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create src/sidePanel.ts with A365TreeDataProvider + A365Node union + contextValue constants</name>
  <files>src/sidePanel.ts</files>
  <read_first>
    - src/workspace.ts (lines 50-122 — function shapes and graphqlRequest behavior)
    - src/auth.ts (full file post-Plan-02 — to confirm ensureWorkspaceToken signature)
    - src/extension.ts (lines 1-36, 96-103 for outputChannel + interface placement convention)
    - .planning/phases/02-side-panel/02-RESEARCH.md §Pattern 1 (lines 168-244 — A365TreeDataProvider template), §Pitfalls 3 and 4
    - .planning/phases/02-side-panel/02-PATTERNS.md §`src/sidePanel.ts` (DI of context + outputChannel)
    - .planning/codebase/CONVENTIONS.md (4-space indent, single quotes, trailing commas, PascalCase interfaces at top of owning file, named imports for local modules)
  </read_first>
  <behavior>
    - Constructed with (context, outputChannel, getEndpoint) — no module-level state
    - getChildren(undefined) returns workspace nodes from listWorkspaces using the base access token; returns one `info` node when the list is empty
    - getChildren(workspaceNode) returns a flat array of project nodes followed by script nodes (siblings, no grouping header in v1)
    - getChildren(projectNode) returns [] (projects are leaves in Phase 2)
    - getChildren(scriptNode) returns [] (scripts are leaves)
    - On any thrown error inside getChildren, returns a single `error` node whose label is the error message; clicking the error node fires `altium365.tree.retryNode` which calls refresh(parent) to retry that level
    - refresh() with no argument clears all caches and fires onDidChangeTreeData(undefined); refresh(node) clears the cache slot for that subtree and fires onDidChangeTreeData(node)
    - All outputChannel diagnostics are prefixed `[Altium 365] tree:`
  </behavior>
  <action>
    Create new file `src/sidePanel.ts`. Follow CONVENTIONS.md: 4-space indent, single quotes, namespace import for vscode, named imports for local modules, interfaces at top.

    1. Imports (order per CONVENTIONS.md):
       - `import * as vscode from 'vscode';`
       - Named from `./auth`: `OAuthConfig`, `getStoredTokens`, `ensureWorkspaceToken`
       - Named from `./workspace`: `WorkspaceInfo`, `ProjectInfo`, `ScriptInfo`, `listWorkspaces`, `listProjects`, `listScripts`

    2. Exported constants (string literals — must match package.json menus from Plan 03):
       - `export const CTX_WORKSPACE = 'workspaceNode';`
       - `export const CTX_PROJECT = 'projectNode';`
       - `export const CTX_SCRIPT = 'scriptNode';`

    3. Exported discriminated union `A365Node` exactly as shown in <interfaces>. Notes:
       - `workspaceUrl` on `workspace` and `script` nodes is the workspace-scoped GraphQL base URL. For v1 use the same configured `altium365.graphqlEndpoint` for ALL workspaces (existing project assumption — see src/extension.ts:307 and 421). Per-workspace URL routing is deferred to v2.
       - `error` node carries the `parent` so retry can re-fire that subtree.

    4. Exported class `A365TreeDataProvider implements vscode.TreeDataProvider<A365Node>`:
       - Private `_onDidChange = new vscode.EventEmitter<A365Node | undefined | void>();`
       - Public readonly `onDidChangeTreeData = this._onDidChange.event;`
       - Constructor takes `(private ctx: vscode.ExtensionContext, private output: vscode.OutputChannel, private getEndpoint: () => string)` — DI per PATTERNS.md §Pattern S6; the `getEndpoint` callback reads the current `altium365.graphqlEndpoint` setting at fetch time so env switches reflect immediately (PATTERNS.md S5).
       - Private cache: `private workspacesCache?: A365Node[];` and `private childrenCache = new Map<string, A365Node[]>();` keyed by `workspaceId`.
       - `refresh(node?: A365Node)`:
         - If `!node`: `this.workspacesCache = undefined; this.childrenCache.clear(); this._onDidChange.fire(undefined);`
         - Else if `node.kind === 'workspace'`: `this.childrenCache.delete(node.info.workspaceId); this._onDidChange.fire(node);`
         - Else: just fire(node) — leaves don't have cached children. Per RESEARCH.md Pitfall 4 do NOT proactively re-fetch unexpanded subtrees.
       - `getTreeItem(n: A365Node): vscode.TreeItem` — switch on kind exactly as RESEARCH.md Pattern 1 lines 198-229:
         - workspace: label = `n.info.name`, collapsibleState = Collapsed, contextValue = CTX_WORKSPACE, iconPath = `new vscode.ThemeIcon('cloud')`, description = `n.info.workspaceId` (optional, helps disambiguate)
         - project: label = `n.project.name`, state = None, contextValue = CTX_PROJECT, iconPath = `new vscode.ThemeIcon('folder')`, description = `n.project.id`
         - script: label = `n.script.name`, state = None, contextValue = CTX_SCRIPT, iconPath = `new vscode.ThemeIcon('file-code')`, tooltip = `n.script.description ?? n.script.name`, description = `n.script.scriptId`
         - info: label = `n.label`, state = None, iconPath = `new vscode.ThemeIcon('info')` — no contextValue (no menus)
         - error: label = `n.label`, state = None, iconPath = `new vscode.ThemeIcon('error')`, command = `{ command: 'altium365.tree.retryNode', title: 'Retry', arguments: [n] }` — Plan 03 declared `altium365.tree.retryNode`; extension.ts registers the handler in Task 2.
       - `async getChildren(element?: A365Node): Promise<A365Node[]>`:
         - Top-level `try { ... } catch (e)` returns a single `{ kind: 'error', label: 'Failed: ' + (e as Error).message, parent: element }` and logs to outputChannel via `this.output.appendLine('[Altium 365] tree: ' + (e as Error).message)` (PATTERNS.md S2 + RESEARCH.md D-07).
         - Branch 1 — `!element`: call `loadWorkspaces()` (private async method).
           - Inside `loadWorkspaces`: if `this.workspacesCache` exists return it. Else read OAuthConfig via `readOAuthConfig()` (import it from `./auth` — add to the imports above), get base token via `const base = await getStoredTokens(this.ctx);`. If `!base?.access_token`, return `[]` (the viewsWelcome view will be visible because Task 2 keeps `altium365.signedIn` false when not signed in). Else `const list = await listWorkspaces(this.getEndpoint(), base.access_token);`. If `list.length === 0`, return `[{ kind: 'info', label: 'No workspaces available for this account' }]` (D-06). Else map each WorkspaceInfo to a workspace node with `workspaceUrl: this.getEndpoint().replace(/\/api(\/.*)?$/, '')` — i.e. strip the trailing `/api[/...]` suffix to give Plan 06 a base URL; if no match, just use the endpoint as-is. Cache and return.
         - Branch 2 — `element.kind === 'workspace'`: call `loadWorkspaceChildren(element)`.
           - Inside: cache key = `element.info.workspaceId`. If cached, return it. Else acquire workspace token via `const wsToken = await ensureWorkspaceToken(this.ctx, readOAuthConfig(), { workspaceId: element.info.workspaceId, authId: element.info.authId });`. Then run BOTH fetches in parallel: `const [projects, scripts] = await Promise.all([listProjects(this.getEndpoint(), wsToken), listScripts(this.getEndpoint(), wsToken)]);`. Build nodes: each ProjectInfo → `{ kind: 'project', workspaceId: element.info.workspaceId, project: p }`, each ScriptInfo → `{ kind: 'script', workspaceId: element.info.workspaceId, workspaceAuthId: element.info.authId, workspaceUrl: element.workspaceUrl, script: s }`. Sort each list alphabetically by `.name` (matches `pickProjectId` sort convention at extension.ts:454). Concatenate projects first, then scripts. Cache under key and return.
         - Branch 3 — any other element kind: return `[]`.

    5. Do NOT register commands or create the TreeView inside this file — Task 2 handles registration in `activate()` per PATTERNS.md anti-pattern note.
    6. Do NOT introduce module-level state (no top-level `let`/`const` beyond the contextValue constants).
  </action>
  <verify>
    <automated>npm run compile</automated>
    Plus:
      grep -nE "^export class A365TreeDataProvider" src/sidePanel.ts
      grep -nE "implements vscode.TreeDataProvider<A365Node>" src/sidePanel.ts
      grep -n "CTX_SCRIPT = 'scriptNode'" src/sidePanel.ts
      grep -n "Promise.all" src/sidePanel.ts
      grep -n "altium365.tree.retryNode" src/sidePanel.ts
    All five greps return at least one match.
  </verify>
  <acceptance_criteria>
    - `npm run compile` exits 0 with strict mode
    - `src/sidePanel.ts` exists, exports `A365TreeDataProvider`, `A365Node`, `CTX_WORKSPACE`, `CTX_PROJECT`, `CTX_SCRIPT`
    - Provider constructor signature: `(ctx, outputChannel, getEndpoint)`; no module-level mutable state
    - `getTreeItem` returns TreeItems with `contextValue === 'scriptNode'` for script nodes (gate for Plan 03 menus)
    - `getChildren(workspaceNode)` runs `listProjects` and `listScripts` via `Promise.all` (siblings, PANEL-04)
    - All thrown errors inside `getChildren` are converted to a single `error` TreeItem with retry command; never propagates to caller
    - Output diagnostic lines prefixed `[Altium 365] tree:`
  </acceptance_criteria>
  <done>sidePanel.ts created, provider class compiles, all PANEL-02/03/04 data paths implemented with caching + D-07 error handling</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire TreeView, refresh/retry commands, and altium365.signedIn context key into extension.ts activate()</name>
  <files>src/extension.ts</files>
  <read_first>
    - src/extension.ts (entire file, especially activate() lines 16-36 and doSignIn/doSignOut/doSelectEnvironment lines 40-94, 104-182)
    - .planning/phases/02-side-panel/02-RESEARCH.md §"Provider registration in activate()" (lines 442-453) and §Pitfall 2 (setContext companion)
    - .planning/phases/02-side-panel/02-PATTERNS.md §`src/extension.ts` (subscription block extension + setContext placements)
  </read_first>
  <behavior>
    - On activate, the TreeView 'altium365.tree' is created and registered with showCollapseAll: true; pushed into ctx.subscriptions
    - On activate, `altium365.signedIn` context key is set based on whether stored tokens exist
    - When `altium365.signIn` succeeds, context key flips to true and the provider refreshes (welcome view hides, workspace list loads)
    - When `altium365.signOut` runs, context key flips to false and provider refreshes (welcome view reappears)
    - When `altium365.selectEnvironment` succeeds and the user chose to clear the session, context key flips to false + provider refreshes
    - `altium365.tree.refresh` and `altium365.tree.retryNode` commands are registered; clicking the refresh icon clears cache and re-fetches; clicking an error node re-runs its failed level
    - Provider subscribes to `onAuthStateChanged` and refreshes on every emit
  </behavior>
  <action>
    Modify `src/extension.ts`. Make minimal, additive changes — do NOT refactor unrelated code.

    1. Add imports at the top alongside existing ones:
       - From `./auth`: also import `getStoredTokens`, `onAuthStateChanged` (already importing from `./auth`).
       - From `./sidePanel`: `import { A365TreeDataProvider, A365Node } from './sidePanel';`

    2. Define a small private helper (right above `activate`) — module-private function, NOT a module-level `let`:
       `async function updateSignedInContext(context: vscode.ExtensionContext): Promise<void>` — body: read `const tok = await getStoredTokens(context);` then `await vscode.commands.executeCommand('setContext', 'altium365.signedIn', !!tok);`. Wrap in try/catch silently (worst case: welcome view stays — recoverable).

    3. Inside `activate(context)`, BEFORE the existing `context.subscriptions.push(...)` call, instantiate the provider:
       ```
       const treeProvider = new A365TreeDataProvider(
           context,
           outputChannel,
           () => vscode.workspace.getConfiguration('altium365').get<string>('graphqlEndpoint', ''),
       );
       const treeView = vscode.window.createTreeView('altium365.tree', {
           treeDataProvider: treeProvider,
           showCollapseAll: true,
       });
       ```
       This snippet illustrates the names and call shape; do not paste the snippet verbatim if linting style demands different — match the existing 4-space indent / single-quote convention.

    4. Extend the EXISTING single `context.subscriptions.push(...)` call (line 19) with additional disposables. Order: keep the original 6 entries first, then append:
       - `treeView`
       - `vscode.commands.registerCommand('altium365.tree.refresh', () => treeProvider.refresh())`
       - `vscode.commands.registerCommand('altium365.tree.retryNode', (n?: A365Node) => { if (n?.kind === 'error') { treeProvider.refresh(n.parent); } })`
       - `onAuthStateChanged(async () => { await updateSignedInContext(context); treeProvider.refresh(); })`

       Do NOT create a second `context.subscriptions.push()` call — match the established style. The Plan 05 status bar additions will append further to this same call.

    5. Initial state push: after the subscriptions block but still inside `activate`, call `updateSignedInContext(context)` (fire-and-forget — don't await activate). This sets `altium365.signedIn` based on whether tokens are already stored from a prior session.

    6. Add `setContext` updates to the existing flow at three points (mirror PATTERNS.md §`src/extension.ts` setContext companion):
       - At the END of `doSignIn` (line 65, after the successful signIn `vscode.window.showInformationMessage('Altium 365: signed in.');` but BEFORE the workspace-pick prompt at line 59) — actually place it AFTER line 58 (the success toast) and BEFORE line 59 prompt: call `await updateSignedInContext(context);` followed by `treeProvider.refresh();`. BUT: `treeProvider` is a local in `activate` — not accessible here. Solution: don't reach for treeProvider in these helpers; rely on the `onAuthStateChanged` subscription registered in Task 2 step 4 to do the refresh. The `signIn` function in `auth.ts` already fires `onAuthStateChanged` (Plan 02 Task 2). So in `doSignIn`/`doSignOut`/`doSelectEnvironment` you only need `await updateSignedInContext(context);` — the refresh + welcome-view toggle happen automatically via the subscription.
         - Insert `await updateSignedInContext(context);` immediately after the success toast in `doSignIn` (between current lines 58 and 59).
       - In `doSignOut` (line 71-75), after `clearAllTokens(context)` at line 72: insert `await updateSignedInContext(context);` BEFORE the existing success toast at line 74. (`clearAllTokens` already fires `onAuthStateChanged` per Plan 02, so the provider refresh + welcome reappearance happens automatically.)
       - In `doSelectEnvironment` (line 175-178), inside the `if (next === 'Sign out & sign in' || next === 'Sign out only')` block, after `clearAllTokens(context)` at line 176: insert `await updateSignedInContext(context);` — and ALSO call `fireAuthStateChanged({ signedIn: !!(await getStoredTokens(context)), environment: pick.name })` right after env update at line 161 so the status bar (Plan 05) and tree see the env change. Import `fireAuthStateChanged` from `./auth`.

    7. Do NOT modify `runScript`, `debugScript`, `prepareRun`, or `pickProjectId` in this plan. Plan 06 will refactor the spawn block.

    8. CONVENTIONS.md compliance: cast caught errors as `(e as Error)`; never throw from a command handler — always surface via `vscode.window.showErrorMessage`. The new retry callback is intentionally fire-and-forget (refresh is async only conceptually — it just emits an event).
  </action>
  <verify>
    <automated>npm run compile</automated>
    Plus:
      grep -n "new A365TreeDataProvider" src/extension.ts
      grep -n "createTreeView('altium365.tree'" src/extension.ts
      grep -n "altium365.tree.refresh" src/extension.ts
      grep -n "altium365.tree.retryNode" src/extension.ts
      grep -n "onAuthStateChanged" src/extension.ts
      grep -n "setContext', 'altium365.signedIn'" src/extension.ts
      grep -c "updateSignedInContext(context)" src/extension.ts | grep -v '^#'
    Each grep returns at least one match; the count of `updateSignedInContext(context)` invocations is at least 4 (activate initial, doSignIn, doSignOut, doSelectEnvironment).
  </verify>
  <acceptance_criteria>
    - `npm run compile` exits 0
    - `A365TreeDataProvider` instantiated in `activate()` and `vscode.window.createTreeView('altium365.tree', { treeDataProvider, showCollapseAll: true })` returns a TreeView pushed into `ctx.subscriptions`
    - `altium365.tree.refresh` and `altium365.tree.retryNode` commands are registered (handler bodies non-empty)
    - `onAuthStateChanged` subscription registered inside activate; refresh fires on auth-state changes
    - `setContext('altium365.signedIn', boolean)` invoked at activation AND after sign-in / sign-out / env-switch
    - All disposables go through the single existing `context.subscriptions.push(...)` call (no second push call)
    - No module-level mutable state added beyond the existing `outputChannel`
  </acceptance_criteria>
  <done>Tree view shows up in Activity Bar, populates on sign-in, refresh button works, welcome view toggles correctly with auth state; compile clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click → command handler | tree-title and tree-item commands invoke registered handlers |
| extension host → A365 GraphQL | base + workspace-scoped tokens used; workspace token from secrets cache (Plan 02) |
| TreeItem label / description rendering | server-supplied script/project/workspace names rendered as plain text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Information Disclosure | error TreeItem.label contains exception text | mitigate | Label is the short `(e as Error).message`; full stack trace and original error go to outputChannel only. D-07. |
| T-02-04-02 | XSS / HTML injection via TreeItem.label | server-controlled script names | accept | VS Code TreeItem.label is rendered as plain text (not HTML) per RESEARCH.md §Security Domain V5; no mitigation needed. |
| T-02-04-03 | Spoofing (cross-workspace token reuse) | ensureWorkspaceToken called per-workspace | mitigate | Cache key in Plan 02 = workspaceId; sidePanel.ts passes `element.info.workspaceId` and `element.info.authId` together from the same WorkspaceInfo — never mixed across workspaces. |
| T-02-04-04 | DoS (refresh storm) | onAuthStateChanged subscription triggers refresh | mitigate | Refresh only clears cache and fires onDidChangeTreeData(undefined); VS Code only re-fetches visible nodes (RESEARCH.md Pitfall 4). Auth events fire at most a few times per user session. |
| T-02-04-05 | Stale welcome view after sign-in | setContext('altium365.signedIn') missed | mitigate | `updateSignedInContext(context)` called at activation, after signIn, after signOut, after env-switch. RESEARCH.md Pitfall 2 directly addressed; Task 2 verification counts ≥4 invocations. |
| T-02-04-06 | Repudiation | no audit log of tree expansions | accept | outputChannel captures fetch errors; expansions themselves are read-only and low-value to audit. |
| T-02-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages added in this plan. Engine version unchanged. |
</threat_model>

<verification>
- `npm run compile` exits 0
- All Task 1 and Task 2 verify-block greps return required matches
- Manual smoke test in Extension Development Host:
  1. Fresh install → Activity Bar icon visible → click → welcome with Sign In button
  2. Click Sign In → complete OAuth → welcome disappears, workspaces appear at root
  3. Expand a workspace → projects and scripts appear as siblings (no nesting)
  4. Right-click a script → 4 menu items visible (Run Local, Execute Remotely, Edit, Publish)
  5. Right-click a project → none of the 4 script menu items visible
  6. Click refresh icon in tree title → tree re-fetches without errors
  7. Disconnect network → click refresh → workspace shows error TreeItem with retry; click error item → re-attempts
  8. Sign Out → welcome view reappears
</verification>

<success_criteria>
- PANEL-01: TreeView is registered and shows in Activity Bar
- PANEL-02: workspaces render at root via listWorkspaces
- PANEL-03: each workspace expands to show its projects
- PANEL-04: scripts appear as siblings of projects under the same workspace (RESEARCH.md Schema Finding §1 confirmed)
- D-07 honored: tree-fetch errors render as inline error TreeItem with retry, no toast
- D-06 honored: zero-workspace account renders single info TreeItem
- D-05 viewsWelcome from Plan 03 hides/reappears correctly via `altium365.signedIn` context key
- D-03 manual refresh works via title-bar icon and via auth-state events
- contextValue 'scriptNode' set so the Plan 03 menus appear only on script nodes
</success_criteria>

<output>
Create `.planning/phases/02-side-panel/02-04-SUMMARY.md` when done.
</output>
