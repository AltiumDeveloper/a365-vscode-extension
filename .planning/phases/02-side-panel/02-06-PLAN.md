---
phase: 02-side-panel
plan: 06
type: execute
wave: 3
depends_on: [02-01, 02-02, 02-03, 02-04, 02-05]
files_modified:
  - src/extension.ts
  - src/scriptCommands.ts
autonomous: false
requirements: [PANEL-05, SCRIPT-01]
user_setup: []
must_haves:
  truths:
    - "Before any run-local implementation work, a human checkpoint confirms the A365 file-download URL pattern and required auth header (RESEARCH.md Assumption A1) and that the package is a single .py file (RESEARCH.md Assumption A2)"
    - "All four altium365.script.* commands are registered via vscode.commands.registerCommand so the right-click menu items declared in Plan 03 never error with 'command not found' (RESEARCH.md Pitfall 7)"
    - "Right-clicking a script node and selecting 'Run Script (Local)' fetches the script body from A365, writes it to os.tmpdir() as a .py file with mode 0o600, runs it through the existing python/_runner.py subprocess pipeline, and unlinks the temp file when execution completes (D-09)"
    - "Right-clicking a script node and selecting Edit, Execute Remotely, or Publish shows a 'Coming in Phase 3' info message (D-03 placeholder contract)"
    - "Invoking altium365.script.runLocal from the command palette (no script arg) shows an error directing the user to right-click a script in the Altium 365 side panel"
    - "The existing altium365.runScript / altium365.debugScript editor commands continue to work unchanged after the runScriptAtPath refactor"
  artifacts:
    - path: "src/scriptCommands.ts"
      provides: "fetchScriptBody + runLocalFromScriptNode + registerScriptCommands(ctx, output) factory returning vscode.Disposable[]"
      contains: "export function registerScriptCommands"
    - path: "src/extension.ts"
      provides: "exported runScriptAtPath(ctx, scriptPath) helper + registerScriptCommands wired into activate()"
      contains: "export async function runScriptAtPath"
  key_links:
    - from: "src/scriptCommands.ts:runLocalFromScriptNode"
      to: "src/auth.ts:ensureWorkspaceToken + src/extension.ts:runScriptAtPath"
      via: "fetch fileToken via GraphQL → HTTP download → os.tmpdir() write → runScriptAtPath → unlink in finally"
      pattern: "ensureWorkspaceToken.*fetchScriptBody.*runScriptAtPath"
    - from: "src/extension.ts activate()"
      to: "src/scriptCommands.ts:registerScriptCommands"
      via: "ctx.subscriptions.push(...registerScriptCommands(context, outputChannel))"
      pattern: "registerScriptCommands\\(context"
---

<objective>
Complete D-09 (run-local from tree) and D-03 (register the three Phase 3 placeholder commands so right-click menus from Plan 03 never error). Adds a new `src/scriptCommands.ts` containing the script-body fetch helpers and the four command handlers, plus a small refactor in `src/extension.ts` to extract `runScriptAtPath(ctx, scriptPath)` from the existing `runScript` so the new run-local handler can reuse the proven Python subprocess pipeline verbatim.

Purpose: PANEL-05 + SCRIPT-01 require the side-panel context menu to actually wire to runnable commands. D-09 specifies the two-step body fetch + tmpdir + finally-unlink lifecycle. RESEARCH.md Pitfall 8 plus Assumptions A1+A2 mandate a blocking human-verify checkpoint BEFORE the run-local handler is implemented — the file-download endpoint and the "single .py vs archive" question must be confirmed against a live workspace.

Output: New `src/scriptCommands.ts` (fetch helpers + 4 handlers + factory); edits to `src/extension.ts` to (a) extract `runScriptAtPath`, (b) call `registerScriptCommands` from activate() and append its disposables into the single ctx.subscriptions.push() call extended by Plans 04 and 05.
</objective>

<execution_context>
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-side-panel/02-CONTEXT.md
@.planning/phases/02-side-panel/02-RESEARCH.md
@.planning/phases/02-side-panel/02-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@AGENTS.md
@src/extension.ts
@src/auth.ts
@src/workspace.ts
@python/_runner.py

<interfaces>
Consumed (from prior plans + existing):

  // From src/workspace.ts (Plan 01 + existing)
  export interface ScriptInfo { scriptId: string; name: string; description?: string; }
  export interface WorkspaceInfo { workspaceId: string; authId: string; name: string; workspaceUrl: string; ... }
  export async function graphqlRequest<T>(endpoint: string, token: string, query: string, variables?: Record<string, unknown>): Promise<T>;

  // From src/auth.ts (Plan 02)
  export async function ensureWorkspaceToken(context, cfg, workspace: { workspaceId: string; authId: string }): Promise<string>;
  export function readOAuthConfig(): OAuthConfig;

  // From src/sidePanel.ts (Plan 04) — the command arg shape
  type A365Node =
    | { kind: 'workspace'; info: WorkspaceInfo; workspaceUrl: string }
    | { kind: 'project';   workspaceId: string; project: ProjectInfo }
    | { kind: 'script';    workspaceId: string; workspaceAuthId: string; workspaceUrl: string; script: ScriptInfo }
    | { kind: 'info';      label: string }
    | { kind: 'error';     label: string; parent: A365Node | undefined };

  // From package.json (Plan 03) — declared command ids (must each have a registerCommand call)
  altium365.script.runLocal
  altium365.script.edit
  altium365.script.executeRemote
  altium365.script.publish

  // From src/extension.ts (existing)
  const outputChannel: vscode.OutputChannel;     // module singleton
  async function runScript(ctx, uri?);            // existing editor-context command
  async function prepareRun(ctx, uri?): Promise<RunPrep | undefined>;
  function resolvePythonPath(): Promise<string>;

New exports this plan adds:

  // From src/extension.ts (after Task 2 refactor)
  export async function runScriptAtPath(context: vscode.ExtensionContext, scriptPath: string): Promise<void>;

  // From src/scriptCommands.ts
  export async function fetchScriptBody(workspaceToken: string, workspaceUrl: string, scriptId: string): Promise<Buffer>;
  export async function runLocalFromScriptNode(context: vscode.ExtensionContext, output: vscode.OutputChannel, node: ScriptCommandArg): Promise<void>;
  export function registerScriptCommands(context: vscode.ExtensionContext, output: vscode.OutputChannel): vscode.Disposable[];
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm A365 file-download endpoint + package format before implementing run-local</name>
  <what-built>
    Nothing yet — this checkpoint exists because RESEARCH.md Pitfall 8 + Assumptions A1 and A2 flag two HIGH-risk unknowns in D-09 that must be resolved against a live workspace before any code ships:

    A1 (HIGH): The HTTP file-download endpoint that consumes a `fileToken` returned by `GloScrScriptPackage.fileToken`. The current best guess is `GET ${workspaceUrl}/api/files/${fileToken}` with header `Authorization: Bearer <workspaceToken>` (RESEARCH.md Code Examples §"Script body fetch"), but the Altium platform-api-docs are silent on this — the GraphQL schema only exposes the token, not the download URL.

    A2 (MEDIUM): Whether the bytes returned by that endpoint are a single `.py` text file or a zip/archive (the type name `GloScrScriptPackage` is suspicious). If it's an archive, run-local needs an unzip step plus an entry-point selection rule.

    Until the human confirms both, the run-local implementation (Task 3) is blocked.
  </what-built>
  <how-to-verify>
    Pick ONE script in a real A365 workspace that the developer has access to. Then:

    1. Sign in via `altium365.signIn` and select a workspace that contains the test script.
    2. Open VS Code's Developer Tools (Help → Toggle Developer Tools) so network/console output is visible, OR use a separate terminal with `curl`.
    3. Issue the GraphQL `GetScriptFileToken` query against `${workspaceUrl}/api` using the workspace-scoped token (you can grab it from `context.secrets` via a temporary debug command, or from the Network panel of an existing workspace request). The query body is in `.planning/phases/02-side-panel/02-RESEARCH.md` lines 478–488. Record the returned `fileToken`.
    4. Issue:
       ```
       curl -i -H "Authorization: Bearer <workspaceToken>" "${workspaceUrl}/api/files/<fileToken>"
       ```
       (URL-encode the fileToken first.)
    5. Record:
       - HTTP status (200 expected). If 401/403, the auth header is wrong — try `X-Session-ID`, cookie, or a different scheme used elsewhere in the codebase.
       - If 404, the URL path is wrong — try alternates such as `/api/storage/files/{token}`, `/api/fileStorage/download?token=...`, or whatever path the A365 web UI uses (inspect via browser DevTools while downloading a script there).
       - `Content-Type` of the response (e.g. `text/x-python`, `application/octet-stream`, `application/zip`).
       - Whether the body starts with `PK\x03\x04` (zip magic) or with plain Python source. `file -b <saved>` will say `Python script` vs `Zip archive` vs `ASCII text`.

    6. Decide and post back ONE of:
       - **APPROVED-A1+A2** — endpoint pattern is exactly `${workspaceUrl}/api/files/{fileToken}` with `Authorization: Bearer`, and body is a single `.py` text file. Task 3 proceeds as written.
       - **APPROVED-A1+A2-CORRECTED** — supply the correct endpoint pattern (full URL template) and required auth header. Body is still a single `.py`. Task 3 proceeds with substitutions.
       - **APPROVED-A1-ARCHIVE** — endpoint confirmed; body is a zip archive. Then also supply the entry-point selection rule (filename convention, manifest field, etc.). Task 3 must be extended with unzip + entry-point logic; revise the plan before continuing.
       - **BLOCKED** — cannot determine. Phase 2 ships with only the three placeholder handlers (edit/executeRemote/publish) registered; runLocal stays unregistered or shows a "not yet available" message. SCRIPT-01 / D-09 deferred to Phase 3.
  </how-to-verify>
  <resume-signal>Reply with exactly one of: APPROVED-A1+A2 | APPROVED-A1+A2-CORRECTED &lt;url-template&gt; &lt;auth-header&gt; | APPROVED-A1-ARCHIVE &lt;entry-point-rule&gt; | BLOCKED</resume-signal>
  <acceptance_criteria>
    - Human posts one of the four resume-signal values above.
    - If APPROVED-A1+A2-CORRECTED, the URL template and auth header are captured into the resume signal so Task 3 can substitute them in `fetchScriptBody`.
    - If APPROVED-A1-ARCHIVE, the executor MUST stop and request a plan revision (Task 3 as-written is invalid).
    - If BLOCKED, the executor MUST skip Task 3's run-local body and instead register `altium365.script.runLocal` with the same "Coming in Phase 3" info handler used by the other three placeholders; SUMMARY.md records the deferral with the reason.
  </acceptance_criteria>
  <done>One of the four resume-signal values received; resulting strategy for Task 3 is unambiguous.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extract runScriptAtPath(ctx, scriptPath) from existing runScript so any caller can run an arbitrary .py file</name>
  <files>src/extension.ts</files>
  <read_first>
    - src/extension.ts (entire file — especially `runScript` at lines 214-238, `prepareRun` at lines 287-398, and the `RunPrep` interface at lines 276-285)
    - .planning/codebase/CONVENTIONS.md (async/await, errors at command boundary, no module-level mutable state beyond outputChannel)
    - AGENTS.md ("All tokens stored via context.secrets — never plaintext on disk. Temp params files use os.tmpdir()")
  </read_first>
  <behavior>
    - `runScriptAtPath(ctx, scriptPath)` executes the same Python subprocess pipeline as the existing `runScript(ctx, uri)` but takes an explicit absolute file path instead of inferring one from the active editor / a URI.
    - It performs the same endpoint check, the same access-token acquisition (with the same "Sign in" prompt fallback), and the same `python/_runner.py` invocation with the same env vars and the same outputChannel logging.
    - The existing `runScript` and `debugScript` commands keep working — `runScript(ctx, uri?)` now resolves the target path the same way it always did, then delegates to `runScriptAtPath`.
    - `debugScript` is NOT routed through `runScriptAtPath` because debug needs `vscode.debug.startDebugging` rather than `spawn`; leave it untouched.
    - No public API changes to `prepareRun` — keep its current signature so internal callers compile unchanged.
    - The refactor is behaviour-preserving: a manual smoke test of `altium365.runScript` from the editor toolbar produces byte-identical outputChannel lines to before.
  </behavior>
  <action>
    Edit `src/extension.ts` only. This task is a refactor; do not add new behaviour.

    1. Lift the body of `runScript` (lines 214-238) into a new exported helper:
       - Signature: `export async function runScriptAtPath(context: vscode.ExtensionContext, scriptPath: string): Promise<void>`
       - Body: same as the current `runScript` body from line 219 (`const { python, ... } = prep;`) onward — i.e. show outputChannel, log endpoint/python/params, spawn the runner, wire stdout/stderr/error/close listeners.
       - The `prep` value is built differently: instead of `prepareRun(context, uri)`, build prep by reusing the `prepareRun` plumbing keyed off an explicit path. Easiest mechanical change with the smallest blast radius:
         - Add an optional second parameter to `prepareRun`: `prepareRun(context, uriOrPath?: vscode.Uri | string)`. If a string is passed, treat it as an already-resolved absolute file path (skip the active-editor save logic, skip the "No Python script selected" error). All downstream resolution (endpoint/token/python/env/params) is identical.
         - Inside `prepareRun`, branch at the top: `const target: vscode.Uri | undefined = typeof uriOrPath === 'string' ? vscode.Uri.file(uriOrPath) : uriOrPath;` then continue with the existing logic.
       - `runScriptAtPath` body:
         - `const prep = await prepareRun(context, scriptPath);`
         - `if (!prep) return;`
         - rest copied from current `runScript` lines 219-237 verbatim.

    2. Rewrite `runScript(context, uri?)` (the existing exported wrapper still called from `registerCommand` at extension.ts:26-27) to:
       - Resolve `target` exactly as it does today (uri arg first, else active editor `.py` document, else error message and return).
       - Save dirty editor exactly as today.
       - Then call `return runScriptAtPath(context, target.fsPath);`
       - This preserves the existing user-facing error "No Python script selected." for the editor-toolbar entry point.

    3. Do NOT modify `debugScript`. Do NOT modify `pickProjectId`. Do NOT remove or rename any existing exports.

    4. Add a one-line module comment above `runScriptAtPath`: `// Reused by src/scriptCommands.ts to run a fetched A365 script body written to os.tmpdir().`

    5. Verify with an `npm run compile` after the edit; investigate any new tsc error before moving on.
  </action>
  <verify>
    <automated>npm run compile</automated>
    Plus:
      grep -nE "^export async function runScriptAtPath" src/extension.ts
      grep -nE "return runScriptAtPath\\(context, target.fsPath\\)" src/extension.ts
      grep -c "prepareRun(context, " src/extension.ts | grep -v '^#'
    First two greps return ≥1 match each. The third returns ≥3 (one inside runScript, one inside runScriptAtPath/wrapper, one inside debugScript).
  </verify>
  <acceptance_criteria>
    - `npm run compile` exits 0 in strict mode
    - `runScriptAtPath` is exported with signature `(context: vscode.ExtensionContext, scriptPath: string) => Promise<void>`
    - `runScript(ctx, uri?)` still resolves uri-or-editor target, saves dirty docs, and now delegates to `runScriptAtPath` via `target.fsPath`
    - `debugScript` is unchanged (diff shows no touched lines inside it)
    - `prepareRun`'s second parameter accepts `vscode.Uri | string | undefined`; string is treated as an absolute path; behaviour for `vscode.Uri | undefined` callers is byte-identical
    - No new module-level mutable state; outputChannel singleton untouched
  </acceptance_criteria>
  <done>Compile clean; runScriptAtPath available for Task 3; existing editor-toolbar run/debug commands behaviourally unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create src/scriptCommands.ts (fetch + handlers + factory) and wire into extension.ts activate()</name>
  <files>src/scriptCommands.ts, src/extension.ts</files>
  <read_first>
    - .planning/phases/02-side-panel/02-RESEARCH.md §"Script body fetch (D-09)" (lines 474-502) — GraphQL query + fileToken indirection + URL template (subject to Task-1 confirmation)
    - .planning/phases/02-side-panel/02-RESEARCH.md §"Temp file lifecycle for D-09" (lines 504-521) — tmpdir + 0o600 + try/finally + unlink
    - .planning/phases/02-side-panel/02-RESEARCH.md §Pitfall 7 (lines 403-411) — every declared command must have a registerCommand call
    - .planning/phases/02-side-panel/02-CONTEXT.md D-03 + D-09 (lines 30-36)
    - src/workspace.ts — `graphqlRequest` signature and the `WorkspaceInfo` shape; the `${workspaceUrl}/api` pattern used by existing callers
    - src/auth.ts post-Plan-02 — `ensureWorkspaceToken(ctx, cfg, { workspaceId, authId })`
    - src/extension.ts post-Task-2 — `runScriptAtPath` + the single `context.subscriptions.push(...)` call (extended by Plans 04 and 05)
  </read_first>
  <behavior>
    - `fetchScriptBody(workspaceToken, workspaceUrl, scriptId)` issues the `GetScriptFileToken` GraphQL query against `${workspaceUrl}/api`, extracts `versions.nodes[0].package.fileToken`, throws `Error('Script has no published version')` if absent, then GETs the file-download URL (default `${workspaceUrl}/api/files/${encodeURIComponent(fileToken)}` with `Authorization: Bearer ${workspaceToken}`) and returns the body as a `Buffer`. If Task 1 returned APPROVED-A1+A2-CORRECTED, substitute the corrected URL template / auth header verbatim.
    - `runLocalFromScriptNode(ctx, output, node)`:
      1. Validate `node && node.kind === 'script'`; otherwise show error message "Run from the Altium 365 side panel by right-clicking a script." and return.
      2. Acquire workspace token via `ensureWorkspaceToken(ctx, readOAuthConfig(), { workspaceId: node.workspaceId, authId: node.workspaceAuthId })`.
      3. Call `fetchScriptBody(token, node.workspaceUrl, node.script.scriptId)` to get the body.
      4. Compute tmp path: `path.join(os.tmpdir(), `altium365-${node.script.scriptId}-${Date.now()}.py`)` — uses the SERVER-issued `scriptId`, not the user-controlled `name` (command-injection mitigation; see threat T-02-06-04).
      5. `await fs.promises.writeFile(tmpPath, body, { mode: 0o600 });`
      6. `try { await runScriptAtPath(ctx, tmpPath); } finally { await fs.promises.unlink(tmpPath).catch(() => {}); }` — D-09 explicit cleanup.
      7. Surface all errors via `vscode.window.showErrorMessage('Altium 365: ' + (e as Error).message)` AND `output.appendLine('[Altium 365] runLocal: ' + (e as Error).message)`.
    - `registerScriptCommands(ctx, output)` returns an array of FOUR `vscode.Disposable` values, one per declared command id:
      - `altium365.script.runLocal` → `(node?: any) => runLocalFromScriptNode(ctx, output, node)`
      - `altium365.script.edit` → `() => vscode.window.showInformationMessage('Altium 365: Edit Script — coming in Phase 3.')`
      - `altium365.script.executeRemote` → `() => vscode.window.showInformationMessage('Altium 365: Execute Script Remotely — coming in Phase 3.')`
      - `altium365.script.publish` → `() => vscode.window.showInformationMessage('Altium 365: Publish Script — coming in Phase 3.')`
    - Right-click "Run Script (Local)" on a script node executes runLocal end-to-end; right-click Edit/Execute Remotely/Publish shows the Phase 3 info message; right-click on script then "Run Script (Local)" with no signed-in workspace surfaces a clear error.
    - If Task 1 returned BLOCKED, the `altium365.script.runLocal` registration MUST instead point at the same "Coming in Phase 3" handler used by the other three; `fetchScriptBody` and `runLocalFromScriptNode` are still defined but unwired, and SUMMARY.md records the deferral.
  </behavior>
  <action>
    PART A — Create new file `src/scriptCommands.ts`:

    1. Imports:
       - `import * as vscode from 'vscode';`
       - `import * as os from 'os';`
       - `import * as path from 'path';`
       - `import * as fs from 'fs';`
       - Named from `./workspace`: `graphqlRequest`, `ScriptInfo` (re-export not needed; type-only use)
       - Named from `./auth`: `ensureWorkspaceToken`, `readOAuthConfig`
       - Named from `./extension`: `runScriptAtPath` (added in Task 2)

    2. Define the GraphQL query constant `GET_SCRIPT_FILE_TOKEN` exactly as in RESEARCH.md lines 478-488. Keep it as a module-private `const` (not exported).

    3. Define `interface ScriptCommandArg { kind: 'script'; workspaceId: string; workspaceAuthId: string; workspaceUrl: string; script: ScriptInfo; }` — duplicate of the script branch of `A365Node` to avoid importing `src/sidePanel.ts` and creating a cycle. The runtime check `node?.kind === 'script'` is the contract.

    4. Export `async function fetchScriptBody(workspaceToken: string, workspaceUrl: string, scriptId: string): Promise<Buffer>`:
       - `const res = await graphqlRequest<{ gloScrScript?: { versions?: { nodes?: Array<{ package?: { fileToken?: string } }> } } }>(`${workspaceUrl}/api`, workspaceToken, GET_SCRIPT_FILE_TOKEN, { scriptId });`
       - `const fileToken = res.gloScrScript?.versions?.nodes?.[0]?.package?.fileToken;`
       - `if (!fileToken) throw new Error('Script has no published version');`
       - Issue the file download. URL template + auth header come from Task 1's resume signal:
         - Default (APPROVED-A1+A2): `const dl = await fetch(`${workspaceUrl}/api/files/${encodeURIComponent(fileToken)}`, { headers: { Authorization: `Bearer ${workspaceToken}` } });`
         - If APPROVED-A1+A2-CORRECTED, substitute the corrected URL template and header verbatim.
       - `if (!dl.ok) throw new Error(`File download failed: HTTP ${dl.status}`);`
       - `return Buffer.from(await dl.arrayBuffer());`
       - Node's global `fetch` is available from Node 18+ (AGENTS.md confirms Node ≥18). No import needed.

    5. Export `async function runLocalFromScriptNode(context: vscode.ExtensionContext, output: vscode.OutputChannel, node: any): Promise<void>`:
       - Implement the behaviour spec exactly as in <behavior> step 1-7. Use `node as ScriptCommandArg` after the kind check.
       - For step 4, the temp filename uses `node.script.scriptId` (server-issued opaque id), NEVER `node.script.name` (T-02-06-04). Sanitise defensively anyway: `const safeId = node.script.scriptId.replace(/[^A-Za-z0-9_-]/g, '_');`
       - Wrap everything from step 2 onward in a top-level try/catch as specified.

    6. Export `function registerScriptCommands(context: vscode.ExtensionContext, output: vscode.OutputChannel): vscode.Disposable[]`:
       - Build a const placeholder helper inside the function: `const placeholder = (label: string) => () => vscode.window.showInformationMessage(`Altium 365: ${label} — coming in Phase 3.`);`
       - Return:
         ```
         [
           vscode.commands.registerCommand('altium365.script.runLocal', (node?: unknown) => runLocalFromScriptNode(context, output, node)),
           vscode.commands.registerCommand('altium365.script.edit', placeholder('Edit Script')),
           vscode.commands.registerCommand('altium365.script.executeRemote', placeholder('Execute Script Remotely')),
           vscode.commands.registerCommand('altium365.script.publish', placeholder('Publish Script')),
         ]
         ```
       - If Task 1 resume-signal was BLOCKED, swap the runLocal entry's callback for `placeholder('Run Script (Local)')` and document this in SUMMARY.md.

    7. No module-level mutable state; no calls to `ctx.subscriptions.push` from inside this file (caller does it).

    PART B — Wire into `src/extension.ts`:

    8. Add named import: `import { registerScriptCommands } from './scriptCommands';`

    9. Inside `activate(context)`, AFTER the Plan 05 `createStatusBar` call and BEFORE the existing single `context.subscriptions.push(...)` block, add:
       `const scriptCommandDisposables = registerScriptCommands(context, outputChannel);`

    10. Extend that SAME `context.subscriptions.push(...)` call (the one Plans 04 and 05 already extend) by spreading the disposables:
        - Append the spread element `...scriptCommandDisposables` to the existing argument list.
        - Do NOT add a second `context.subscriptions.push` statement.

    11. Do NOT remove the existing `altium365.runScript` / `altium365.debugScript` registrations (they remain the editor-toolbar entry points; the new `altium365.script.runLocal` is the side-panel entry point).
  </action>
  <verify>
    <automated>npm run compile</automated>
    Plus:
      grep -nE "^export function registerScriptCommands" src/scriptCommands.ts
      grep -nE "^export async function fetchScriptBody" src/scriptCommands.ts
      grep -nE "^export async function runLocalFromScriptNode" src/scriptCommands.ts
      grep -n "altium365.script.runLocal" src/scriptCommands.ts
      grep -n "altium365.script.edit" src/scriptCommands.ts
      grep -n "altium365.script.executeRemote" src/scriptCommands.ts
      grep -n "altium365.script.publish" src/scriptCommands.ts
      grep -n "registerScriptCommands(context" src/extension.ts
      grep -n "scriptCommandDisposables" src/extension.ts
      grep -n "os.tmpdir()" src/scriptCommands.ts
      grep -n "mode: 0o600" src/scriptCommands.ts
      grep -n "unlink" src/scriptCommands.ts
    Each grep returns ≥1 match. Additionally:
      node -e "const t = require('fs').readFileSync('src/scriptCommands.ts','utf-8'); if (!/try\\s*\\{[\\s\\S]*runScriptAtPath[\\s\\S]*\\}\\s*finally\\s*\\{[\\s\\S]*unlink/.test(t)) throw new Error('missing try/finally unlink around runScriptAtPath'); console.log('ok');"
  </verify>
  <acceptance_criteria>
    - `npm run compile` exits 0 in strict mode
    - `src/scriptCommands.ts` exists and exports `fetchScriptBody`, `runLocalFromScriptNode`, `registerScriptCommands`
    - All four `altium365.script.*` command ids appear in `vscode.commands.registerCommand` calls inside `registerScriptCommands`
    - `runLocalFromScriptNode` writes to `os.tmpdir()` with `mode: 0o600`, calls `runScriptAtPath(ctx, tmpPath)` inside a `try { ... } finally { ... unlink ... }`, and uses `node.script.scriptId` (not `node.script.name`) in the temp filename
    - `extension.ts` activate() calls `registerScriptCommands(context, outputChannel)` exactly once and spreads its disposables into the single existing `context.subscriptions.push(...)` call (no second push statement)
    - Invoking `altium365.script.runLocal` with no/invalid arg shows the directed error message (no crash, no silent failure)
    - Invoking the three placeholder commands shows the corresponding "coming in Phase 3" info message
    - If Task 1 returned BLOCKED, `altium365.script.runLocal` is registered to the placeholder handler and SUMMARY.md notes the deferral
  </acceptance_criteria>
  <done>All four script commands registered; right-click "Run Script (Local)" runs an A365 script end-to-end with tmpdir + cleanup; the other three show Phase 3 placeholders; manual smoke test (sign in → expand workspace → expand scripts → right-click → Run Script (Local)) produces output in the Altium 365 output channel and the temp file is gone after exit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| A365 GraphQL response → fileToken | server-issued opaque string; passed through to file-download URL |
| A365 file-download HTTP response → script body bytes | UNTRUSTED — written to disk and executed by Python subprocess |
| `node.script.scriptId` from tree → temp filename | server-issued; treated as semi-trusted; sanitised defensively |
| `node.script.name` from tree → display only | NEVER used in filesystem paths, subprocess args, or shell strings |
| workspaceToken (from `ensureWorkspaceToken`) → Authorization header | in-memory; never logged; standard pattern from existing graphqlRequest |
| Temp file in `os.tmpdir()` | `mode: 0o600` (owner-only); unlinked in `finally` even on subprocess crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-01 | Tampering | A365 script body bytes executed by Python subprocess | accept | The user explicitly invoked "Run Script (Local)" on a script they selected from their own A365 workspace. The trust model for A365 scripts is the same as for any script they would clone from their own org repo. Documented in SUMMARY.md as accepted. |
| T-02-06-02 | Information Disclosure | script body persisted to disk | mitigate | `os.tmpdir()` per AGENTS.md (matches existing params-file pattern at extension.ts:362-371). `mode: 0o600` on write. Unlinked in `finally` so it does not survive process exit even on subprocess crash. |
| T-02-06-03 | Tampering | other local users reading/replacing the temp file before subprocess opens it | mitigate | `mode: 0o600` restricts read/write to the current user. Filename includes `${scriptId}-${Date.now()}` so two concurrent runs do not collide. |
| T-02-06-04 | Command Injection | temp filename derived from script name | mitigate | Filename uses `node.script.scriptId` (server-issued opaque id), not the user-controllable `node.script.name`. Even so, scriptId is run through `replace(/[^A-Za-z0-9_-]/g, '_')` defensively. The Python subprocess receives the path as `argv[1]` via `spawn` (no shell), so shell metacharacters in the path would not be interpreted regardless. |
| T-02-06-05 | Information Disclosure | workspaceToken logged to outputChannel | mitigate | `runLocalFromScriptNode` never appendLines the token. `graphqlRequest` already sends it as a header and does not log. `fetchScriptBody` errors include HTTP status but not the token. |
| T-02-06-06 | Tampering | file-download URL constructed from server-supplied fileToken | mitigate | `encodeURIComponent(fileToken)` prevents path traversal in the URL. The base URL `${workspaceUrl}/api/files/` is locked to the workspace; fileToken cannot escape that prefix even if it contains `/` or `..`. |
| T-02-06-07 | Repudiation | runLocal failures invisible | mitigate | All errors are logged via `output.appendLine('[Altium 365] runLocal: ...')` AND surfaced via `vscode.window.showErrorMessage` (CONVENTIONS.md S2). Successful runs log via the existing `runScriptAtPath` outputChannel lines (endpoint, python, exit code). |
| T-02-06-08 | DoS | hung subprocess prevents temp-file cleanup | mitigate | `try/finally` around `runScriptAtPath` runs the unlink regardless of subprocess outcome. The subprocess itself is fire-and-forget (existing pattern in `runScript`), so unlink runs immediately after spawn returns control — note: this is the same lifecycle as the existing params-file pattern, which also does not wait for subprocess exit. This is an accepted limitation of the existing architecture; not new in this plan. |
| T-02-06-09 | Spoofing (command-not-found UX) | Plan 03 menus point at unregistered commands | mitigate | `registerScriptCommands` registers ALL FOUR declared command ids (RESEARCH.md Pitfall 7); the three Phase 3 placeholders return harmless info messages rather than failing. |
| T-02-06-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages added in this plan. Reuses existing `graphqlRequest`, Node 18+ global `fetch`, Node stdlib `os`/`path`/`fs`. |
</threat_model>

<verification>
- Task 1 resume signal received and recorded in SUMMARY.md
- `npm run compile` exits 0
- All grep gates in Tasks 2 and 3 pass
- Manual smoke test in Extension Development Host:
  1. Sign in via the side-panel welcome view (Plan 04)
  2. Expand a workspace → expand the Scripts group (Plan 04) → right-click a script
  3. Pick "Run Script (Local)" → Altium 365 output channel shows `Running /tmp/altium365-<scriptId>-<ts>.py` and the script's stdout, then `Exit code: 0`
  4. After exit, `ls /tmp/altium365-*` (or equivalent `%TEMP%` on Windows) shows the file is gone (give it a second; unlink runs after spawn returns control)
  5. Right-click another script → pick Edit → info message "Altium 365: Edit Script — coming in Phase 3." appears
  6. Same for Execute Remotely and Publish
  7. From command palette, invoke "Altium 365: Run Script (Local)" with no script selected → error message directs the user to the side panel; no crash
  8. Existing `altium365.runScript` from the editor toolbar still runs the active .py file with byte-identical outputChannel output to before the refactor
</verification>

<success_criteria>
- PANEL-05: right-click context menu on a script node fires the correct command for all four entries (verified by Plan 03's menu wiring + Plan 06's registrations meeting in the middle)
- SCRIPT-01: A user can run an A365 script body locally without opening it in the editor (D-09 satisfied end-to-end), OR — if Task 1 returned BLOCKED — the deferral is recorded in SUMMARY.md with the reason and Phase 3 picks it up
- D-09 lifecycle honoured: tmpdir + 0o600 + try/finally unlink, reusing the existing `_runner.py` pipeline verbatim
- D-03 honoured: all three Phase 3 commands are REGISTERED (no "command not found" errors when their menu items are clicked); their handlers show consistent "coming in Phase 3" info messages
- RESEARCH.md Pitfall 7 cleared: every command in package.json contributes.commands has a matching registerCommand call (verified by grep)
- RESEARCH.md Pitfall 8 cleared: Assumptions A1+A2 explicitly confirmed (or marked BLOCKED with a tracked follow-up) before any byte of run-local code shipped
- No regression in `altium365.runScript` / `altium365.debugScript` editor commands
</success_criteria>

<output>
Create `.planning/phases/02-side-panel/02-06-SUMMARY.md` when done. The SUMMARY MUST record:
  - The exact resume-signal value posted for Task 1 (APPROVED-A1+A2 | APPROVED-A1+A2-CORRECTED &lt;url&gt; &lt;header&gt; | APPROVED-A1-ARCHIVE &lt;rule&gt; | BLOCKED) and the resulting strategy taken for Task 3
  - Confirmation that all four `altium365.script.*` commands are registered
  - Confirmation that `runScriptAtPath` was extracted and the existing run/debug editor commands still work
  - Any deferred work (e.g. archive-unpacking, pagination, etc.) flagged for Phase 3
</output>
