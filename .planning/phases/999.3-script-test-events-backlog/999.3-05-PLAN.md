---
phase: 999.3-script-test-events-backlog
plan: 05
type: execute
wave: 5
depends_on: [999.3-04]
files_modified:
  - package.json
autonomous: false
requirements: [SCRIPT-V2-04.9]

must_haves:
  truths:
    - "[D-10, D-11] All 5 test-event commands appear in `contributes.commands` with human-readable titles under category 'Altium 365', visible in Command Palette (no `when: false` for any of them)."
    - "[D-12 A/B trial] Both UI placements ship together: (A) under existing `altium365.editorTitle` submenu as group `4_events@1` showing `altium365.testEvents.pick`; (B) sibling direct entry in `editor/title` as `navigation@2` with the same command + icon `$(symbol-event)`."
    - "[D-09] `altium365.inputParametersPath` setting description updated to clarify its new role: an escape-hatch seed for the `From settings` preset in `Create Test Event`, no longer the runtime params source."
    - "[D-22] `altium365.promptForProjectId` setting description updated to mark it as superseded by the test-events flow (kept for back-compat; recommend users migrate to test events). Setting NOT removed in this phase."
    - "[Pitfall 7 / VS Code menu API] The `when` clauses gating placements (A) and (B) in `editor/title` MUST reuse the SAME `when` clause string as the existing `altium365.runScript` submenu row in package.json (currently `resourceLangId == python`). Do NOT hand-author a new predicate — copy verbatim from the existing entry to guarantee identical visibility rules."
    - "[CONVENTIONS] Command titles follow existing convention: `Altium 365: <Verb> Test Event` (e.g., `Altium 365: Pick Test Event`, `Altium 365: Create Test Event`)."
  artifacts:
    - path: "package.json contributes.commands"
      provides: "5 new command entries: altium365.testEvents.{pick,create,edit,delete,setDefault}. Each has command, title, category='Altium 365'. The pick entry additionally has icon='$(symbol-event)'."
      contains: "altium365.testEvents.pick"
    - path: "package.json contributes.menus.altium365.editorTitle"
      provides: "New entry: { command: 'altium365.testEvents.pick', group: '4_events@1' } — appended to the existing submenu items array."
      contains: "4_events@1"
    - path: "package.json contributes.menus.editor/title"
      provides: "New entry: { command: 'altium365.testEvents.pick', group: 'navigation@2', when: '<same as existing script when>', icon: '$(symbol-event)' } — sibling to the existing run button."
      contains: "altium365.testEvents.pick"
    - path: "package.json contributes.configuration.properties"
      provides: "Updated descriptions for altium365.inputParametersPath and altium365.promptForProjectId reflecting their new/deprecated roles."
      contains: "test events"
  key_links:
    - from: "package.json contributes.menus.altium365.editorTitle"
      to: "package.json contributes.submenus altium365.editorTitle"
      via: "submenu id reference"
      pattern: "altium365\\.editorTitle"
    - from: "package.json contributes.menus.editor/title"
      to: "package.json contributes.commands altium365.testEvents.pick"
      via: "command id reference + icon"
      pattern: "symbol-event"
---

<objective>
Wire the 5 commands from Plan 04 into the manifest so they appear in the Command Palette AND in the editor-title UI surface. Ship both A/B placements simultaneously per D-12 so the developer can compare the submenu vs sibling-button affordances on real usage in Plan 06's UAT. Also update two configuration setting descriptions to reflect their evolved roles (D-09 escape-hatch, D-22 deprecation marker).

Purpose: Without this plan, the commands exist but are only reachable from the palette and have no script-context-aware UI affordance. The A/B trial design lets a single dogfood pass inform a future decision (out of scope for 999.3) on which to keep.

Output: One edited file (package.json). One commit.
</objective>

<execution_context>
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.3-script-test-events-backlog/999.3-CONTEXT.md
@.planning/phases/999.3-script-test-events-backlog/999.3-RESEARCH.md
@package.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add 5 commands + 2 menu placements + update 2 setting descriptions in package.json</name>
  <files>package.json</files>
  <read_first>
    - package.json `contributes.commands` (note title/category style)
    - package.json `contributes.submenus` line ~254 (`altium365.editorTitle` exists)
    - package.json `contributes.menus["altium365.editorTitle"]` lines ~306-327 (existing submenu items + their group ordering)
    - package.json `contributes.menus["editor/title"]` lines ~299-305 (existing script run button — copy its `when` clause)
    - package.json `contributes.menus.commandPalette` lines ~265-298 (existing `when: false` block — confirm we do NOT add the 5 test-event commands here)
    - package.json `contributes.configuration.properties` lines ~175-200 (existing descriptions for `altium365.inputParametersPath` and `altium365.promptForProjectId`)
  </read_first>
  <action>
    **(a) Add to `contributes.commands` array** (append at the end, before the closing `]`):

    Titles MUST match exactly: `Altium 365: Pick Test Event`, `Altium 365: Create Test Event`, `Altium 365: Edit Test Event`, `Altium 365: Delete Test Event`, `Altium 365: Set Default Test Event`. Each entry has `command`, `title`, `category: 'Altium 365'`. The `pick` entry additionally has `icon: '$(symbol-event)'`.

    **(b) Add Placement A — submenu entry** to `contributes.menus["altium365.editorTitle"]`:

    Append one entry: `{ "command": "altium365.testEvents.pick", "group": "4_events@1" }`. Use a fresh group `4_events` so it sorts after existing groups (verify the highest existing group number — likely `3_*` or similar — and ensure `4_*` slots last). Do NOT add the other 4 commands to the submenu — only `pick` (the entry point); the picker action rows handle create/edit/delete/setDefault from within the QuickPick UX.

    **(c) Add Placement B — sibling editor/title button** to `contributes.menus["editor/title"]`:

    Append: `{ "command": "altium365.testEvents.pick", "group": "navigation@2", "when": "<COPY THE WHEN CLAUSE from the existing altium365.runScript or sibling editor/title entry>" }`. The icon is sourced from the command definition in step (a), so no `icon` field needed on the menu entry itself.

    **(d) DO NOT add the 5 commands to `contributes.menus.commandPalette` `when: false` block.** Confirm absence — palette visibility is the default. If any reviewer added them accidentally, REMOVE them.

    **(e) Update setting descriptions in `contributes.configuration.properties`:**

    - `altium365.inputParametersPath` — change `description` to: `Path to a JSON file used to seed the "From settings" preset when creating a test event (Altium 365: Create Test Event). Relative paths resolve to the first workspace folder. Not used at runtime — test events stored per-script are the source of truth for script parameters.`

    - `altium365.promptForProjectId` — change `description` to: `(Deprecated — kept for backward compatibility.) When true, prompts for a project ID before script execution if none is set on the active test event. Prefer creating a test event with the "Project-related" preset, which embeds the projectId directly. Will be removed in a future release.`

    Also add `"deprecationMessage": "Use the test-events workflow (Altium 365: Create Test Event → Project-related preset) instead."` and `"markdownDeprecationMessage"` (same text) to the `altium365.promptForProjectId` entry so VS Code renders it struck-through in the Settings UI.

    Commit message: `feat(999.3-05): wire test-event commands + dual UI placement + update setting descriptions (D-09, D-10, D-12, D-22)`.
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"</automated>
    <automated>for c in pick create edit delete setDefault; do node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); const f=p.contributes.commands.find(x=>x.command==='altium365.testEvents.$c'); if(!f){console.error('MISSING $c');process.exit(1)} else console.log('OK $c:',f.title)"; done</automated>
    <automated>node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); const sub=p.contributes.menus['altium365.editorTitle']; const hit=sub.find(x=>x.command==='altium365.testEvents.pick'&&x.group&&x.group.startsWith('4_events')); console.log(hit?'OK submenu':'MISSING submenu placement A'); process.exit(hit?0:1)"</automated>
    <automated>node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); const tb=p.contributes.menus['editor/title']; const hit=tb.find(x=>x.command==='altium365.testEvents.pick'); console.log(hit?'OK titlebar':'MISSING placement B'); process.exit(hit?0:1)"</automated>
    <automated>node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); const cp=p.contributes.menus.commandPalette||[]; const leaks=cp.filter(x=>x.command&&x.command.startsWith('altium365.testEvents.')); if(leaks.length){console.error('LEAK: test-event commands in commandPalette when:false block',leaks);process.exit(1)} else console.log('OK palette visibility')"</automated>
    <automated>node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); const d=p.contributes.configuration.properties['altium365.promptForProjectId']; console.log(d.deprecationMessage?'OK deprecated':'MISSING deprecationMessage'); process.exit(d.deprecationMessage?0:1)"  </verify>
  <done>
    package.json parses; all 5 commands present; submenu placement A + title-bar placement B both registered; no test-event commands leaked into commandPalette when:false block; promptForProjectId has deprecationMessage.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — verify both A/B placements + palette + setting deprecation</name>
  <what-built>
    5 commands manifested with titles. Submenu (Placement A) and editor title-bar button (Placement B) both shipped. Two settings have updated descriptions; promptForProjectId rendered as deprecated.
  </what-built>
  <how-to-verify>
    1. `npm run compile && code --extensionDevelopmentPath=$PWD`. Open a `.py` script file in the dev host.
    2. Open Command Palette → type "Altium 365: " → expect to see all 5 test-event commands (Pick, Create, Edit, Delete, Set Default).
    3. Look at the editor title bar of the open .py file → expect the existing run-script button AND a new `$(symbol-event)` icon button next to it (Placement B). Hover → tooltip says "Altium 365: Pick Test Event".
    4. Click the existing altium365.editorTitle submenu trigger (caret/3-dot menu) → expect a "Pick Test Event" item listed under the existing submenu items, sorted to the bottom (group `4_events`) — Placement A.
    5. Open a non-Python file (e.g., README.md). Confirm neither Placement A nor Placement B appears (when clause gating).
    6. Open Settings UI → search `altium365.promptForProjectId` → expect it shown struck-through with the deprecation message; the new description is visible.
    7. Search `altium365.inputParametersPath` → expect the updated description mentioning "From settings" preset and test events.
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 checks pass. Otherwise list failed step numbers.</resume-signal>
</task>

</tasks>

<verification>
- `node -e "JSON.parse(...)"` parses package.json.
- All 5 grep gates pass.
- No regressions in existing menus (manual spot-check that altium365.runScript still appears in editor/title).
- Setting descriptions verified visually in Settings UI.
</verification>

<success_criteria>
- 5 commands in `contributes.commands` with `Altium 365: <Verb> Test Event` titles.
- Both A/B placements live and gated to .py editors.
- `altium365.promptForProjectId` flagged with `deprecationMessage`.
- `altium365.inputParametersPath` description reflects escape-hatch role.
- D-09, D-10, D-12, D-22 satisfied.
</success_criteria>

<output>
Create `.planning/phases/999.3-script-test-events-backlog/999.3-05-SUMMARY.md` covering: exact command titles registered, group numbers chosen, the `when` clause copied for Placement B, UAT results, any naming changes from plan, follow-up note for Plan 06 (A/B trial telemetry / observation criteria).
</output>
