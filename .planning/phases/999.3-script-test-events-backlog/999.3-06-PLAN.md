---
phase: 999.3-script-test-events-backlog
plan: 06
type: execute
wave: 6
depends_on: [999.3-05]
files_modified:
  - src/testEvents/commands.ts
  - src/testEvents/store.ts
  - src/extension.ts
  - README.md
autonomous: false
requirements: [SCRIPT-V2-04.7, SCRIPT-V2-04.10]

must_haves:
  truths:
    - "[RESEARCH §Q9, D-21] On `altium365.testEvents.create`, after the new event is written, if the total event count for that identity exceeds 25 AND the bloat-warning flag has not been set for that identity, surface a one-time non-blocking informational toast: `[Altium 365] You now have <N> test events for this script. Consider deleting unused ones — globalState is shared across all workspaces.` Then set `globalState` key `altium365.scriptParams.bloatWarned.<identity> = true` so the warning never re-fires for the same identity."
    - "[RESEARCH §Q9] The warning fires ONLY from `.create`, not from `.edit` or `.setDefault`, so legitimate maintenance does not nag the user."
    - "[CONVENTIONS log-audit] All test-event subsystem outputChannel writes use the `[Altium 365]` prefix. Audit covers: store read/write boundaries (one line per write at INFO level), resolver decision path (which branch fired: default / picker / create / setDefault), FSP read/write (debug-only, ok to gate on outputChannel verbosity if extension already has such a flag — otherwise leave on)."
    - "[Docs] README.md gains a `## Test Events` section describing: what test events are, how to create one (Command Palette → Altium 365: Create Test Event), the per-script/per-machine globalState semantics, the relationship to the legacy `altium365.inputParametersPath` setting, and a screenshot/text-mockup of the picker UX."
    - "[D-12 A/B trial] Final UAT explicitly compares Placement A (submenu) vs Placement B (sibling button) on at least 3 real script run scenarios; observations recorded in the SUMMARY to inform a future placement decision (out of scope for this phase)."
  artifacts:
    - path: "src/testEvents/commands.ts"
      provides: "doCreateTestEvent extended with post-write bloat check; uses globalState helper from store.ts to set/get the bloatWarned flag."
      contains: "scriptParams.bloatWarned"
    - path: "src/testEvents/store.ts"
      provides: "Two new exports: `isBloatWarned(context, identity): boolean` and `markBloatWarned(context, identity): Thenable<void>`. Keys: `altium365.scriptParams.bloatWarned.<identity>` in globalState."
      contains: "bloatWarned"
    - path: "src/extension.ts"
      provides: "Activation-time log line: `[Altium 365] Test events subsystem active — N identities tracked.` (where N is store key count) so a dogfooder can see in the output channel that the subsystem booted."
      contains: "Test events subsystem active"
    - path: "README.md"
      provides: "New top-level section `## Test Events` covering concept, commands, UI affordances, storage location, migration note for `altium365.inputParametersPath` users."
      contains: "Test Events"
  key_links:
    - from: "src/testEvents/commands.ts doCreateTestEvent"
      to: "src/testEvents/store.ts isBloatWarned/markBloatWarned"
      via: "named import; post-write side effect"
      pattern: "markBloatWarned\\("
    - from: "src/extension.ts activate"
      to: "outputChannel.appendLine('[Altium 365] Test events subsystem active'"
      via: "after registerTestEventCommands call"
      pattern: "Test events subsystem active"
---

<objective>
Polish pass: ship the bloat-warning safeguard (one-time toast at >25 events per identity), audit and clean up outputChannel logs across the test-events subsystem, document the feature in README so dogfooders and future users have a single source of truth, and run a structured A/B UAT comparing Placements A and B to inform a future placement-consolidation decision.

Purpose: Closes the long-tail risk items from RESEARCH (storage bloat, observability gaps, documentation debt) and produces structured evidence from the dual-placement trial. After this plan, Phase 999.3 is fully shipped and the team has a record to consult when deciding which UI placement to keep.

Output: 4 files touched. 3 commits (feat: bloat warning + log audit; docs: README; chore: activation log line) batched as ergonomic.
</objective>

<execution_context>
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.3-script-test-events-backlog/999.3-CONTEXT.md
@.planning/phases/999.3-script-test-events-backlog/999.3-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@src/testEvents/store.ts
@src/testEvents/commands.ts
@src/testEvents/resolver.ts
@src/extension.ts
@README.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add bloat-warning helpers to store.ts + wire into doCreateTestEvent</name>
  <files>src/testEvents/store.ts, src/testEvents/commands.ts</files>
  <read_first>
    - src/testEvents/store.ts (existing readStore/writeStore signatures + key conventions)
    - src/testEvents/commands.ts (locate `doCreateTestEvent` — append the bloat check AFTER the `writeStore` call, BEFORE the editor-open call)
    - .planning/phases/999.3-script-test-events-backlog/999.3-RESEARCH.md §Q9 (warning threshold + fire-once semantics)
    - .planning/phases/999.3-script-test-events-backlog/999.3-CONTEXT.md D-21
  </read_first>
  <action>
    **(a) store.ts** — add two exports:

    - `isBloatWarned(context, identity)` — synchronous read of `globalState.get<boolean>(\`altium365.scriptParams.bloatWarned.\${identity}\`) === true`.
    - `markBloatWarned(context, identity)` — `context.globalState.update(\`altium365.scriptParams.bloatWarned.\${identity}\`, true)` returning the Thenable.

    Use the same key-prefix derivation idiom already in store.ts (CONVENTIONS: `altium365.*` prefix mandatory). No changes to readStore/writeStore signatures.

    **(b) commands.ts** — inside `doCreateTestEvent`, after the `await writeStore(context, identity, store);` line and before the `await vscode.window.showTextDocument(...)` line, insert:

    Pseudocode (implementer writes real TS):
    ```
    const eventCount = Object.keys(store.events).length;
    if (eventCount > 25 && !isBloatWarned(context, identity)) {
        vscode.window.showInformationMessage(
            `[Altium 365] You now have ${eventCount} test events for this script. Consider deleting unused ones — globalState is shared across all workspaces.`
        );
        await markBloatWarned(context, identity);
    }
    ```

    Threshold `25` MUST be a named constant `BLOAT_WARN_THRESHOLD = 25` declared at module-top of commands.ts.

    Critically: the warning fires from `.create` ONLY. Do NOT add this check to `.edit` or `.setDefault`.

    Commit message: `feat(999.3-06): one-time bloat warning at >25 test events per script (D-21)`.
  </action>
  <verify>
    <automated>npm run compile</automated>
    <automated>grep -nE "export function isBloatWarned\b" src/testEvents/store.ts</automated>
    <automated>grep -nE "export function markBloatWarned\b" src/testEvents/store.ts</automated>
    <automated>grep -nE "BLOAT_WARN_THRESHOLD\s*=\s*25" src/testEvents/commands.ts</automated>
    <automated>grep -c "isBloatWarned\|markBloatWarned" src/testEvents/commands.ts | awk '$1 < 2 {exit 1}'</automated>
    <automated>! grep -nE "isBloatWarned" src/testEvents/commands.ts | grep -iE "doEdit|doSetDefault" # ensures bloat check is NOT inside edit/setDefault handlers</automated>
  </verify>
  <done>
    Helpers exist in store.ts; doCreateTestEvent calls both; threshold named constant; check absent from edit/setDefault handlers; compile clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: outputChannel log audit across test-events subsystem + activation boot line</name>
  <files>src/testEvents/store.ts, src/testEvents/commands.ts, src/testEvents/resolver.ts, src/testEvents/eventFs.ts, src/extension.ts</files>
  <read_first>
    - .planning/codebase/CONVENTIONS.md (logging conventions: `[Altium 365]` prefix; INFO-level for state changes; debug noise discouraged)
    - All 4 test-events source files (audit existing log lines — DO NOT add files to files_modified that don't actually receive edits)
  </read_first>
  <action>
    **Audit pass** — for each file under src/testEvents/, list every `outputChannel.appendLine(...)` call and classify:

    - KEEP: state changes (write succeeded, default changed, deleted), errors caught at handler boundary, resolver decision branches.
    - DROP: per-keystroke or per-read noise (e.g., FSP readFile firing on every editor scroll); replace with no-op or remove.
    - ADD if missing: one INFO line per successful `writeStore` in store.ts (`[Altium 365] testEvents.write identity=<id> count=<N> default=<name|none>`); one line in resolver per decision branch.

    Ensure every log line starts with `[Altium 365] testEvents.<area>` for grep-ability (e.g., `[Altium 365] testEvents.resolver picked default 'foo'`).

    **Boot line in extension.ts** — immediately after `context.subscriptions.push(...testEventCmdDisposables);` in activate(), add:

    ```
    const identityCount = context.globalState.keys().filter(k => k.startsWith('altium365.scriptParams.') && !k.startsWith('altium365.scriptParams.bloatWarned.')).length;
    outputChannel.appendLine(`[Altium 365] Test events subsystem active — ${identityCount} identities tracked.`);
    ```

    Modify the files_modified list of THIS plan post-hoc if any audited file ends up unchanged — only commit files actually edited.

    Commit message: `chore(999.3-06): audit + normalize test-events outputChannel logs`.
  </action>
  <verify>
    <automated>npm run compile</automated>
    <automated>grep -rnE "outputChannel\.appendLine\(" src/testEvents/ | grep -vE "\[Altium 365\] testEvents\." && exit 1 || true</automated>
    <automated>grep -nE "Test events subsystem active" src/extension.ts</automated>
  </verify>
  <done>
    All log lines in src/testEvents/ start with `[Altium 365] testEvents.`; activation logs identity count; no per-keystroke FSP noise; compile clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: README.md — add ## Test Events section</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — match heading style, code-block style, screenshot/image conventions if any)
  </read_first>
  <action>
    Add a new top-level `## Test Events` section, placed after the existing usage/quick-start section and before any troubleshooting/contributing sections. Cover:

    1. **What** — One-paragraph explainer: named JSON parameter sets attached to a script, AWS-Lambda-style test events. Used by local Run, Debug, and Remote Execute uniformly.
    2. **Why** — Replaces the older `inputParametersPath` flow; lets you keep multiple parameterizations per script (e.g., "small project", "with errors", "production-id") without juggling files.
    3. **How** — Command Palette commands list (table: Command | What it does):
       | `Altium 365: Pick Test Event` | Choose an event to run with, or create/edit from the picker |
       | `Altium 365: Create Test Event` | New event with Empty / Project-related / From-settings presets |
       | `Altium 365: Edit Test Event` | Open an event's JSON in a tab; Cmd+S saves |
       | `Altium 365: Delete Test Event` | Remove an event |
       | `Altium 365: Set Default Test Event` | Mark an event as the default for unattended runs |
    4. **UI affordances** — Mention both placements: title-bar icon next to the run button AND the entry in the Altium 365 editor-title submenu.
    5. **Storage** — `vscode.context.globalState`, scoped to the user's machine, shared across all workspaces. Identity key is derived from `(workspaceId, scriptIdOrLocalPath)`. Not synced via Settings Sync (intentional — params often contain machine-specific paths or IDs).
    6. **Migration note for `inputParametersPath` users** — That setting is now an escape-hatch seed for the `From settings` create preset; runtime params always come from the active test event.
    7. **`promptForProjectId` deprecation note** — Brief mention; pointer to use `Project-related` preset.

    No screenshots required (out of scope for v1 docs). Plain text mockup of the picker is OK if useful.

    Commit message: `docs(999.3-06): document test-events feature in README`.
  </action>
  <verify>
    <automated>grep -nE "^## Test Events" README.md</automated>
    <automated>grep -cE "altium365\.testEvents\.(pick|create|edit|delete|setDefault)|Pick Test Event|Create Test Event|Edit Test Event|Delete Test Event|Set Default Test Event" README.md | awk '$1 < 5 {exit 1}'</automated>
    <automated>grep -nE "inputParametersPath" README.md # confirms migration note present</automated>
  </verify>
  <done>
    `## Test Events` section exists, references all 5 commands, mentions storage location + migration notes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Full A/B UAT — final phase acceptance + Placement A vs B observations</name>
  <what-built>
    Complete Phase 999.3: storage, FSP editor, 5 commands, dual UI placements, bloat warning, logs, docs. Ready for the structured A/B trial that informs future placement choice.
  </what-built>
  <how-to-verify>
    **Acceptance gates (all must pass):**
    1. Run all UATs from Plans 02, 03, 04, 05 once more in sequence on a clean Extension Development Host. All must pass.
    2. Verify the activation boot line appears in the Output channel on extension load.
    3. **Bloat warning trial:** create 26 events for one script (use a small script + 26 sequential `Create` calls with empty preset, unique names `e01`…`e26`). On the 26th create, expect the bloat toast. Create `e27` — expect NO toast (fire-once flag).
    4. **Local Run loop:** with 3 events stored, trigger `altium365.runScript`. Expect picker → select event → script runs with that event's body as params. Repeat with Remote Execute. Repeat with Debug.
    5. README renders correctly on github.com preview (paste into a gist or use VS Code's markdown preview).

    **A/B observation pass (record in SUMMARY):**
    For each of 3 real script-run scenarios (your choice — pick representative scripts you actually use), use Placement A (open submenu, click Pick) then repeat with Placement B (click the title-bar icon). Note:
    - Time-to-action (subjective: faster/slower/same)
    - Visual clutter impression (title bar with N+1 buttons vs submenu hiding the entry)
    - Discoverability (would a first-time user notice it?)
    - Conflicts/collisions with existing affordances

    Write 3-5 bullet observations into 999.3-06-SUMMARY.md under `## A/B Trial Observations`. No decision required here — observations only.

    **Regression sweep:**
    6. Existing `altium365.runScript` still works on a script with zero stored events (resolver should dispatch `.create` per Plan 04 stub replacement, user creates one, run continues).
    7. `altium365.inputParametersPath` set to a valid JSON file → Create with `From settings` preset still seeds correctly (validates Plan 04 wiring did not regress).
    8. `altium365.promptForProjectId` toggle still functional (back-compat — D-22 keeps the setting alive).
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 numbered gates pass AND observations recorded. Otherwise list failed gates.</resume-signal>
</task>

</tasks>

<verification>
- `npm run compile` clean across all source edits.
- All grep gates from Tasks 1-3 pass.
- All UATs from Plans 02-05 pass on final sweep.
- Bloat warning fires once at threshold and never again.
- README renders cleanly.
- A/B observations captured.
</verification>

<success_criteria>
- Bloat warning shipped + fire-once flag stored under `altium365.scriptParams.bloatWarned.<identity>`.
- All test-events outputChannel logs follow `[Altium 365] testEvents.<area>` prefix.
- Activation boot line confirms subsystem is alive + reports identity count.
- README `## Test Events` section covers 7 listed topics.
- A/B observations recorded for future placement-consolidation decision.
- Requirements SCRIPT-V2-04.7 and SCRIPT-V2-04.10 satisfied.
- Phase 999.3 ready to mark complete in ROADMAP.md.
</success_criteria>

<output>
Create `.planning/phases/999.3-script-test-events-backlog/999.3-06-SUMMARY.md` covering: bloat warning verification (count at which fired, fire-once confirmed), log audit before/after diff, README section link, A/B Trial Observations section (3-5 bullets per placement), regression sweep results, and a final "Phase 999.3 acceptance" line. Also create `.planning/phases/999.3-script-test-events-backlog/999.3-PHASE-SUMMARY.md` rolling up all 6 plan SUMMARYs into a single phase-level retrospective per ROADMAP convention.
</output>
