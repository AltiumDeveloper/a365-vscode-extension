---
phase: 999.3-script-test-events-backlog
plan: 02
subsystem: scripting
tags: [vscode-extension, async-mutex, parameter-resolution, atomic-migration]

# Dependency graph
requires:
  - phase: 999.3-01
    provides: testEvents/store + testEvents/identity + normalizeLocalScriptKey
provides:
  - src/testEvents/resolver.ts (unified async resolveScriptParameters, per-identity AsyncMutex)
  - src/testEvents/importSibling.ts (one-shot 3-button import flow with decline marker)
  - prepareRun + executeRemoteScript Block B now route through the single resolver
  - Legacy resolveScriptParameters in remoteExecution.ts deleted
  - altium365.promptForProjectId is now a no-op (D-22)
affects: [999.3-03 FSP writes commit via writeStore, 999.3-04 commands replace two TODO(999.3-04) stubs, 999.3-05 menu wiring + setting deprecation, 999.3-06 polish]

tech-stack:
  added: []
  patterns:
    - "Per-identity AsyncMutex serializes first-run resolution (Pitfall 3 — no double-fire)"
    - "Pitfall 8 atomicity: divergence-prone callers migrated in a single commit"
    - "Escape-hatch precedence (inputParametersPath > sibling > store > stub) documented in resolver header"

key-files:
  created:
    - src/testEvents/resolver.ts
    - src/testEvents/importSibling.ts
  modified:
    - src/extension.ts (prepareRun parameter block replaced; pickProjectId import removed)
    - src/remoteExecution.ts (Block B replaced; legacy resolveScriptParameters deleted; pickProjectId import removed; module doc-comment updated to D-20..D-22)

key-decisions:
  - "Resolver returns Awaited<ReturnType<...>> in SetupResult type alias — async-aware version of the legacy ReturnType wrapping"
  - "Stale doc-comments updated rather than deleted — preserves migration trail for future readers (D-05 → D-20..D-22)"
  - "Defensive: if escape-hatch file does not exist or is not a JSON object, log warning and continue down resolver chain rather than throwing"

patterns-established:
  - "Resolver entry point: every caller passes ScriptIdentity (not raw uri/scriptId) — keeps identity normalization in one place"
  - "STAGE-ONLY tasks: file is staged via git add but never committed; Pitfall 8 atomicity protected by pre-flight gate"

requirements-completed: [SCRIPT-V2-04.3, SCRIPT-V2-04.6, SCRIPT-V2-04.8, SCRIPT-V2-04.9]

duration: ~20min
completed: 2026-05-23
---

# Phase 999.3 Plan 02: ATOMIC Resolver Migration Summary

**Both run paths now resolve script parameters through a single async resolver with per-identity mutex, sibling-import prompt, escape-hatch precedence, and silent-default semantics — divergence eliminated in one atomic commit.**

## Performance

- **Duration:** ~20 min
- **Completed:** 2026-05-23
- **Tasks:** 3 implementation tasks + 1 human UAT (approved)
- **Files created:** 2
- **Files modified:** 2
- **Commits:** 1 (Pitfall 8 atomic)

## Accomplishments
- Unified `resolveScriptParameters` (src/testEvents/resolver.ts) consumed identically by `prepareRun` (local Python runner) and `executeRemoteScript` Block B (remote gloScrExecuteScript)
- One-shot sibling-import flow (src/testEvents/importSibling.ts) with `Import` / `Not now` / `Never for this file` 3-button toast + decline marker
- Per-identity AsyncMutex (Pitfall 3) prevents double-fire on concurrent first-run prompts
- Legacy resolver in remoteExecution.ts (lines 265-295) deleted; `pickProjectId` import removed from both extension.ts and remoteExecution.ts
- `altium365.promptForProjectId` setting is now a no-op (SCRIPT-V2-04.8); Plan 05 will mark deprecated in package.json
- Pitfall 8 atomicity preserved: 4 files in 1 commit (prepareRun + Block B + 2 new modules)
- Two `TODO(999.3-04)` stubs in resolver mark the first-run + setDefault UI dispatch points Plan 04 will replace
- Human UAT (7 scenarios) approved

## Task Commits

Single atomic commit per Pitfall 8:

1. **Tasks 1+2+3 (atomic):** `762727f` — `feat(999.3-02): unify script-parameter resolution across local + remote (D-20..D-22, Pitfall 8 atomic)`

Tasks 1 and 2 were STAGED but not committed individually; pre-flight gate verified Tasks 1+2 outputs were staged-but-not-committed before Task 3 ran, then all 4 files landed in one commit.

## Files Created/Modified
- `src/testEvents/resolver.ts` (NEW, 134 lines) — `resolveScriptParameters(ctx, identity, output, options?)` + `stringifyEvent` helper; module-level `AsyncMutex` (CONVENTIONS exception documented)
- `src/testEvents/importSibling.ts` (NEW, 79 lines) — `maybePromptForSiblingImport(ctx, identity, output)`; marker key `altium365.scriptParams.importedFrom.<identity>`; never deletes the sibling file (D-08 hard rule)
- `src/extension.ts` — prepareRun parameter block (~44 lines) replaced with single resolver call fed by `resolveScriptIdentity(targetUri)`; tmpfile-write pattern retained; `pickProjectId` import removed
- `src/remoteExecution.ts` — Block B (~37 lines) replaced; legacy `resolveScriptParameters` function (~30 lines) deleted; module doc-comment updated D-05 → D-20..D-22; `pickProjectId` import removed; `SetupResult.parameters` type changed to `Awaited<ReturnType<...>>` for async correctness

## Decisions Made
- Stale doc-comments updated rather than deleted — preserves the D-05 → D-20..D-22 migration trail for future readers; verification gate intent ("no code references") satisfied
- Defensive escape-hatch: missing file or non-object JSON logs a warning and falls through to next branch rather than throwing — matches Phase 6 user experience for invalid `inputParametersPath`
- Resolver does NOT re-export `ScriptIdentity` — callers import directly from `./identity` to avoid barrel-shaped patterns (CONVENTIONS)

## Deviations from Plan

None — plan executed exactly as written. Three remaining text matches for `promptForProjectId` / `altium365.scriptParams.` in run-path files are intentional doc-comments documenting the migration; verification gates use `grep -v '^#'` (shell-comment filter) but the intent ("no code references") is satisfied — zero code reads/writes either symbol.

## Issues Encountered
None — atomic commit landed cleanly; all 7 UAT scenarios approved on first pass.

## User Setup Required
None.

## Next Phase Readiness
- Plan 03 (FSP on `altium365-event://`) can register the FileSystemProvider; `writeStore` is the commit point per D-17
- Plan 04 (commands + first-run UI) will replace the two `TODO(999.3-04)` stubs in resolver.ts with `vscode.commands.executeCommand` dispatch
- No blockers; working tree clean

---
*Phase: 999.3-script-test-events-backlog*
*Plan: 02*
*Completed: 2026-05-23*
