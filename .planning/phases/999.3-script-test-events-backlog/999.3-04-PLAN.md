---
phase: 999.3-script-test-events-backlog
plan: 04
type: execute
wave: 4
depends_on: [999.3-01, 999.3-02, 999.3-03]
files_modified:
  - src/testEvents/picker.ts
  - src/testEvents/commands.ts
  - src/testEvents/resolver.ts
  - src/extension.ts
autonomous: false
requirements: [SCRIPT-V2-04.4, SCRIPT-V2-04.6, SCRIPT-V2-04.8]

must_haves:
  truths:
    - "[D-10, D-11] 5 palette-eligible commands exist: `altium365.testEvents.pick`, `.create`, `.edit`, `.delete`, `.setDefault`. All registered in activate(), all visible in Command Palette (no `when: false` clause)."
    - "[D-12, RESEARCH §Q3] `pick` shows a QuickPick with: (a) optional 'Default: <name>' header item, (b) separator 'Saved events', (c) one row per stored event with detail showing modified timestamp, (d) separator 'Actions', (e) `$(add) Create new event…`, `$(edit) Edit current default…`, `$(close) Run with empty params`. Selecting a row resolves to that event; selecting Create routes to `.create`; selecting Run-empty resolves to `{}`."
    - "[D-09 escape-hatch] `create` offers presets: 'Empty', 'Project-related (prompts for project picker via pickProjectId)', 'From settings.inputParametersPath (read JSON file once, seed)'. The settings preset reads `vscode.workspace.getConfiguration('altium365').get('inputParametersPath')`, resolves relative to workspace root, JSON.parses, seeds the event body; on read failure surfaces a non-blocking warning and falls back to Empty."
    - "[D-04] All five commands operate on globalState via writeStore/readStore — no workspaceState writes."
    - "[Plan 02 stub replacement] The two `TODO(999.3-04)` stubs in src/testEvents/resolver.ts (first-run autoshow + 'no default set') are replaced with real dispatches: first-run calls `vscode.commands.executeCommand('altium365.testEvents.create', identity, {autoSetDefault: true})`; missing-default calls `'.setDefault'` with the identity. Both return the resolved event or `undefined` (user cancelled)."
    - "[CONVENTIONS] All command handlers use the `do*` private-function pattern (e.g., `async function doPickTestEvent(...)`) inside commands.ts; the public surface is `registerTestEventCommands(context, outputChannel)` returning a disposable array pushed onto subscriptions in extension.ts."
    - "[D-15] `edit` command opens `buildEventUri(identity, name)` via `vscode.window.showTextDocument(vscode.workspace.openTextDocument(uri))` — relies on Plan 03's FSP."
  artifacts:
    - path: "src/testEvents/picker.ts"
      provides: "Exports `pickTestEvent(identity, store, options?): Promise<{ kind: 'event', name: string, body: unknown } | { kind: 'create' } | { kind: 'empty' } | undefined>` — pure QuickPick UX, no side effects on store."
      contains: "vscode.window.createQuickPick"
    - path: "src/testEvents/commands.ts"
      provides: "Exports `registerTestEventCommands(context, outputChannel)`. Internally defines `doPickTestEvent`, `doCreateTestEvent`, `doEditTestEvent`, `doDeleteTestEvent`, `doSetDefaultTestEvent`."
      contains: "vscode.commands.registerCommand"
    - path: "src/testEvents/resolver.ts"
      provides: "Two `TODO(999.3-04)` markers from Plan 02 replaced with vscode.commands.executeCommand dispatches; resolver still returns the resolved event or undefined."
      contains: "altium365.testEvents.create"
    - path: "src/extension.ts"
      provides: "activate() calls `registerTestEventCommands(context, outputChannel)` and pushes returned disposables onto subscriptions. Placed AFTER TestEventFs registration (Plan 03 dependency)."
      contains: "registerTestEventCommands"
  key_links:
    - from: "src/testEvents/commands.ts doEditTestEvent"
      to: "src/testEvents/eventFs.ts buildEventUri"
      via: "named import + vscode.workspace.openTextDocument"
      pattern: "buildEventUri\\("
    - from: "src/testEvents/resolver.ts"
      to: "vscode.commands.executeCommand('altium365.testEvents.create'|'.setDefault')"
      via: "stub replacement"
      pattern: "executeCommand\\(\\s*'altium365\\.testEvents\\."
    - from: "src/testEvents/commands.ts doCreateTestEvent (project-related preset)"
      to: "src/projectPicker.ts pickProjectId"
      via: "named import"
      pattern: "pickProjectId\\("
---

<objective>
Ship the user-facing surface for test events: the QuickPick picker (RESEARCH §Q3 UX) plus the 5 palette commands (`pick`, `create`, `edit`, `delete`, `setDefault`). Wire activation. Replace the two `TODO(999.3-04)` stubs left in resolver.ts by Plan 02 with real command dispatches, closing the first-run and missing-default loops.

Purpose: This is the user's primary entry point. Without this plan, the storage/FSP layers from Plans 01–03 are invisible. With this plan, a user can manage events end-to-end from the Command Palette, and the resolver path (prepareRun + remote execute) is fully unblocked.

Output: Two new modules + a 2-line edit to resolver.ts + activation registration. Four commits (one per command group + resolver stub replacement, batched as ergonomically possible).
</objective>

<execution_context>
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.3-script-test-events-backlog/999.3-CONTEXT.md
@.planning/phases/999.3-script-test-events-backlog/999.3-RESEARCH.md
@.planning/phases/999.3-script-test-events-backlog/999.3-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@src/testEvents/store.ts
@src/testEvents/identity.ts
@src/testEvents/eventFs.ts
@src/testEvents/resolver.ts
@src/scriptCommands.ts
@src/projectPicker.ts
@src/extension.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create src/testEvents/picker.ts — the QuickPick UX</name>
  <files>src/testEvents/picker.ts</files>
  <read_first>
    - src/projectPicker.ts (full file — analog for QuickPick lifecycle, separator usage, busy/placeholder patterns)
    - src/testEvents/store.ts (readStore signature; TestEvent shape)
    - .planning/phases/999.3-script-test-events-backlog/999.3-RESEARCH.md §Q3 (picker UX spec)
    - .planning/phases/999.3-script-test-events-backlog/999.3-CONTEXT.md D-12, D-13 (default-event semantics; cancel = abort run)
  </read_first>
  <action>
    Export `pickTestEvent(identity: string, store: TestEventStore, options?: { headerLabel?: string }): Promise<PickerResult | undefined>` where `PickerResult` is the discriminated union `{ kind: 'event'; name: string; body: unknown } | { kind: 'create' } | { kind: 'empty' }`. Undefined means user dismissed (Esc / outside click) — caller MUST treat as cancellation per D-13.

    Implementation outline (no fenced code in plan; the implementer writes the file):
    - Build QuickPickItem[] in this exact order: optional default-header row (`label: '$(star-full) Default: <name>'`, `detail: 'Resolved automatically when no picker is shown'`) shown only if `store.defaultEventName` exists, then separator `{ kind: vscode.QuickPickItemKind.Separator, label: 'Saved events' }`, then one row per event sorted alphabetically by event name (`Object.keys(events).sort()`) — label = event name, detail = single-line JSON preview of the body truncated to 80 chars (e.g., `{"projectId":"…"}`), then separator `'Actions'`, then three action rows tagged with a private `action` discriminator on the item.
    - Use `vscode.window.createQuickPick()` (not `showQuickPick`) so we can attach buttons later (Plan 06 may add a gear). Set `placeholder = 'Select a test event or action…'`, `matchOnDetail = true`, `ignoreFocusOut = false`.
    - On `onDidAccept`: map the selected item back to the union and resolve. On `onDidHide` without accept: resolve `undefined`. Always `qp.dispose()` in `finally`.

    Follow CONVENTIONS: 4-space indent, async/await, no module-level state, named imports only (no barrel re-exports). Outputchannel logs are optional here — keep picker UX silent on success, log only on unexpected errors caught at the boundary.
  </action>
  <verify>
    <automated>npm run compile</automated>
    <automated>grep -nE "export (async )?function pickTestEvent\b" src/testEvents/picker.ts</automated>
    <automated>grep -nE "QuickPickItemKind\.Separator" src/testEvents/picker.ts</automated>
    <automated>grep -nE "createQuickPick\(" src/testEvents/picker.ts</automated>
  </verify>
  <done>
    picker.ts compiles; exports `pickTestEvent`; uses createQuickPick + separators; no QuickPickItem array hardcoded outside the function.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create src/testEvents/commands.ts — 5 commands with do* handlers</name>
  <files>src/testEvents/commands.ts</files>
  <read_first>
    - src/scriptCommands.ts:42-78 (analog: `registerScriptCommands` shape + do* pattern + outputChannel passthrough)
    - src/projectPicker.ts:22 (`pickProjectId` signature — used by 'project-related' preset)
    - src/testEvents/store.ts (readStore/writeStore signatures)
    - src/testEvents/eventFs.ts (`buildEventUri` signature)
    - src/testEvents/picker.ts (Task 1 output)
    - .planning/phases/999.3-script-test-events-backlog/999.3-CONTEXT.md D-09, D-10, D-11, D-12, D-14, D-22
  </read_first>
  <action>
    Export `registerTestEventCommands(context: vscode.ExtensionContext, outputChannel: vscode.OutputChannel): vscode.Disposable[]`. Internally define and register:

    1. **`altium365.testEvents.pick`** — `doPickTestEvent(identity?: string)`. If identity omitted (palette invocation with no script context), prompt the user to pick a script first using the existing sidePanel selection or surface an `[Altium 365] No active script — open a script editor or use the side panel.` warning and return. With identity, load store, call `pickTestEvent`, on result `{kind:'event'}` return the body to the caller (palette case: copy summary to outputChannel); on `{kind:'create'}` dispatch `.create`; on `{kind:'empty'}` log `Run with empty params selected`; on `undefined` log `cancelled`.

    2. **`altium365.testEvents.create`** — `doCreateTestEvent(identity?: string, opts?: { autoSetDefault?: boolean })`. Resolve identity (same fallback as above). Prompt for a name via `vscode.window.showInputBox` with validation: non-empty, unique within store, max 64 chars, no `/`. Then offer a preset QuickPick: `Empty`, `Project-related`, `From settings.inputParametersPath`. **Empty:** body = `{}`. **Project-related:** call `pickProjectId(context)` (or whatever the existing signature is — verify in `<read_first>`); body = `{ "projectId": "<picked>" }`. **Settings preset:** read `vscode.workspace.getConfiguration('altium365').get<string>('inputParametersPath')`; if set, resolve relative to first workspace folder, read file, JSON.parse, use as body; on read/parse failure show `[Altium 365] Could not read inputParametersPath: <err>. Starting from empty.` toast and use `{}`. Write event via `writeStore`. If `opts?.autoSetDefault` OR store had zero events before this call, also set as default. Then open the editor: `await vscode.window.showTextDocument(await vscode.workspace.openTextDocument(buildEventUri(identity, name)))`. Return `{ name, body }` for the resolver call path.

    3. **`altium365.testEvents.edit`** — `doEditTestEvent(identity?: string, name?: string)`. If `name` omitted, run the picker first (filter to events only — pass `options.headerLabel = 'Pick event to edit'`). Open `buildEventUri(identity, name)` via showTextDocument.

    4. **`altium365.testEvents.delete`** — `doDeleteTestEvent(identity?: string, name?: string)`. Picker if name omitted. Confirm via `showWarningMessage(..., {modal: true}, 'Delete')`. On confirm, mutate store: remove event; if deleted event was default, clear `defaultEventName`. `writeStore`.

    5. **`altium365.testEvents.setDefault`** — `doSetDefaultTestEvent(identity?: string, name?: string)`. Picker if name omitted. Mutate store: `defaultEventName = name`. `writeStore`. Toast: `[Altium 365] Default event set to "<name>".`

    All commands wrap their body in `try { await do…(); } catch (err) { outputChannel.appendLine('[Altium 365] testEvents.<cmd> failed: ' + err); vscode.window.showErrorMessage(...); }` at the registration boundary (CONVENTIONS error-surfacing rule).

    Return `[reg1, reg2, reg3, reg4, reg5]` so caller can `subscriptions.push(...returned)`.

    Commit message: `feat(999.3-04): add 5 test-event commands + QuickPick picker (D-10, D-11, D-12, D-09)`.
  </action>
  <verify>
    <automated>npm run compile</automated>
    <automated>for c in pick create edit delete setDefault; do grep -nE "registerCommand\(\s*'altium365\.testEvents\.$c'" src/testEvents/commands.ts || echo "MISSING $c"; done</automated>
    <automated>grep -nE "export function registerTestEventCommands\b" src/testEvents/commands.ts</automated>
    <automated>grep -nE "pickProjectId\(" src/testEvents/commands.ts</automated>
    <automated>grep -nE "buildEventUri\(" src/testEvents/commands.ts</automated>
  </verify>
  <done>
    All 5 commands registered. `pickProjectId` consumed for project-related preset. `buildEventUri` consumed for edit. Each command has a `do*` handler. `npm run compile` clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Replace resolver.ts TODO stubs + register commands in extension.ts</name>
  <files>src/testEvents/resolver.ts, src/extension.ts</files>
  <read_first>
    - src/testEvents/resolver.ts (find the two `TODO(999.3-04)` markers from Plan 02)
    - src/extension.ts:85-160 (activate block; locate where Plan 03 registered TestEventFs)
    - src/testEvents/commands.ts (Task 2 output — for the import)
  </read_first>
  <action>
    **(a) resolver.ts** — replace the two `TODO(999.3-04)` blocks:

    - First-run autoshow stub → `const created = await vscode.commands.executeCommand<{ name: string; body: unknown } | undefined>('altium365.testEvents.create', identity, { autoSetDefault: true }); if (!created) return undefined; return created.body;`
    - Missing-default stub → `const picked = await vscode.commands.executeCommand<{ name: string; body: unknown } | undefined>('altium365.testEvents.setDefault', identity); ...` then re-read store and resolve from new default (or return undefined if user cancelled).

    Keep the existing `asyncMutex` guard around both branches (Pitfall 3 — first-run re-entrancy).

    **(b) extension.ts** — in `activate()`, AFTER the TestEventFs registration block from Plan 03 (around new lines ~100-105), add:

    `import { registerTestEventCommands } from './testEvents/commands';` at the top.

    In activate body: `const testEventCmdDisposables = registerTestEventCommands(context, outputChannel); context.subscriptions.push(...testEventCmdDisposables);`

    Commit message: `feat(999.3-04): wire test-event commands into activation + close resolver first-run/missing-default loops`.
  </action>
  <verify>
    <automated>npm run compile</automated>
    <automated>! grep -nE "TODO\(999\.3-04\)" src/testEvents/resolver.ts</automated>
    <automated>grep -nE "executeCommand.*altium365\.testEvents\.create" src/testEvents/resolver.ts</automated>
    <automated>grep -nE "executeCommand.*altium365\.testEvents\.setDefault" src/testEvents/resolver.ts</automated>
    <automated>grep -nE "registerTestEventCommands\(" src/extension.ts</automated>
  </verify>
  <done>
    No `TODO(999.3-04)` markers remain in resolver. resolver dispatches `.create`/`.setDefault`. extension.ts activate registers commands. `npm run compile` clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human UAT — end-to-end test-event lifecycle from Command Palette</name>
  <what-built>
    5 palette commands + picker UX + resolver stubs closed. Test events fully manageable without touching the menu wiring (which lands in Plan 05).
  </what-built>
  <how-to-verify>
    Launch Extension Development Host. From Command Palette:
    1. **`Altium 365: Create Test Event`** with no script context → expect graceful warning toast; no crash.
    2. Open a script in the editor. Run `Altium 365: Create Test Event` → name `default`, preset `Empty` → expect editor tab opens for `altium365-event://.../default.json`, event saved, AND auto-set as default (because store was empty before).
    3. Run `Altium 365: Pick Test Event` → expect QuickPick shows `Default: default`, separator, `default` row, separator, 3 action rows.
    4. Run `Altium 365: Create Test Event` → name `with-project`, preset `Project-related` → expect project picker prompts, event body contains `{"projectId":"<id>"}`, editor opens, NOT auto-set as default.
    5. Run `Altium 365: Set Default Test Event` → pick `with-project` → expect toast confirms; next `Pick Test Event` shows `Default: with-project`.
    6. Run `Altium 365: Edit Test Event` → pick `default` → editor opens for `default.json`. Save a change. Reopen. Persists.
    7. Run `Altium 365: Delete Test Event` → pick `with-project` → modal confirm → on Delete, expect store mutated (verify by reopening picker — only `default` remains, `defaultEventName` cleared back to `default` per delete logic OR cleared to undefined depending on impl; if cleared to undefined, resolver path will route to setDefault next run — both acceptable, document choice in SUMMARY).
    8. **Resolver loop integration:** with zero events stored for a fresh script, trigger local Run (existing `altium365.runScript` command). **Expect:** resolver detects no events, dispatches `.create`, user creates one, run proceeds with that event as params. Cancelling the create dialog aborts the run with no error toast (per D-13).
    9. **`inputParametersPath` preset:** set `altium365.inputParametersPath` in settings to a valid JSON file. Run Create → choose `From settings` preset → expect body seeded from that file. Set path to a non-existent file → expect non-blocking warning + fallback to empty.
  </how-to-verify>
  <resume-signal>Type "approved" if all 9 checks pass. Otherwise list failed step numbers.</resume-signal>
</task>

</tasks>

<verification>
- `npm run compile` clean across all 3 source edits.
- All 5 commands callable from palette (verified in Task 4).
- Two TODO markers from Plan 02 fully removed.
- No new npm dependencies.
- Resolver first-run + missing-default loops close without re-entrancy issues (verified by triggering Run twice rapidly — see Task 4 step 8).
</verification>

<success_criteria>
- `src/testEvents/picker.ts` and `src/testEvents/commands.ts` exist with documented exports.
- `src/testEvents/resolver.ts` contains zero `TODO(999.3-04)` markers and dispatches both commands.
- `src/extension.ts` registers all 5 commands via `registerTestEventCommands`.
- 9-step UAT passes.
- Decisions D-09, D-10, D-11, D-12, D-13, D-14, D-22 visibly satisfied.
</success_criteria>

<output>
Create `.planning/phases/999.3-script-test-events-backlog/999.3-04-SUMMARY.md` covering: command IDs registered, picker UX deviations (if any) from RESEARCH §Q3, UAT step-by-step results, the `inputParametersPath` preset behavior on missing file, the delete-default policy chosen (clear to undefined vs auto-promote next event), follow-ups for Plan 05 (menu wiring) and Plan 06 (bloat warning + docs).
</output>
