---
phase: 999.3-script-test-events-backlog
plan: 01
subsystem: scripting
tags: [vscode-extension, globalstate, jsonschema, identity, test-events]

# Dependency graph
requires:
  - phase: 06-script-run
    provides: localScriptCache (tmp-file → scriptId lookup) + ad-hoc params blob this phase replaces
provides:
  - schemas/test-event.schema.json (draft-07 bundled JSONSchema)
  - src/testEvents/store.ts (typed CRUD over globalState 'altium365.scriptParams.<identity>')
  - src/testEvents/identity.ts (resolveScriptIdentity union + 3-branch resolver)
  - src/localScriptCache.ts exports normalizeLocalScriptKey (single source of truth)
affects: [999.3-02 resolver+migration, 999.3-03 FSP, 999.3-04 commands+UI, 999.3-05 menus, 999.3-06 polish]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Bundled JSONSchema in /schemas referenced via package.json contributes.jsonValidation"
    - "Pure-module identity resolver — no FSP machinery imports, regex duplicated to keep module light"
    - "globalState (not workspaceState) for cross-window test-event storage (D-01)"

key-files:
  created:
    - schemas/test-event.schema.json
    - src/testEvents/store.ts
    - src/testEvents/identity.ts
  modified:
    - src/localScriptCache.ts (promoted private key() to exported normalizeLocalScriptKey)
    - .planning/REQUIREMENTS.md (SCRIPT-V2-04 + 10 sub-bullets + traceability row)
    - .planning/ROADMAP.md (Phase 999.3 finalized plan list)

key-decisions:
  - "Copied URI regex from remoteScriptFs.ts into identity.ts verbatim rather than importing — avoids pulling FSP into a pure resolver module"
  - "Followed CONVENTIONS.md no-barrel rule — downstream plans import direct from './testEvents/store' and './testEvents/identity'"
  - "TestEventStore.events typed as Record<string, Record<string, unknown>> for stricter consumer typing than the plan's Record<string, object>"

patterns-established:
  - "Identity normalization: normalizeLocalScriptKey is the sole owner of cross-platform fsPath case rules; never re-implement"
  - "Store key prefix exported as TEST_EVENT_KEY_PREFIX — verified by Plan 02 that no other module hardcodes 'altium365.scriptParams.'"

requirements-completed: [SCRIPT-V2-04.1, SCRIPT-V2-04.2]

# Metrics
duration: ~15min
completed: 2026-05-23
---

# Phase 999.3 Plan 01: Foundation Summary

**Bundled JSONSchema + typed globalState CRUD store + pure identity resolver — three building blocks for the test-events feature, zero behavior change.**

## Performance

- **Duration:** ~15 min
- **Completed:** 2026-05-23
- **Tasks:** 3
- **Files created:** 3
- **Files modified:** 3

## Accomplishments
- Exported `normalizeLocalScriptKey` from `localScriptCache.ts` as the single source of truth for cross-platform fsPath identity
- Created `src/testEvents/store.ts` — 6 functions (readStore/writeStore/deleteEvent/listEvents/setDefault/eventCount) + `TestEventStore` interface + `TEST_EVENT_KEY_PREFIX` constant; all CRUD bounded to `context.globalState`
- Created `src/testEvents/identity.ts` — pure 3-branch resolver returning `ScriptIdentity` discriminated union (`{kind:'remote'|'local', identity:string}`)
- Created `schemas/test-event.schema.json` — draft-07, optional `projectId:string`, `additionalProperties:true`
- Registered SCRIPT-V2-04 + 10 sub-bullets in REQUIREMENTS.md with traceability row
- Finalized Phase 999.3 entry in ROADMAP.md with all 6 plan checkboxes

## Task Commits

Each task was committed atomically:

1. **Task 1: Promote `key()` → `normalizeLocalScriptKey`** — `b43e433` (refactor)
2. **Task 2: schema + store + identity** — `1553524` (feat)
3. **Task 3: REQUIREMENTS + ROADMAP wiring** — `1150cba` (docs)

## Files Created/Modified
- `schemas/test-event.schema.json` — Bundled JSONSchema (draft-07) for test-event payloads; loose by design (`additionalProperties:true`)
- `src/testEvents/store.ts` — Typed globalState CRUD wrapper around `altium365.scriptParams.<identity>`; defensive parse returns undefined for malformed blobs
- `src/testEvents/identity.ts` — `resolveScriptIdentity(uri)` mapping VS Code URIs to the discriminated identity space used everywhere downstream
- `src/localScriptCache.ts` — Promoted private `key()` to exported `normalizeLocalScriptKey` with JSDoc; 2 in-file callsites updated
- `.planning/REQUIREMENTS.md` — SCRIPT-V2-04 entry under v2 §Script Management + traceability row
- `.planning/ROADMAP.md` — Phase 999.3 plans list (6 checkboxes), requirements line, plans count

## Decisions Made
- Identity regex duplicated in `identity.ts` instead of imported from `remoteScriptFs.ts` — keeps the resolver module pure (no FSP machinery import pull-in)
- Typed `events: Record<string, Record<string, unknown>>` (stricter than plan's `Record<string, object>`) — better consumer type-safety with no contract change
- No barrel `index.ts` per CONVENTIONS.md; downstream plans use named imports from the module files directly

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None.

## Next Phase Readiness
- All three foundation contracts (schema, store, identity) are stable and exported
- Plan 02 (ATOMIC resolver migration) can import these without re-exploring source — `prepareRun` and `executeRemoteScript` Block B both consume `resolveScriptIdentity` + `readStore`
- No blockers; working tree clean

---
*Phase: 999.3-script-test-events-backlog*
*Plan: 01*
*Completed: 2026-05-23*
