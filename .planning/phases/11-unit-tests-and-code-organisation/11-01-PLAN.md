---
phase: "11"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/__mocks__/vscode.ts
  - src/auth.ts
  - test/auth.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "test/__mocks__/vscode.ts exports EventEmitter, Disposable, Uri, FileSystemError, window stubs, workspace stubs, commands stubs, and factory functions makeSecretStorage/makeGlobalState/makeExtensionContext"
    - "decodeIdTokenClaims, userLabelFromClaims, withExpiry, isExpired are exported from src/auth.ts"
    - "test/auth.test.ts exercises token lifecycle (withExpiry, isExpired), claims helpers (decodeIdTokenClaims, userLabelFromClaims), getStoredTokens, clearAllTokens, and refreshTokens happy path"
    - "npm test exits 0 with all new auth tests passing"
    - "npm run compile exits 0 after auth.ts modifications"
  artifacts:
    - path: "test/__mocks__/vscode.ts"
      provides: "Shared vscode mock for all Phase 11 test plans"
      exports: ["EventEmitter", "Disposable", "Uri", "FileSystemError", "FileType", "QuickPickItemKind", "window", "workspace", "commands", "env", "makeSecretStorage", "makeGlobalState", "makeExtensionContext"]
    - path: "test/auth.test.ts"
      provides: "Auth module unit tests"
      min_lines: 80
  key_links:
    - from: "test/auth.test.ts"
      to: "src/auth.ts"
      via: "import { decodeIdTokenClaims, userLabelFromClaims, withExpiry, isExpired, getStoredTokens, clearAllTokens, refreshTokens, onAuthStateChanged }"
      pattern: "from.*['\"]\\.\\.?/.*auth['\"]"
    - from: "test/auth.test.ts"
      to: "test/__mocks__/vscode.ts"
      via: "vitest alias resolution — import 'vscode' resolves to mock"
      pattern: "makeSecretStorage|makeExtensionContext"
---

<objective>
Build the shared VS Code mock that every Phase 11 plan depends on, then export four private helper functions from auth.ts and write the auth module unit tests.

Purpose: This plan is the prerequisite for all other Phase 11 plans. Without a functional test/__mocks__/vscode.ts, tests in other plans cannot import the `vscode` module. Without exporting the private auth helpers, the auth contract tests cannot be written.

Output:
- test/__mocks__/vscode.ts — comprehensive shared mock with factory functions (D-01, D-03)
- src/auth.ts — four helpers promoted to exported (D-04, D-05)
- test/auth.test.ts — auth module test suite covering token lifecycle, claims parsing, store read/clear, and refresh flow
</objective>

<execution_context>
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-unit-tests-and-code-organisation/11-CONTEXT.md
@.planning/phases/11-unit-tests-and-code-organisation/11-RESEARCH.md
@vitest.config.ts
@test/__mocks__/vscode.ts
@src/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build test/__mocks__/vscode.ts comprehensive shared mock</name>
  <files>test/__mocks__/vscode.ts</files>
  <read_first>
    - test/__mocks__/vscode.ts (current empty file — understand baseline)
    - vitest.config.ts (confirm alias: vscode → test/__mocks__/vscode.ts)
    - .planning/phases/11-unit-tests-and-code-organisation/11-RESEARCH.md §Code Examples (complete mock skeleton to use verbatim)
  </read_first>
  <action>
    Replace the current `export {}` stub with the full mock skeleton from RESEARCH.md §Code Examples section. The mock must export:

    Classes: `EventEmitter<T>` (with `listeners` array, `fire(event)`, `get event()` returning subscription function, `dispose()`), `Disposable` (constructor takes `() => void`, has `dispose()` method), `FileSystemError extends Error` (with `static FileNotFound`, `static Unavailable`, `static FileNotADirectory`, `static NoPermissions` factory methods, and `public code: string` property).

    Namespace objects: `Uri` (with `from({scheme, path, authority?, query?, fragment?})` returning plain object with all URI fields + `fsPath` and `toString()`, `parse(value)` splitting on first colon, `file(path)` with `scheme:'file'`), `window` (with `showErrorMessage`, `showInformationMessage`, `showWarningMessage`, `showQuickPick`, `createQuickPick`, `setStatusBarMessage` — all `vi.fn()`), `workspace` (with `getConfiguration` returning `{get: vi.fn(() => undefined)}`, `onDidSaveTextDocument: vi.fn(() => new Disposable(() => {}))`, `fs: {writeFile: vi.fn()}`), `commands` (with `executeCommand: vi.fn()`), `env` (with `openExternal: vi.fn()`).

    Enums: `FileType` (Unknown=0, File=1, Directory=2, SymbolicLink=64), `FileChangeType` (Changed=1, Created=2, Deleted=3), `QuickPickItemKind` (Separator=-1, Default=0).

    Factory functions (D-03): `makeSecretStorage()` returns object with Map-backed `get/store/delete` (all `vi.fn` wrapping the Map) and `onDidChange` from `new EventEmitter<any>().event`. `makeGlobalState()` returns object with Map-backed `get/update/keys/setKeysForSync` (all `vi.fn`). `makeExtensionContext(overrides?)` returns `{ secrets: makeSecretStorage(), globalState: makeGlobalState(), asAbsolutePath: vi.fn((rel) => '/mock/extension/' + rel), subscriptions: [], ...overrides } as any`.

    The file must `import { vi } from 'vitest'` at the top. `Uri.from` must preserve raw path characters without percent-encoding (Pitfall 3 from RESEARCH.md). `FileSystemError.FileNotFound` must return `new FileSystemError(...)` with `.code = 'FileNotFound'` (Pitfall 4).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>test/__mocks__/vscode.ts exports EventEmitter, Disposable, Uri, FileSystemError, FileType, window, workspace, commands, env, makeSecretStorage, makeGlobalState, makeExtensionContext; npm test passes (--passWithNoTests)</done>
</task>

<task type="auto">
  <name>Task 2: Export private auth helpers from src/auth.ts</name>
  <files>src/auth.ts</files>
  <read_first>
    - src/auth.ts (full file — locate decodeIdTokenClaims at line 125, userLabelFromClaims at line 146, withExpiry at line 213, isExpired at line 558)
    - .planning/phases/11-unit-tests-and-code-organisation/11-RESEARCH.md §Extraction Requirements Summary
  </read_first>
  <action>
    Add the `export` keyword to the four private functions in src/auth.ts (D-05):
    - `function decodeIdTokenClaims(` → `export function decodeIdTokenClaims(`
    - `function userLabelFromClaims(` → `export function userLabelFromClaims(`
    - `function withExpiry(` → `export function withExpiry(`
    - `function isExpired(` → `export function isExpired(`

    No other changes. Do not move the functions to a separate file — they stay in auth.ts (per RESEARCH.md recommendation: "closely coupled to TokenSet"). Do not modify function bodies or signatures. Do not change any other visibility modifiers.
  </action>
  <verify>
    <automated>npm run compile</automated>
  </verify>
  <done>npm run compile exits 0; grep confirms `export function decodeIdTokenClaims`, `export function userLabelFromClaims`, `export function withExpiry`, `export function isExpired` all appear in src/auth.ts</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write test/auth.test.ts</name>
  <files>test/auth.test.ts</files>
  <read_first>
    - src/auth.ts (full file — understand TokenSet interface, SECRET_TOKENS constant at line 98, SECRET_WS_TOKEN_PREFIX at line 99, GLOBAL_WS_TOKEN_INDEX_KEY at line 100, getStoredTokens at line 509, clearAllTokens at line 516, refreshTokens at line 475)
    - test/__mocks__/vscode.ts (factory function signatures)
    - test/dedupLogPage.test.ts (established test structure pattern)
    - .planning/phases/11-unit-tests-and-code-organisation/11-RESEARCH.md §Plan 11-01 Auth Module
  </read_first>
  <behavior>
    Pure helper tests (no mock needed):
    - decodeIdTokenClaims(undefined) → undefined
    - decodeIdTokenClaims('not.a.jwt') (2 parts) → undefined
    - decodeIdTokenClaims(valid 3-part JWT with base64url payload '{"sub":"user1"}') → {sub:'user1'}
    - decodeIdTokenClaims(JWT with malformed JSON payload) → undefined
    - userLabelFromClaims(undefined) → '(signed in)'
    - userLabelFromClaims({preferred_username:'alice'}) → 'alice'
    - userLabelFromClaims({email:'b@example.com'}) → 'b@example.com'
    - userLabelFromClaims({name:'Bob'}) → 'Bob'
    - userLabelFromClaims({}) → '(signed in)'
    - withExpiry({access_token:'t', expires_in:3600}) → result.expires_at ≈ Math.floor(Date.now()/1000)+3570 (±5s)
    - withExpiry({access_token:'t', expires_in:3600, expires_at:999}) → expires_at unchanged (still 999)
    - isExpired({access_token:'t'}) → false (no expires_at)
    - isExpired({access_token:'t', expires_at: Math.floor(Date.now()/1000)+100}) → false (future)
    - isExpired({access_token:'t', expires_at: Math.floor(Date.now()/1000)-1}) → true (past)

    Mock-based tests (using makeSecretStorage/makeExtensionContext):
    - getStoredTokens(ctx) with empty secrets → undefined
    - getStoredTokens(ctx) with valid JSON stored at 'altium365.tokens' → parsed TokenSet
    - clearAllTokens(ctx) with no stored tokens → fires onAuthStateChanged with {signedIn:false}; dispose listener in afterEach
    - clearAllTokens(ctx, {silent:true}) with stored tokens → does NOT fire onAuthStateChanged
    - clearAllTokens(ctx) with workspace token index → clears per-workspace entries at 'altium365.workspaceTokens.<id>'

    SKIPPED (document in a comment block at bottom of file, per D-08):
    - signIn() — requires vscode.env.openExternal + Node https mock
    - pollActionWait() — requires Node http/https mechanics
    - readOAuthConfig() — low value config key accessor
  </behavior>
  <action>
    Create test/auth.test.ts. Import test functions with `import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest'` and module imports from `'../src/auth'`. Use `import { makeSecretStorage, makeExtensionContext } from './__mocks__/vscode'` for factory instances. Use `vi.stubGlobal('fetch', vi.fn())` before refresh tests and `afterEach(() => vi.unstubAllGlobals())` (Pitfall 5 from RESEARCH.md).

    For the onAuthStateChanged event tests: subscribe with `const d = onAuthStateChanged(handler)` and call `d.dispose()` in afterEach (Pitfall 1). For clearAllTokens test, pre-seed the secrets store by calling `await ctx.secrets.store('altium365.tokens', JSON.stringify({access_token:'at',refresh_token:'rt'}))` before the function call.

    For refreshTokens test: mock fetch to return a 200 response with `{ok:true, text: async () => JSON.stringify({access_token:'new_at', expires_in:3600})}`. Confirm that `getStoredTokens(ctx)` returns the new token after the call.
  </action>
  <verify>
    <automated>npx vitest run test/auth.test.ts</automated>
  </verify>
  <done>npx vitest run test/auth.test.ts exits 0 with all describe blocks passing; skipped functions documented in a SKIPPED comment block at the bottom of the file</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → src | Test files import and call production functions directly; no new network or I/O boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-SC | Tampering | npm installs | accept | No new packages installed in this phase (vitest already present) |
</threat_model>

<verification>
- npm test exits 0 (all test files pass)
- npm run compile exits 0 (no TypeScript errors from auth.ts export additions)
- grep confirms `export function decodeIdTokenClaims` in src/auth.ts
- grep confirms `export function isExpired` in src/auth.ts
- test/auth.test.ts exists and contains at least 15 test cases
</verification>

<success_criteria>
- test/__mocks__/vscode.ts exports the full mock surface including factory functions (D-01, D-03)
- src/auth.ts exports decodeIdTokenClaims, userLabelFromClaims, withExpiry, isExpired (D-04, D-05)
- test/auth.test.ts covers: JWT decode/claims parsing, expiry logic, getStoredTokens, clearAllTokens (with and without silent flag), refreshTokens happy path with fetch mock
- npm test passes with all new tests
- Skipped UI-heavy functions are documented with a comment block (D-08)
</success_criteria>

<output>
Create `.planning/phases/11-unit-tests-and-code-organisation/11-01-SUMMARY.md` when done
</output>
