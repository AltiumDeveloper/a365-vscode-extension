---
phase: "11"
plan: "05"
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - test/localScriptCache.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "test/localScriptCache.test.ts tests normalizeLocalScriptKey, registerLocalScript, getLocalScript, findLocalScriptByRemoteId"
    - "npm test exits 0 with the new test file passing"
  artifacts:
    - path: "test/localScriptCache.test.ts"
      provides: "Unit tests for localScriptCache in-memory registry and path normalization"
      min_lines: 50
  key_links:
    - from: "test/localScriptCache.test.ts"
      to: "src/localScriptCache.ts"
      via: "import { normalizeLocalScriptKey, registerLocalScript, getLocalScript, findLocalScriptByRemoteId }"
      pattern: "from.*localScriptCache"
---

<objective>
Write unit tests for the localScriptCache module — specifically the in-memory registry operations (register/get/find) and path normalization fallback for non-existent paths.

Purpose: The local script cache is the lookup table that maps local file paths to remote script identities. Registry correctness (case-insensitive lookup on macOS, scriptId lookup, miss on unregistered) is essential for test event resolution working correctly for local scripts.

Output:
- test/localScriptCache.test.ts — tests for normalizeLocalScriptKey fallback, registerLocalScript, getLocalScript (hit/miss/case), findLocalScriptByRemoteId
</objective>

<execution_context>
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-unit-tests-and-code-organisation/11-CONTEXT.md
@.planning/phases/11-unit-tests-and-code-organisation/11-RESEARCH.md
@src/localScriptCache.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write test/localScriptCache.test.ts</name>
  <files>test/localScriptCache.test.ts</files>
  <read_first>
    - src/localScriptCache.ts (full file — normalizeLocalScriptKey at line 43 with realpathSync+catch fallback, registerLocalScript at line 59, getLocalScript at line 67, findLocalScriptByRemoteId at line 79, LocalScriptIdentity interface at line 31)
    - .planning/phases/11-unit-tests-and-code-organisation/11-RESEARCH.md §Plan 11-05 and Pitfall 2
  </read_first>
  <behavior>
    normalizeLocalScriptKey tests (no mock needed — non-existent paths use fallback per RESEARCH Pitfall 2):
    - normalizeLocalScriptKey('/nonexistent/path/script.py') → returns '/nonexistent/path/script.py' or lowercased variant (does not throw; catch block returns original)
    - normalizeLocalScriptKey('/SOME/UPPER/PATH.PY') → on any platform, does not throw; on darwin/win32 returns lowercased value; on linux returns as-is

    Registry operation tests (use beforeEach to clear state):
    Note: the registry is module-level Map — tests must NOT rely on a fresh module between tests; use unique paths per test or call a clear mechanism if one exists.
    - registerLocalScript('/tmp/test-cache-11-05-a.py', {scriptId:'uuid-a', workspaceAuthId:'ws-a'}), then getLocalScript(normalizeLocalScriptKey('/tmp/test-cache-11-05-a.py')) → {scriptId:'uuid-a', workspaceAuthId:'ws-a'}
    - getLocalScript('/nonexistent-11-05-b.py') → undefined (not registered)
    - findLocalScriptByRemoteId('uuid-a') after registering '/tmp/test-cache-11-05-a.py' → returns entry with scriptId:'uuid-a'
    - findLocalScriptByRemoteId('not-registered-uuid') → undefined

    SKIPPED (document in a comment block per D-08):
    - rehydrateLocalScriptCacheFromDisk() — reads readdirSync from tmpdir; skip without a stable tmpdir fixture
    - registerLocalScriptSaveBridge() — requires vscode.workspace.onDidSaveTextDocument (D-08)
  </behavior>
  <action>
    Create test/localScriptCache.test.ts. Import `import { describe, it, expect } from 'vitest'` and `import { normalizeLocalScriptKey, registerLocalScript, getLocalScript, findLocalScriptByRemoteId } from '../src/localScriptCache'`.

    Since the cache is a module-level Map, use unique path strings per test (e.g. '/tmp/test-localcache-11-05-<N>.py') to avoid test interference. The normalizeLocalScriptKey fallback test: pass a path string that does not exist on any CI machine (e.g. '/does-not-exist-11-05-test-path/file.py') — the catch block returns the original or lowercased version, never throws.

    For platform-conditional behavior in normalizeLocalScriptKey: wrap in `if (process.platform === 'darwin' || process.platform === 'win32')` checks inside the test assertion, or use `expect(result).toEqual(result.toLowerCase())` only on those platforms with a conditional skip on linux using `it.skipIf(process.platform === 'linux')`.
  </action>
  <verify>
    <automated>npx vitest run test/localScriptCache.test.ts</automated>
  </verify>
  <done>npx vitest run test/localScriptCache.test.ts exits 0; normalizeLocalScriptKey, registerLocalScript, getLocalScript, and findLocalScriptByRemoteId cases pass; SKIPPED comment block present for rehydrate and saveBridge</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → src | Registry tests only; realpathSync fallback path confirmed for non-existent paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-SC | Tampering | npm installs | accept | No new packages installed |
</threat_model>

<verification>
- npx vitest run test/localScriptCache.test.ts exits 0
- No source file changes (all localScriptCache.ts functions are already exported)
- normalizeLocalScriptKey non-existent path test does not throw on any platform
</verification>

<success_criteria>
- test/localScriptCache.test.ts covers: normalizeLocalScriptKey fallback, registerLocalScript + getLocalScript hit, getLocalScript miss, findLocalScriptByRemoteId hit and miss
- Platform-conditional case-sensitivity behavior handled in tests
- Skipped functions documented (D-08)
</success_criteria>

<output>
Create `.planning/phases/11-unit-tests-and-code-organisation/11-05-SUMMARY.md` when done
</output>
