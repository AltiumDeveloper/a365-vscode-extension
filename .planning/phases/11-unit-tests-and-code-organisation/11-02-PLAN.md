---
phase: "11"
plan: "02"
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/testEvents/picker.ts
  - test/picker.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "buildPickerItems is extracted as an exported function from src/testEvents/picker.ts"
    - "previewBody is exported from src/testEvents/picker.ts"
    - "test/picker.test.ts tests buildPickerItems with multiple store shapes and previewBody truncation logic"
    - "npm test exits 0"
    - "npm run compile exits 0 after picker.ts changes"
  artifacts:
    - path: "test/picker.test.ts"
      provides: "Unit tests for picker helper functions"
      min_lines: 50
  key_links:
    - from: "test/picker.test.ts"
      to: "src/testEvents/picker.ts"
      via: "import { buildPickerItems, previewBody }"
      pattern: "from.*picker"
---

<objective>
Extract and export two pure helper functions from src/testEvents/picker.ts, then write unit tests for the item-building and preview-truncation logic.

Purpose: The pickTestEvent command handler is VS Code-QuickPick-heavy and skipped per D-08. Its pure inner logic (what items to show, how to truncate preview text) is fully testable without any VS Code mock. Extract that logic first, then verify the contracts.

Output:
- src/testEvents/picker.ts — buildPickerItems extracted and exported; previewBody exported (D-04, D-05)
- test/picker.test.ts — tests for item construction and preview truncation across store shapes
</objective>

<execution_context>
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-unit-tests-and-code-organisation/11-CONTEXT.md
@.planning/phases/11-unit-tests-and-code-organisation/11-RESEARCH.md
@src/testEvents/picker.ts
@test/__mocks__/vscode.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract buildPickerItems and export previewBody from picker.ts</name>
  <files>src/testEvents/picker.ts</files>
  <read_first>
    - src/testEvents/picker.ts (full file — understand PickerItem type, existing items[] construction loop inside pickTestEvent starting around line 60, and previewBody function at line 149)
    - src/testEvents/store.ts (TestEventStore interface — events Record<string, Record<string, unknown>>, defaultEventName string)
    - .planning/phases/11-unit-tests-and-code-organisation/11-RESEARCH.md §Plan 11-02 picker.ts
  </read_first>
  <action>
    Make two changes to src/testEvents/picker.ts (D-05):

    1. Export `previewBody`: change `function previewBody(` to `export function previewBody(`. No other changes to the function body.

    2. Extract `buildPickerItems`: identify the `items: PickerItem[] = []` construction block inside `pickTestEvent` (the loop that pushes items with labels, descriptions, detail fields, and the "Create new event" separator + action items). Move this logic into a new exported function:
       `export function buildPickerItems(store: TestEventStore | undefined, options: { eventsOnly?: boolean }): PickerItem[]`
       The function returns the same array that was previously assembled inline. Replace the inline block in `pickTestEvent` with a call to `buildPickerItems(store, options)`. Keep the existing `PickerItem` type and all imports unchanged.

    After extraction, pickTestEvent must behave identically — only the item-building is moved out. The QuickPick creation and resolution logic stays inside pickTestEvent.
  </action>
  <verify>
    <automated>npm run compile</automated>
  </verify>
  <done>npm run compile exits 0; grep confirms `export function buildPickerItems` and `export function previewBody` in src/testEvents/picker.ts</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write test/picker.test.ts</name>
  <files>test/picker.test.ts</files>
  <read_first>
    - src/testEvents/picker.ts (after Task 1 — inspect buildPickerItems signature and previewBody signature)
    - src/testEvents/store.ts (TestEventStore type for constructing test inputs)
    - test/__mocks__/vscode.ts (QuickPickItemKind enum — needed for separator item assertions)
  </read_first>
  <behavior>
    previewBody tests (pure, no mock needed):
    - previewBody({}) → string, length ≤ 80
    - previewBody({key:'value'}) → contains 'value'
    - previewBody with a large JSON object (> 80 chars serialized) → result length ≤ 80 and ends with '…'
    - previewBody with unserializable value (circular reference or BigInt) → returns some fallback string (no throw)

    buildPickerItems tests with various store shapes:
    - buildPickerItems(undefined, {}) → array containing at least a 'Create new event' item
    - buildPickerItems({defaultEventName:'', events:{}}, {}) → array with 'Create new event' present
    - buildPickerItems({defaultEventName:'run1', events:{run1:{projectId:'p1'}}}, {}) → array contains item with label 'run1'; item detail contains 'p1' (from previewBody)
    - buildPickerItems({defaultEventName:'run1', events:{run1:{}, run2:{}}}, {}) → item for 'run1' has some default indicator (description or label prefix); 'run2' item also present
    - buildPickerItems(store, {eventsOnly:true}) → no 'Create new event' item present (separator/action items excluded)
    - buildPickerItems with store of 3 events → returns array length ≥ 3 (one per event, plus action items unless eventsOnly)

    SKIPPED (document in a comment block per D-08):
    - pickTestEvent() full QuickPick flow — requires createQuickPick + event subscriptions
  </behavior>
  <action>
    Create test/picker.test.ts. Import with `import { describe, it, expect } from 'vitest'` and `import { buildPickerItems, previewBody } from '../src/testEvents/picker'`. No vscode factory mock needed for these pure-function tests — both functions only use vscode types structurally, not functionally.

    For the circular-reference previewBody test, construct `const obj: any = {}; obj.self = obj` and pass it. Expect the result to be a non-empty string with no throw.

    For buildPickerItems, construct TestEventStore objects inline — `{ defaultEventName: 'run1', events: { run1: { projectId: 'p1' } } }`. Assert on `label` and `detail` fields of returned PickerItem objects. Use `Array.isArray(items)` and `.some(i => i.label?.includes('Create new'))` style assertions to avoid brittleness on exact label strings.
  </action>
  <verify>
    <automated>npx vitest run test/picker.test.ts</automated>
  </verify>
  <done>npx vitest run test/picker.test.ts exits 0; all buildPickerItems and previewBody cases pass; SKIPPED comment block present for pickTestEvent full flow</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → src | Test files import production functions directly; no new I/O or network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-SC | Tampering | npm installs | accept | No new packages installed |
</threat_model>

<verification>
- npm run compile exits 0
- npx vitest run test/picker.test.ts exits 0
- grep confirms `export function buildPickerItems` in src/testEvents/picker.ts
- grep confirms `export function previewBody` in src/testEvents/picker.ts
</verification>

<success_criteria>
- buildPickerItems extracted and exported; pickTestEvent calls it (D-05)
- previewBody exported (D-05)
- test/picker.test.ts covers empty store, single event, multi-event, default indicator, eventsOnly mode, and previewBody truncation
- Skipped QuickPick flow documented (D-08)
</success_criteria>

<output>
Create `.planning/phases/11-unit-tests-and-code-organisation/11-02-SUMMARY.md` when done
</output>
