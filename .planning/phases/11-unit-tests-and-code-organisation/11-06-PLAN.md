---
phase: "11"
plan: "06"
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - test/workspace.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "test/workspace.test.ts tests getWorkspaceApiUrl (3 input shapes), getWorkspaceFilesUrl (3 input shapes including throw), GraphQLError constructor, graphqlRequest (200+data, 200+errors, 4xx, non-JSON), and checkAppInstalled"
    - "fetch is mocked with vi.stubGlobal per test and restored with vi.unstubAllGlobals in afterEach"
    - "npm test exits 0"
  artifacts:
    - path: "test/workspace.test.ts"
      provides: "Unit tests for workspace.ts pure functions and graphqlRequest"
      min_lines: 90
  key_links:
    - from: "test/workspace.test.ts"
      to: "src/workspace.ts"
      via: "import { getWorkspaceApiUrl, getWorkspaceFilesUrl, GraphQLError, graphqlRequest, checkAppInstalled }"
      pattern: "from.*workspace"
---

<objective>
Write unit tests for workspace.ts covering the two pure URL helper functions, the GraphQLError class, graphqlRequest with a global fetch mock, and checkAppInstalled.

Purpose: graphqlRequest is the gateway for all Altium 365 API calls. Verifying its error-mapping (GraphQL errors array → GraphQLError, HTTP non-2xx → plain Error, non-JSON body → Error) protects every higher-level function that uses it. The URL helpers protect the cluster-routing logic (D-19) from regression.

Output:
- test/workspace.test.ts — URL helpers, GraphQLError, graphqlRequest with fetch mock, checkAppInstalled
</objective>

<execution_context>
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-unit-tests-and-code-organisation/11-CONTEXT.md
@.planning/phases/11-unit-tests-and-code-organisation/11-RESEARCH.md
@src/workspace.ts
@test/__mocks__/vscode.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write test/workspace.test.ts — URL helpers, GraphQLError, graphqlRequest</name>
  <files>test/workspace.test.ts</files>
  <read_first>
    - src/workspace.ts lines 1–163 (getWorkspaceApiUrl at line 46, getWorkspaceFilesUrl at line 69, GraphQLError class at line 93, graphqlRequest at line 114 — read to understand the full error-mapping logic)
    - .planning/phases/11-unit-tests-and-code-organisation/11-RESEARCH.md §Plan 11-06 and Pattern 3 (fetch mock)
  </read_first>
  <behavior>
    getWorkspaceApiUrl tests (pure, no mock):
    - getWorkspaceApiUrl(undefined, 'https://global.api') → 'https://global.api'
    - getWorkspaceApiUrl({name:'ws', workspaceId:'', authId:'', location:{apiServiceUrl:'https://ws.api'}}, 'https://global.api') → 'https://ws.api'
    - getWorkspaceApiUrl({name:'ws', workspaceId:'', authId:'', location:{apiServiceUrl:'  '}}, 'https://global.api') → 'https://global.api' (whitespace-only treated as empty)
    - getWorkspaceApiUrl({name:'ws', workspaceId:'', authId:''}, 'https://global.api') → 'https://global.api' (no location property)

    getWorkspaceFilesUrl tests (pure, no mock):
    - getWorkspaceFilesUrl(undefined, 'https://files.fallback') → 'https://files.fallback'
    - getWorkspaceFilesUrl({name:'ws', workspaceId:'', authId:'', location:{filesServiceUrl:'https://ws.files'}}) → 'https://ws.files'
    - getWorkspaceFilesUrl(undefined, undefined) → throws Error matching 'filesServiceUrl unavailable'
    - getWorkspaceFilesUrl(undefined, '') → throws Error matching 'filesServiceUrl unavailable'

    GraphQLError tests (pure, no mock):
    - new GraphQLError('msg', {rawErrors:[]}).message === 'msg'
    - new GraphQLError('msg', {code:'NOT_FOUND', rawErrors:[]}).code === 'NOT_FOUND'
    - new GraphQLError('msg', {rawErrors:[]}).name === 'GraphQLError'
    - err instanceof GraphQLError is true
    - err instanceof Error is true (inheritance chain)

    graphqlRequest tests (with fetch mock via vi.stubGlobal):
    - 200 response with {data:{items:['a']}} → returns {items:['a']}
    - 200 response with {errors:[{message:'Not found', extensions:{code:'NOT_FOUND'}}]} → throws GraphQLError with message containing 'Not found' and code 'NOT_FOUND'
    - 200 response with {errors:[{message:'oops'}], data:{}} → throws GraphQLError (errors array takes precedence)
    - fetch returns response with ok:false, status:403, text:'Forbidden' → throws plain Error with '403' in message
    - fetch returns response with ok:true, text returning non-JSON → throws Error
    - response errors array contains multiple errors → GraphQLError message comes from first error's message

    checkAppInstalled tests (with fetch mock):
    - graphqlRequest returns {gloAppInstalledApps:[{id:'app-1'},{id:'app-2'}]} → checkAppInstalled('ep','tok','app-1') returns true
    - graphqlRequest returns {gloAppInstalledApps:[{id:'app-2'}]} → checkAppInstalled returns false for 'app-1'
    - graphqlRequest returns {gloAppInstalledApps:[]} → checkAppInstalled returns false
    - graphqlRequest returns {gloAppInstalledApps: 'not-an-array'} → checkAppInstalled returns false

    SKIPPED (document in a comment block per D-08):
    - listWorkspaces/listProjects/listScripts/listExtensionPoints — require full auth token + vscode context chain
    - pickWorkspace — requires vscode.window.showQuickPick
  </behavior>
  <action>
    Create test/workspace.test.ts. Import `import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest'` and `import { getWorkspaceApiUrl, getWorkspaceFilesUrl, GraphQLError, graphqlRequest, checkAppInstalled } from '../src/workspace'`.

    Per Pitfall 5 from RESEARCH.md: add `afterEach(() => vi.unstubAllGlobals())` at the top-level suite level to ensure fetch stubs don't bleed between test cases.

    For each fetch-mocked test: call `vi.stubGlobal('fetch', vi.fn())` then configure the mock with `.mockResolvedValueOnce({ok:true, text: async () => JSON.stringify({data:{...}})})`. Use this pattern from RESEARCH.md Pattern 3.

    For graphqlRequest non-JSON test: mock `text: async () => 'this is not json'` with `ok:true`.
    For HTTP 4xx test: mock `{ok:false, status:403, text: async () => 'Forbidden'}`.

    For checkAppInstalled, mock graphqlRequest indirectly by mocking fetch at the global level (same mechanism — checkAppInstalled calls graphqlRequest which calls fetch). The mock response must be `{ok:true, text: async () => JSON.stringify({data:{gloAppInstalledApps:[{id:'app-1'}]}})}`.

    For WorkspaceInfo construction in URL helper tests, use minimal inline objects satisfying the WorkspaceInfo type — only populate the fields relevant to each test case.
  </action>
  <verify>
    <automated>npx vitest run test/workspace.test.ts</automated>
  </verify>
  <done>npx vitest run test/workspace.test.ts exits 0; all URL helper, GraphQLError, graphqlRequest, and checkAppInstalled cases pass; SKIPPED comment block present</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → network | fetch is stubbed globally per test; no real network calls made |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06-01 | Information Disclosure | graphqlRequest error mapping | mitigate (tested) | errors[] array handling and non-JSON body tests verify no silent failures |
| T-11-06-SC | Tampering | npm installs | accept | No new packages installed |
</threat_model>

<verification>
- npx vitest run test/workspace.test.ts exits 0
- No source file changes needed (all workspace.ts test targets already exported)
- afterEach vi.unstubAllGlobals() present in test file to prevent fetch bleed (Pitfall 5)
</verification>

<success_criteria>
- test/workspace.test.ts covers: getWorkspaceApiUrl (4 shapes), getWorkspaceFilesUrl (4 shapes including throw), GraphQLError class contract, graphqlRequest with 6 response shapes, checkAppInstalled (4 shapes)
- fetch global mock used with vi.stubGlobal and restored with vi.unstubAllGlobals in afterEach
- Skipped UI-heavy functions documented (D-08)
</success_criteria>

<output>
Create `.planning/phases/11-unit-tests-and-code-organisation/11-06-SUMMARY.md` when done
</output>
