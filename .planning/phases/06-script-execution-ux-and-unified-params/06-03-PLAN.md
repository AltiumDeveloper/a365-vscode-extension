---
phase: 06-script-execution-ux-and-unified-params
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/projectPicker.ts
  - src/extension.ts
  - src/scriptCommands.ts
  - src/remoteExecution.ts
autonomous: false
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-21, D-22, D-23, D-24]

must_haves:
  truths:
    - "[SC-1] Right-clicking a script in workspace B while workspace A is active and invoking 'Run Script (Local)' or 'Debug Script (Local)' executes the Python subprocess with workspace B's token and workspace B's apiServiceUrl; the active workspace selection in the tree remains A throughout (no implicit switch). (D-01, D-02)"
    - "[SC-1] The OutputChannel header for the local run/debug shows the script's owning workspace name, NOT '<workspace-name unknown>' (cross-workspace plumbing fixed per RESEARCH §5.4). Same fix applies to remote execute's header."
    - "[SC-1] Palette-invoked `altium365.runScript` / `altium365.debugScript` on a standalone `.py` (no node arg, fsPath not in `localScriptCache`) falls back to the active workspace token + active workspace's apiServiceUrl per D-03; if no active workspace, the existing 'select workspace first' prompt fires unchanged."
    - "[SC-2] Debug-local on a remote script → edit the opened tmp tab → Cmd+S publishes back to the remote script via the existing save bridge (verified by UAT per D-04; no code change required — defensive plan added only if the UAT repro fails)."
    - "[SC-3] With `altium365.promptForProjectId: true`, invoking Execute Remotely on a script that has no entry under `altium365.scriptParams.<scriptId>` prompts via the shared `pickProjectId` QuickPick and sends the chosen value as `parameters: [{ key: 'projectId', value: <picked> }]`. With the config `false`, remote execute sends no parameters (current behavior preserved). (D-05, D-06)"
    - "[SC-3] The `pickProjectId` QuickPick (whether invoked from local Run/Debug or from remote Execute) pre-fills its initial selection from the shared workspaceState key `altium365.lastProjectId.<workspaceId>`, and updates that same key after a successful pick — local↔remote cache is symmetric per D-07."
    - "[D-08] `pickProjectId` accepts an optional `target?: WorkspaceInfo` parameter; when set, the placeholder/loading copy names the target workspace (NOT the active one) and the project list is loaded against the target's `apiServiceUrl` + workspace-scoped token supplied by the caller."
    - "[D-21] No new GraphQL queries, no auth-surface changes, no new package.json `contributes.configuration` keys are introduced; the existing `altium365.promptForProjectId` config is the only knob consulted."
    - "[D-22] The new `pickProjectId` call inside `executeRemoteScript` Block B sits INSIDE the existing `withScriptProgress` wrapper that already covers Block A+B (no nested wrap added)."
    - "[D-23] The reserved workspaceState key `altium365.scriptParams.<scriptId>` remains read-only via the existing `resolveScriptParameters` helper; this plan does NOT write to it (Phase 999.3 owns activation)."
    - "[D-24] UAT scenarios for cross-workspace routing, debug-save-back, and unified projectId prompting are executed end-to-end as part of Task 3 below."
  artifacts:
    - path: "src/projectPicker.ts"
      provides: "Exported `pickProjectId(context, endpoint, accessToken, last, target?, output?)` extracted verbatim from `extension.ts:773-852` with new optional `target?: WorkspaceInfo` (D-08) and optional `output?: vscode.OutputChannel` (so callers from `remoteExecution.ts` can route logging through their own channel)"
      min_lines: 40
      exports: ["pickProjectId"]
    - path: "src/extension.ts"
      provides: "`runScriptAtPath` / `debugScriptAtPath` / `prepareRun` accept optional `target?: { workspaceId: string; workspaceAuthId: string }`; when set, token comes from `ensureWorkspaceToken({workspaceId, authId})` (NOT `getActiveAccessToken`), endpoint comes from `getWorkspaceApiUrl(resolvedWs, envGlobalEndpoint)` where `resolvedWs` is found via `getSelectedWorkspace ?? listWorkspaces(...).find` (mirror of `remoteExecution.ts:99-127`); OutputChannel header includes resolved workspace name. The inline `pickProjectId` function definition is removed in favor of `import { pickProjectId } from './projectPicker';`"
    - path: "src/scriptCommands.ts"
      provides: "`runLocalFromScriptNode` and `debugLocalFromScriptNode` resolve the script's workspace identity (`workspaceId`, `workspaceAuthId`) from the node and pass it as the new `target` arg to `runScriptAtPath` / `debugScriptAtPath`"
    - path: "src/remoteExecution.ts"
      provides: "Block A overwrites `args.workspaceName` from the resolved `ws.name` so cross-workspace headers no longer print '<workspace-name unknown>' (RESEARCH §5.4); Block B adds a projectId-prompt fallback that runs ONLY when `resolveScriptParameters` returns undefined AND `altium365.promptForProjectId` is true, calling `pickProjectId(args.context, apiUrl, wsToken, last, ws, args.output)`, reading/writing `globalState['altium365.lastProjectId.<workspaceId>']` symmetric with local (D-06, D-07)"
  key_links:
    - from: "src/extension.ts prepareRun (with target)"
      to: "ensureWorkspaceToken({workspaceId, authId}) + getWorkspaceApiUrl(ws, envGlobalEndpoint)"
      via: "mirror of remoteExecution.ts:99-127 Block A workspace resolution"
      pattern: "ensureWorkspaceToken\\(.*workspaceId"
    - from: "src/remoteExecution.ts Block B"
      to: "pickProjectId imported from './projectPicker'"
      via: "fallback when resolveScriptParameters returns undefined and altium365.promptForProjectId is true"
      pattern: "pickProjectId\\("
    - from: "local prepareRun + remoteExecution Block B"
      to: "globalState key 'altium365.lastProjectId.<workspaceId>'"
      via: "shared read+write — D-07 symmetric cache"
      pattern: "altium365\\.lastProjectId\\."
---

<objective>
Make execution correctness coherent across local and remote, without touching the editor-title surface (Plan 2 owns that):

1. Local Run/Debug on a tree-node script always routes via the script's owning workspace token + apiServiceUrl, transparently — no active-workspace switch (D-01, D-02, SC-1). Standalone `.py` and palette invocations keep today's active-workspace fallback (D-03).
2. Fix the cross-workspace OutputChannel header that today prints `<workspace-name unknown>` for both local routing (post-D-02) and existing remote execute (RESEARCH §5.4).
3. Bring remote Execute Remotely up to local's parameter-prompt level by reusing `pickProjectId`, gated by `altium365.promptForProjectId`, against the script's workspace, with shared `altium365.lastProjectId.<workspaceId>` cache (D-05..D-08, SC-3).
4. Validate that save-back-after-debug already works (D-04, SC-2) — UAT-only, no code change planned; if repro fails the UAT step requests a defensive follow-up plan.

Purpose: SC-1, SC-2, SC-3 are the largest user-correctness wins of Phase 6. Sequenced after Plan 1 to avoid the §5.6 `scriptCommands.ts` merge risk; sequenced before Plan 2 so Plan 2's D-17 standalone-`.py` fallback can reuse the `target` plumbing introduced here.

Output: Four atomic commits (one per task; checkpoint is non-commit).
</objective>

<execution_context>
Phase mode is MVP. This slice's user story:

## Phase Goal (slice 3 — vertical user story)

**As a** developer with multiple A365 workspaces signed in, **I want to** click Run/Debug/Execute on a script that lives in a workspace I haven't activated and have the right thing happen, **so that** I don't get cryptic "workspace not found" errors, my active workspace selection isn't silently hijacked, and remote execute prompts me for a projectId the same way local already does.

@/Users/dmitrykolomiets/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-script-execution-ux-and-unified-params/06-CONTEXT.md
@.planning/phases/06-script-execution-ux-and-unified-params/06-RESEARCH.md
@.planning/phases/06-script-execution-ux-and-unified-params/06-PATTERNS.md
@.planning/phases/06-script-execution-ux-and-unified-params/06-01-SUMMARY.md
@AGENTS.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Reference implementation — copy this shape into prepareRun's `target` branch -->
<!-- From src/remoteExecution.ts:99-155 (Block A) -->

```typescript
let ws: WorkspaceInfo | undefined = getSelectedWorkspace(args.context);
if (!ws || ws.workspaceId !== args.workspaceId) {
    const baseToken = await getBaseAccessToken(args.context, cfg);
    if (!baseToken) { /* surface "not signed in" + return undefined */ }
    const list = await listWorkspaces(args.envGlobalEndpoint, baseToken);
    ws = list.find((w) => w.workspaceId === args.workspaceId);
}
if (!ws) { /* surface "workspace not found" + return undefined */ }

const wsToken = await ensureWorkspaceToken(args.context, cfg, {
    workspaceId: args.workspaceId,
    authId: args.workspaceAuthId || ws.authId,
});
const apiUrl = getWorkspaceApiUrl(ws, args.envGlobalEndpoint);
```

<!-- Current prepareRun snippet that must be generalized (extension.ts ~ :660-668) -->
```typescript
const endpoint = getWorkspaceApiUrl(getSelectedWorkspace(context), envGlobalEndpoint);
const oauthCfg = readOAuthConfig();
let token = await getActiveAccessToken(context, oauthCfg);
```

<!-- Current Block B (remoteExecution.ts ~:157-160) — D-06 insertion point -->
```typescript
const parameters = resolveScriptParameters(args.context, args.scriptId);
return { ws, wsToken, apiUrl, parameters };
```

<!-- New D-06 insertion (PATTERNS Pattern 1 for remoteExecution.ts) -->
```typescript
let parameters = resolveScriptParameters(args.context, args.scriptId);
if (parameters === undefined) {
    const wcfg = vscode.workspace.getConfiguration('altium365');
    if (wcfg.get<boolean>('promptForProjectId', true)) {
        const lastKey = `altium365.lastProjectId.${args.workspaceId}`;
        const last = args.context.globalState.get<string>(lastKey, '');
        const picked = await pickProjectId(args.context, apiUrl, wsToken, last, ws, args.output);
        if (picked === undefined) return undefined;     // user cancelled
        if (picked) {
            await args.context.globalState.update(lastKey, picked);
            parameters = [{ key: 'projectId', value: picked }];
        }
    }
}
return { ws, wsToken, apiUrl, parameters };
```

<!-- Current pickProjectId signature in extension.ts ~:773 — to extract -->
```typescript
async function pickProjectId(
    context: vscode.ExtensionContext,
    endpoint: string,
    accessToken: string,
    last: string,
): Promise<string | undefined>
```

<!-- Target signature in new src/projectPicker.ts (D-08) -->
```typescript
export async function pickProjectId(
    context: vscode.ExtensionContext,
    endpoint: string,
    accessToken: string,
    last: string,
    target?: WorkspaceInfo,                 // NEW (D-08)
    output?: vscode.OutputChannel,          // NEW (decouples from extension.ts singleton)
): Promise<string | undefined>;
```

<!-- Current local-side last-pick cache (extension.ts ~:715) — D-07 shared key -->
```typescript
const lastKey = `altium365.lastProjectId.${ws.workspaceId}`;
const last = context.globalState.get<string>(lastKey, '');
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract pickProjectId to src/projectPicker.ts with target?: WorkspaceInfo</name>
  <files>src/projectPicker.ts, src/extension.ts</files>
  <read_first>
    - src/extension.ts (FULL file — the inline `async function pickProjectId(...)` definition around lines 773-852 AND its single call site inside `prepareRun` around line 717; also the `outputChannel` module-level singleton and the imports at top)
    - src/workspace.ts (the `WorkspaceInfo` interface export, `listProjects(endpoint, token)` signature, and `getSelectedWorkspace(context)` signature)
    - .planning/phases/06-script-execution-ux-and-unified-params/06-CONTEXT.md (decision D-08)
    - .planning/phases/06-script-execution-ux-and-unified-params/06-RESEARCH.md (§2.6 — confirms pickProjectId has exactly ONE caller today; §5.8 — circular-import-risk rationale for extraction)
    - .planning/phases/06-script-execution-ux-and-unified-params/06-PATTERNS.md (`src/projectPicker.ts` section — verbatim extraction recipe + the line-780 placeholder-name change + the line-791-793 outputChannel decoupling note)
    - .planning/codebase/CONVENTIONS.md (file naming `camelCase.ts`, named exports only, no default exports, no barrel files, JSDoc on exported functions)
  </read_first>
  <action>
    Create new file `src/projectPicker.ts` containing the extracted `pickProjectId` function with the signature:
    `export async function pickProjectId(context: vscode.ExtensionContext, endpoint: string, accessToken: string, last: string, target?: import('./workspace').WorkspaceInfo, output?: vscode.OutputChannel): Promise<string | undefined>`.

    Copy the function body verbatim from `extension.ts` lines ~773-852 with exactly TWO behavioral changes:
    1. Replace the line that computes the workspace-name placeholder (currently `getSelectedWorkspace(context)?.name || '-'`) with `(target ?? getSelectedWorkspace(context))?.name || '-'` so the loading/placeholder copy names the target workspace when provided (D-08).
    2. The original body calls `outputChannel.appendLine(...)` in its catch block (referring to the `extension.ts` module-level singleton). Since the new module cannot import that singleton without creating a circular dependency, replace those calls with `output?.appendLine(...)` (no-op when the caller doesn't pass one). DO NOT add a fallback `console.error` — keep behavior identical to today for the existing caller (which today's catch path already silently falls through to the manual-input UI).

    Add a top-of-file JSDoc block on the exported function summarizing return semantics (mirrors existing JSDoc on the original) and noting "D-08 (Phase 6): accepts `target` for cross-workspace remote execute; extracted from extension.ts to break a circular import risk (remoteExecution → extension → remoteExecution chain)."

    In `src/extension.ts`:
    - Delete the now-extracted `pickProjectId` definition.
    - Add `import { pickProjectId } from './projectPicker';` at the top alongside other local imports (per CONVENTIONS import order).
    - Update the single call site inside `prepareRun` to pass FOUR positional args (no `target`, no `output` — preserves today's active-workspace behavior; `target` defaults to undefined → branch reads `getSelectedWorkspace(context)`).
    - Do NOT pass `outputChannel` from `prepareRun` for now — the existing call did not pipe it through and the catch-fallthrough behavior is preserved.

    Run `npm run compile` to confirm no remaining reference to the old definition and that `remoteExecution.ts`'s upcoming import resolves.

    Commit message: `refactor(06-03): extract pickProjectId to projectPicker.ts with target param (D-08)`.
  </action>
  <verify>
    <automated>npm run compile</automated>
    <automated>grep -c "async function pickProjectId" src/extension.ts | grep -E '^0$' || (echo "FAIL: pickProjectId still defined in extension.ts" && exit 1)</automated>
    <automated>grep -n "export async function pickProjectId" src/projectPicker.ts</automated>
    <automated>grep -n "import { pickProjectId } from './projectPicker'" src/extension.ts</automated>
    <automated>grep -nE "pickProjectId\(" src/extension.ts | grep -v '^#'</automated>
  </verify>
  <acceptance_criteria>
    - `npm run compile` exits 0.
    - `src/projectPicker.ts` exists and exports exactly one symbol `pickProjectId` whose signature accepts (in order): `context`, `endpoint`, `accessToken`, `last`, `target?`, `output?`.
    - `extension.ts` contains no `async function pickProjectId` definition (verifier #2 must report 0).
    - `extension.ts` contains exactly one import line `import { pickProjectId } from './projectPicker';`.
    - The single existing call site in `prepareRun` calls `pickProjectId(context, endpoint, token, last)` with FOUR positional args (no behavioral change to local).
    - Inside the new `pickProjectId`, the workspace-name placeholder line is `(target ?? getSelectedWorkspace(context))?.name || '-'` (verifiable by `grep -n "target ?? getSelectedWorkspace" src/projectPicker.ts`).
    - The catch block uses `output?.appendLine(...)` (no `outputChannel` reference); `grep -c "outputChannel" src/projectPicker.ts` returns 0.
    - JSDoc block on the exported function mentions "D-08" and "circular import".
  </acceptance_criteria>
  <done>
    Foundation laid: `pickProjectId` is importable from `projectPicker.ts` by any module without coupling to `extension.ts`; local behavior preserved bit-for-bit. Commit made.
  </done>
</task>

<task type="auto">
  <name>Task 2: Plumb workspace target through prepareRun / runScriptAtPath / debugScriptAtPath + fix OutputChannel header workspace name</name>
  <files>src/extension.ts, src/scriptCommands.ts</files>
  <read_first>
    - src/extension.ts (FULL file — focus on the exported `runScriptAtPath` (~:533), `debugScriptAtPath` (~:580ish), `prepareRun` (~:634) including its endpoint/token resolution at ~:660-668, the OutputChannel header lines at ~:544-545, the palette command handlers `altium365.runScript` (~:514) and `altium365.debugScript` (~:562))
    - src/scriptCommands.ts (`runLocalFromScriptNode` ~:289, `debugLocalFromScriptNode` ~:308, the `resolveScriptContext` helper ~:118-171, and the `await runScriptAtPath(context, tmpPath)` / `await debugScriptAtPath(context, tmpPath)` call sites ~:298, ~:330)
    - src/auth.ts (the `ensureWorkspaceToken(context, cfg, {workspaceId, authId})` signature and the `getActiveAccessToken` / `getBaseAccessToken` exports around lines 580-610)
    - src/workspace.ts (`getWorkspaceApiUrl(ws, envGlobalEndpoint)`, `listWorkspaces`, `getSelectedWorkspace`, `WorkspaceInfo`)
    - src/remoteExecution.ts lines 88-155 (the Block A pattern that resolves `ws` via `getSelectedWorkspace ?? listWorkspaces(...).find` then calls `ensureWorkspaceToken` + `getWorkspaceApiUrl` — this is the SOURCE OF TRUTH for the `target` branch)
    - .planning/phases/06-script-execution-ux-and-unified-params/06-CONTEXT.md (decisions D-01, D-02, D-03)
    - .planning/phases/06-script-execution-ux-and-unified-params/06-RESEARCH.md (§2.8 cross-workspace API URL, §5.4 workspaceName landmine — MUST resolve & log `ws.name`)
    - .planning/phases/06-script-execution-ux-and-unified-params/06-PATTERNS.md (`src/extension.ts` Pattern 1 + `src/scriptCommands.ts` Pattern 1 — exact target-arg shape and call-site updates)
  </read_first>
  <action>
    **In `src/extension.ts`:**

    1. Extend `prepareRun(context, scriptPath)` to a new signature `prepareRun(context, scriptPath, target?: { workspaceId: string; workspaceAuthId: string })`. When `target` is undefined, preserve all existing behavior verbatim (D-03 fallback). When `target` is provided, replace the endpoint/token resolution block (~:660-668) with a port of the `remoteExecution.ts:99-155` Block A pattern:
       - Resolve `ws: WorkspaceInfo | undefined = getSelectedWorkspace(context)`; if `ws?.workspaceId !== target.workspaceId`, fall back to `listWorkspaces(envGlobalEndpoint, baseToken).find(w => w.workspaceId === target.workspaceId)` where `baseToken` comes from `getBaseAccessToken(context, oauthCfg)` (the same pattern Block A uses; per AGENTS.md/CONVENTIONS.md surface errors via `vscode.window.showErrorMessage('Altium 365: ...')` and return `undefined` on missing baseToken / missing ws).
       - If resolved, `wsToken = await ensureWorkspaceToken(context, oauthCfg, { workspaceId: target.workspaceId, authId: target.workspaceAuthId || ws.authId })` and `endpoint = getWorkspaceApiUrl(ws, envGlobalEndpoint)`.
       - Set the returned `RunPrep` shape's token/endpoint fields to these values (do not touch other RunPrep fields).
       - Stash the resolved `ws.name` in a local that the OutputChannel header below can consume.

    2. In `runScriptAtPath` (~:533) and `debugScriptAtPath`, extend each signature with the SAME optional `target?: { workspaceId: string; workspaceAuthId: string }` and forward it to `prepareRun`. Update the OutputChannel header lines (~:544-545) so that when `target` was provided AND a resolved workspace name is available, an additional line `outputChannel.appendLine(\`[Altium 365] Workspace: ${resolvedWs.name}\`)` is emitted ahead of (or in place of — at executor discretion, see §5.4) the existing endpoint line. When `target` is undefined, header is unchanged (regression-safe for palette runs).

    3. The palette command handlers (`altium365.runScript` ~:514, `altium365.debugScript` ~:562) MUST NOT auto-derive a target — they call `runScriptAtPath(context, target_path)` (no third arg), preserving today's active-workspace behavior per D-03.

    **In `src/scriptCommands.ts`:**

    4. In `runLocalFromScriptNode` (~:289) and `debugLocalFromScriptNode` (~:308), after `downloadScriptToTmp` returns the tmpPath, build a `target` object from the node's identity by resolving via `resolveScriptContext(context, node)` (the helper already gives back `{ workspaceId, workspaceAuthId, ... }`). Pass `{ workspaceId: sc.workspaceId, workspaceAuthId: sc.workspaceAuthId }` as the third arg to the existing `runScriptAtPath(context, tmpPath)` / `debugScriptAtPath(context, tmpPath)` calls (~:298, ~:330). If `resolveScriptContext` returns undefined (defensive — shouldn't happen on a tree-driven invocation), pass undefined and let the active-workspace fallback handle it.

    Do NOT:
    - Add D-17's "no node + active editor not in cache" standalone-`.py` fallback to `runLocalFromScriptNode` here — that is Plan 2's responsibility (depends on this plan's `target` plumbing landing first, so it slots in cleanly).
    - Change `getActiveAccessToken` (auth.ts) — D-02 explicitly bypasses it for the target branch but leaves the function itself untouched (CONTEXT D-21: no auth changes).
    - Touch `remoteExecution.ts` in this task (Task 3 owns it).
    - Introduce a new RunPrep field; reuse existing token/endpoint slots.

    Commit message: `feat(06-03): route local run/debug through script's owning workspace (D-01..D-03) + fix cross-workspace header name (RESEARCH §5.4)`.
  </action>
  <verify>
    <automated>npm run compile</automated>
    <automated>grep -nE "function (runScriptAtPath|debugScriptAtPath|prepareRun)\b" src/extension.ts | grep -v '^#'</automated>
    <automated>grep -nE "ensureWorkspaceToken\(" src/extension.ts | grep -v '^#'</automated>
    <automated>grep -nE "runScriptAtPath\(context, tmpPath," src/scriptCommands.ts | grep -v '^#'</automated>
    <automated>grep -nE "debugScriptAtPath\(context, tmpPath," src/scriptCommands.ts | grep -v '^#'</automated>
    <automated>grep -nE "Workspace:" src/extension.ts | grep -v '^#'</automated>
  </verify>
  <acceptance_criteria>
    - `npm run compile` exits 0.
    - `prepareRun`, `runScriptAtPath`, `debugScriptAtPath` all accept an optional third parameter typed as `{ workspaceId: string; workspaceAuthId: string }` (or a named type alias — TypeScript shape must be structurally equivalent).
    - When the `target` branch is entered, `prepareRun` calls `ensureWorkspaceToken(context, oauthCfg, { workspaceId: ..., authId: ... })` — verifiable by `grep -nE "ensureWorkspaceToken\(.*workspaceId" src/extension.ts`.
    - The `target` branch sources its endpoint via `getWorkspaceApiUrl(ws, envGlobalEndpoint)` where `ws` was resolved from `getSelectedWorkspace ?? listWorkspaces(...).find(...)`.
    - The `target` branch does NOT call `getActiveAccessToken` (verifiable by reading the code — it should be in the `else` / fallback branch only).
    - The OutputChannel header path in `runScriptAtPath` / `debugScriptAtPath` now includes a `Workspace: <name>` line for the target branch (regression-safe: palette runs without target keep today's header).
    - In `scriptCommands.ts`, both `runLocalFromScriptNode` and `debugLocalFromScriptNode` now pass a third arg of shape `{ workspaceId, workspaceAuthId }` to their respective `*AtPath` call.
    - `getActiveAccessToken` and `ensureWorkspaceToken` are NOT modified (`git diff src/auth.ts` is empty).
    - Palette commands `altium365.runScript` / `altium365.debugScript` continue to call `runScriptAtPath(context, scriptPath)` with exactly two args (no target — D-03).
  </acceptance_criteria>
  <done>
    Tree-driven local run/debug routes via the script's owning workspace token + apiServiceUrl; palette/standalone path is unchanged; header shows correct workspace name; build passes; commit made.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add projectId-prompt fallback to remoteExecution Block B + fix Block A workspaceName header</name>
  <files>src/remoteExecution.ts</files>
  <read_first>
    - src/remoteExecution.ts (FULL file — Block A ~lines 88-155 (workspace resolution + token + apiUrl + the existing `withScriptProgress` wrapper opening), Block B ~:157-160 (`resolveScriptParameters` call), the `resolveScriptParameters` helper ~:234-253, the header lines around ~:175-177 that read `args.workspaceName`)
    - src/projectPicker.ts (the now-exported `pickProjectId` signature created in Task 1)
    - src/workspace.ts (`WorkspaceInfo` shape — needs `.name`)
    - .planning/phases/06-script-execution-ux-and-unified-params/06-CONTEXT.md (decisions D-05, D-06, D-07, D-22, D-23)
    - .planning/phases/06-script-execution-ux-and-unified-params/06-RESEARCH.md (§2.7 Block B control flow + insertion point INSIDE the existing withScriptProgress wrapper; §5.4 workspaceName plumbing fix; §5.5 double-prompt non-issue)
    - .planning/phases/06-script-execution-ux-and-unified-params/06-PATTERNS.md (`src/remoteExecution.ts` Pattern 1 verbatim — the insertion block; Pattern 4 — the `args.workspaceName = ws.name` overwrite)
  </read_first>
  <action>
    **Block A workspaceName fix (RESEARCH §5.4):** In `remoteExecution.ts` Block A, immediately AFTER the line that resolves `ws` (i.e., after the `if (!ws) { ... return undefined; }` guard), add: `if (ws.name && ws.name !== args.workspaceName) { args.workspaceName = ws.name; }`. This makes the existing header at ~:175-177 print the correct workspace name on cross-workspace remote execute. Do not touch the header lines themselves.

    **Block B projectId prompt (D-05, D-06, D-07):** In Block B, replace the current `const parameters = resolveScriptParameters(args.context, args.scriptId);` with the form shown in the `<interfaces>` block above (let-binding + the `if (parameters === undefined)` fallback). Specifically:

    1. Change `const parameters` → `let parameters`.
    2. Immediately AFTER the `resolveScriptParameters` call and BEFORE the existing `return { ws, wsToken, apiUrl, parameters };`, add the fallback block:
       - Read `const wcfg = vscode.workspace.getConfiguration('altium365');`.
       - Guard: `if (parameters === undefined && wcfg.get<boolean>('promptForProjectId', true))`.
       - Inside the guard: build `const lastKey = \`altium365.lastProjectId.${args.workspaceId}\`;` (D-07 — exact same key prefix that `extension.ts` `prepareRun` writes to).
       - Read `const last = args.context.globalState.get<string>(lastKey, '');`.
       - Call `const picked = await pickProjectId(args.context, apiUrl, wsToken, last, ws, args.output);` (D-06 — passes the script's resolved `ws` as target per D-08; `args.output` from the existing args shape gives the picker a channel for any error logging — confirm `args.output` is the OutputChannel field, otherwise pass `undefined`).
       - On cancel (`picked === undefined`), `return undefined;` — the surrounding `executeRemoteScript` already treats a Block-B undefined as a clean abort.
       - On non-empty pick: `await args.context.globalState.update(lastKey, picked);` and `parameters = [{ key: 'projectId', value: picked }];`.
       - On empty pick (`picked === ''` — user chose "no parameters" in the picker), leave `parameters` undefined and fall through (current behavior — server uses script defaults).

    3. Add `import { pickProjectId } from './projectPicker';` to the top imports of `remoteExecution.ts` (alongside existing local imports).

    Do NOT:
    - Wrap the new prompt in a nested `withScriptProgress` (D-22 — the surrounding wrap at the function body already covers Block A+B).
    - Read or write the reserved key `altium365.scriptParams.<scriptId>` directly in this task (D-23 — keep that key untouched; `resolveScriptParameters` is the only allowed reader and it stays exactly as today).
    - Add a sibling `.params.json` lookup for remote (D-06 explicitly: "No sibling .params.json lookup for remote — tmp/virtual files don't have meaningful siblings").
    - Refactor `resolveScriptParameters` itself.

    Commit message: `feat(06-03): projectId prompt fallback for remote execute + shared last-pick cache (D-05..D-07); fix Block A workspaceName header (§5.4)`.
  </action>
  <verify>
    <automated>npm run compile</automated>
    <automated>grep -n "import { pickProjectId } from './projectPicker'" src/remoteExecution.ts</automated>
    <automated>grep -nE "altium365\.lastProjectId\." src/remoteExecution.ts | grep -v '^#'</automated>
    <automated>grep -nE "wcfg\.get<boolean>\('promptForProjectId'" src/remoteExecution.ts | grep -v '^#'</automated>
    <automated>grep -nE "args\.workspaceName = ws\.name" src/remoteExecution.ts | grep -v '^#'</automated>
    <automated>grep -c "withScriptProgress(" src/remoteExecution.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run compile` exits 0.
    - `src/remoteExecution.ts` imports `pickProjectId` from `./projectPicker`.
    - Block A contains the `args.workspaceName = ws.name` assignment guarded by an equality check (no unconditional overwrite of a falsy ws.name).
    - Block B uses `let parameters` (NOT `const`) — verifiable by `grep -nE "let parameters" src/remoteExecution.ts`.
    - Block B reads the config key `altium365.promptForProjectId` via `getConfiguration('altium365').get<boolean>('promptForProjectId', true)` (DEFAULT true matches CONTEXT D-06).
    - Block B reads and writes the global-state key `altium365.lastProjectId.<workspaceId>` using `args.workspaceId` (the same prefix `extension.ts` `prepareRun` uses; symmetric per D-07).
    - The `pickProjectId(...)` call inside Block B passes 6 args: `args.context`, `apiUrl`, `wsToken`, `last`, `ws`, `args.output` (the 5th arg is the script's resolved workspace `ws`; the 6th is the OutputChannel from args — Task 1 made both optional, so either may be undefined at runtime but BOTH must be present at the call site).
    - The count of `withScriptProgress(` occurrences in the file is UNCHANGED from before the task (no nested wrap added — verifiable: count before vs after must match; D-22).
    - `resolveScriptParameters` body is unchanged (`git diff src/remoteExecution.ts` shows changes only in Block A and Block B regions, not in the helper at ~:234-253).
    - No new reference to the reserved key prefix `altium365.scriptParams.` is introduced (`grep -c "altium365.scriptParams" src/remoteExecution.ts` returns the same count as before — likely 1, inside `resolveScriptParameters`).
  </acceptance_criteria>
  <done>
    Remote Execute Remotely now prompts via `pickProjectId` against the script's workspace when no per-script params are set and the config flag is on; pick is cached under the shared key local already uses; cross-workspace header shows the right workspace name; build passes; commit made.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human UAT — cross-workspace routing, debug-save-back, unified projectId prompting</name>
  <what-built>
    Tasks 1-3 delivered: tree-driven local Run/Debug routes via the script's owning workspace token + apiServiceUrl; cross-workspace OutputChannel header shows the correct workspace name; remote Execute Remotely now prompts via `pickProjectId` (target-aware) when no per-script params are set and the config flag is on; local↔remote share the `altium365.lastProjectId.<workspaceId>` cache key; `pickProjectId` lives in `src/projectPicker.ts`.
  </what-built>
  <how-to-verify>
    Run a Dev Host (F5) signed in to TWO Altium 365 workspaces (call them A and B; both must contain at least one runnable script with the same Python entry point). Active workspace = A throughout unless explicitly switched.

    1. **SC-1 — Local Run cross-workspace routing.** Active = A. Expand workspace B in the side panel → Scripts → right-click any script → "Run Script (Local)". Expected: OutputChannel header shows `[Altium 365] Workspace: <B's name>` (NOT A's name, NOT `<workspace-name unknown>`), `[Altium 365] Endpoint:` line uses B's apiServiceUrl, script executes successfully. The side panel's active-workspace cue still shows A as active (no implicit switch — D-01).

    2. **SC-1 — Local Debug cross-workspace routing.** Same as above with "Debug Script (Local)". Same expectations.

    3. **SC-1 — Palette + standalone .py active-workspace fallback (D-03).** Open a local `.py` file from disk that is NOT a downloaded remote tmp. Run `Altium 365: Run Script` from the Command Palette. Expected: header shows A's workspace name (or fires the existing "select workspace first" prompt if no active workspace), executes against A's token.

    4. **SC-2 — Debug-save-back (D-04 UAT-only).** Active = A. Expand workspace B → Scripts → right-click a script → "Debug Script (Local)". When the debugger starts, edit a line in the opened tmp tab (the file under `os.tmpdir()/altium365/<authIdB>/<scriptId>/<name>.py` from Plan 1). Press Cmd+S. Expected: existing save bridge fires; status message / OutputChannel reports `[Altium 365] Published <scriptName>`; refreshing the side panel and re-opening the script shows the edit. **If this UAT FAILS:** type a description of the failure mode; the planner will create a defensive follow-up plan (D-04 caveat).

    5. **SC-3 — Unified projectId prompt (config on).** In VS Code settings, set `altium365.promptForProjectId = true` (default). Right-click a script in workspace A → "Execute Remotely". Expected: the `pickProjectId` QuickPick appears titled with workspace A's name (placeholder copy), lists A's projects, pre-filled with the last project id picked in A (or empty on first run). Pick a project. Expected: remote execution sends `parameters: [{key:'projectId', value: <picked>}]` (verify via OutputChannel — there should be a log of the request body or at minimum a successful execute response keyed to that project).

    6. **SC-3 — Shared last-pick cache symmetry (D-07).** After step 5, right-click the same workspace A's script → "Run Script (Local)". Expected: the projectId QuickPick (already wired in local) appears pre-filled with the value chosen in step 5. Pick a DIFFERENT project. Then right-click → "Execute Remotely" again. Expected: the QuickPick is now pre-filled with the value from this Run, proving the two flows share `altium365.lastProjectId.<workspaceId>`.

    7. **SC-3 — config off preserves no-prompt behavior.** Set `altium365.promptForProjectId = false`. Right-click a script in A → "Execute Remotely". Expected: NO QuickPick appears; execute fires immediately; server uses script defaults (today's behavior preserved).

    8. **SC-3 — cross-workspace remote execute.** Active = A. Right-click a script in workspace B → "Execute Remotely" (with `promptForProjectId = true`, no per-script params for that script). Expected: the QuickPick placeholder names workspace B, project list is B's projects, picking writes to `altium365.lastProjectId.<workspaceId-of-B>` (NOT A's key). Verify by then running the same in A — A's QuickPick should NOT be pre-filled with B's pick (the keys are workspace-scoped per D-07).

    9. **Cancel path.** Trigger step 5, but in the QuickPick press Esc. Expected: remote execute aborts cleanly (no GraphQL request sent, no error toast, status returns to idle).

    10. **Reserved key untouched (D-23 regression).** Open Developer: Inspect Context Keys / use the VS Code memento inspector (or via a temporary `console.log` removed before merge) to confirm `altium365.scriptParams.<scriptId>` for the script you executed remains absent in workspaceState — only `altium365.lastProjectId.<workspaceId>` is written.
  </how-to-verify>
  <resume-signal>Type "approved" if scenarios 1-10 all pass (record step 4's result explicitly — pass or fail). If step 4 fails, describe the exact UI/log behavior so the planner can scope a defensive plan per D-04.</resume-signal>
</task>

</tasks>

<verification>
- `npm run compile` exits 0 after each commit.
- `pickProjectId` is defined exactly once in the repo (`src/projectPicker.ts`); not in `extension.ts`.
- `extension.ts` `prepareRun`/`runScriptAtPath`/`debugScriptAtPath` carry `target?: { workspaceId, workspaceAuthId }` and use `ensureWorkspaceToken` + `getWorkspaceApiUrl(ws, ...)` in the target branch.
- `remoteExecution.ts` Block B contains the prompt fallback gated on `altium365.promptForProjectId` AND `parameters === undefined`; Block A overwrites `args.workspaceName` from `ws.name`.
- `scriptCommands.ts` `runLocalFromScriptNode` / `debugLocalFromScriptNode` pass a target object built from the node's `workspaceId` + `workspaceAuthId`.
- Manual UAT (Task 4) confirms SC-1, SC-2, SC-3.
</verification>

<success_criteria>
- SC-1 (workspace-context routing) verified by Task 4 steps 1-3.
- SC-2 (debug save-back) verified by Task 4 step 4 (or escalated for defensive follow-up if repro fails).
- SC-3 (unified parameter model with shared cache) verified by Task 4 steps 5-9.
- D-07 cache symmetry verified by step 6.
- D-08 target-aware picker verified by step 8.
- D-22 (no nested withScriptProgress wraps) verified by automated grep count check in Task 3.
- D-23 (reserved key untouched) verified by Task 4 step 10.
- All four commits land on the working branch with the messages specified above.
</success_criteria>

<output>
Create `.planning/phases/06-script-execution-ux-and-unified-params/06-03-SUMMARY.md` when done.
</output>
