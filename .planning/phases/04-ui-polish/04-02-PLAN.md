---
id: 04-02
phase: 04-ui-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sidePanel.ts
  - src/extension.ts
autonomous: true
requirements_addressed: [D-06, D-07, D-08, D-09, D-10]
must_haves:
  truths:
    - "Projects category node renders with $(project) codicon (D-06)"
    - "Scripts category node renders with $(file-code) codicon (D-06)"
    - "Active workspace renders with $(circle-filled) codicon; inactive renders with $(cloud) (D-08, D-10)"
    - "Active workspace TreeItem.description is NOT '(active)' (suffix dropped) (D-09)"
    - "A365 tree view header shows current environment name as TreeView.description (D-07)"
    - "Tree view description updates whenever the active environment changes (D-07)"
  artifacts:
    - path: src/sidePanel.ts
      provides: "Updated category icons + active-workspace icon swap + dropped '(active)' suffix"
      contains: "ThemeIcon('project')"
      contains: "ThemeIcon('file-code')"
      contains: "ThemeIcon('circle-filled')"
    - path: src/extension.ts
      provides: "treeView.description wired to active environment name"
      contains: "treeView.description"
  key_links:
    - from: src/extension.ts (activate)
      to: treeView.description
      via: "set initial value + update on configuration change event for altium365.activeEnvironment"
      pattern: "treeView\\.description"
    - from: src/sidePanel.ts getTreeItem('workspace')
      to: getSelectedWorkspace(ctx)
      via: "existing selected-workspace lookup determines which codicon to render"
      pattern: "circle-filled|cloud"
---

<objective>
Update three tree-rendering details in the side panel: (1) Projects category icon `folder-library` → `project`, (2) Scripts category icon `folder-library` → `file-code`, (3) active workspace icon `cloud` → `circle-filled` while inactive keeps `cloud` (drop the `(active)` text suffix). Also set `treeView.description` to the active environment name on `vscode.window.createTreeView` and refresh it whenever the environment changes.

Purpose: Give the user clearer visual semantics — different category icons make Projects vs Scripts scannable; the active-workspace icon swap carries the signal that was previously a dim-color hack + redundant `(active)` text; the env name in the view header replaces a context-menu round-trip for "which env am I on?".

Output: `src/sidePanel.ts` with three icon updates + suffix removal; `src/extension.ts` with `treeView.description` wiring (initial value + listener).
</objective>

<execution_context>
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-ui-polish/04-CONTEXT.md
@AGENTS.md
@.planning/codebase/CONVENTIONS.md
@src/sidePanel.ts
@src/extension.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap category + workspace icons; drop '(active)' suffix</name>
  <files>src/sidePanel.ts</files>
  <read_first>
    - src/sidePanel.ts (lines 108-196, focus on workspace case 110-136 and category cases 137-154)
    - .planning/phases/04-ui-polish/04-CONTEXT.md (D-06, D-08, D-09, D-10)
  </read_first>
  <action>
    In `src/sidePanel.ts`:

    1. **Workspace case (lines ~110-136)**: When `isActive === true`, set `item.iconPath = new vscode.ThemeIcon('circle-filled')` (replacing the current `'cloud'`); do NOT set `item.description = '(active)'` — drop that line entirely so active workspaces have no description suffix. Inactive branch keeps the existing `new vscode.ThemeIcon('cloud', new vscode.ThemeColor('descriptionForeground'))` (no change). Preserve the comment block at lines 116-123 but refresh it to mention the new icon strategy ("active = $(circle-filled), inactive = $(cloud) + descriptionForeground"). Per D-08, D-09, D-10. **Icon choice rationale (D-10): `circle-filled` is reserved here because future 999.2 workspace-favorites will use `star-full`/`pinned`; the two cues can stack without semantic collision** — capture this rationale in the comment so a future revisitor does not undo it.

    2. **Projects category case (line ~143)**: Replace `new vscode.ThemeIcon('folder-library')` with `new vscode.ThemeIcon('project')`. Per D-06.

    3. **Scripts category case (line ~152)**: Replace `new vscode.ThemeIcon('folder-library')` with `new vscode.ThemeIcon('file-code')`. Per D-06.

    Do not change `contextValue` assignments in this task (Plan 04-04 handles the `CTX_WORKSPACE` split). Do not touch any other case (`project`, `script`, `info`, `error`). All four codicons (`project`, `file-code`, `circle-filled`, `cloud`) are built-in to VS Code engine ^1.85.0 — no `contributes.icons` entry needed.
  </action>
  <verify>
    <automated>grep -n "ThemeIcon('project')" src/sidePanel.ts | grep -v '^#'</automated>
    <automated>grep -n "ThemeIcon('file-code')" src/sidePanel.ts | grep -v '^#'</automated>
    <automated>grep -n "ThemeIcon('circle-filled')" src/sidePanel.ts | grep -v '^#'</automated>
    <automated>! grep -n "description = '(active)'" src/sidePanel.ts</automated>
    <automated>npm run compile</automated>
  </verify>
  <done>
    `src/sidePanel.ts` references `ThemeIcon('project')` (Projects category), `ThemeIcon('file-code')` (Scripts category + scripts items — note `file-code` was already used at line 170 for individual scripts; both occurrences are fine), and `ThemeIcon('circle-filled')` (active workspace). The literal string `description = '(active)'` does NOT appear anywhere. `folder-library` no longer appears in the file. `npm run compile` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire treeView.description to active environment name</name>
  <files>src/extension.ts</files>
  <read_first>
    - src/extension.ts (lines 33-98, focus on `activate()` and the `createTreeView` call at lines 41-44)
    - src/extension.ts (lines 86-89 — existing `onAuthStateChanged` listener that calls `treeProvider.refresh()`)
    - .planning/phases/04-ui-polish/04-CONTEXT.md (D-07)
    - package.json (lines 197-201 — `altium365.activeEnvironment` configuration property)
  </read_first>
  <action>
    In `src/extension.ts` `activate(context)`:

    1. Immediately after the `createTreeView` call at line 41-44, set the initial description from configuration:
       - Read `vscode.workspace.getConfiguration('altium365').get<string>('activeEnvironment', '')` into a local `const activeEnv`.
       - Set `treeView.description = activeEnv || undefined` (use `undefined` for empty string so VS Code hides the description gracefully instead of rendering an empty appendix).

    2. Add a `vscode.workspace.onDidChangeConfiguration` listener that refreshes `treeView.description` whenever `altium365.activeEnvironment` changes. The listener body: `if (e.affectsConfiguration('altium365.activeEnvironment')) { treeView.description = vscode.workspace.getConfiguration('altium365').get<string>('activeEnvironment', '') || undefined; }`. Push the resulting disposable into `context.subscriptions`. This automatically handles `doSelectEnvironment` updating the configuration at line 357 (no direct call into `doSelectEnvironment` needed — listening to the canonical event keeps the wiring decoupled).

    Per D-07. Do NOT modify `doSelectEnvironment` itself. Do NOT modify the existing `onAuthStateChanged` listener (it already fires `treeProvider.refresh()` which re-runs `getChildren`; tree description is independent of provider refresh — VS Code does not re-read it on refresh, hence the explicit configuration listener).
  </action>
  <verify>
    <automated>grep -n "treeView.description" src/extension.ts | grep -v '^#'</automated>
    <automated>grep -n "onDidChangeConfiguration" src/extension.ts | grep -v '^#'</automated>
    <automated>grep -n "altium365.activeEnvironment" src/extension.ts | grep -v '^#'</automated>
    <automated>npm run compile</automated>
  </verify>
  <done>
    `src/extension.ts` sets `treeView.description` once in `activate` from `altium365.activeEnvironment`, and updates it via an `onDidChangeConfiguration` listener gated on `e.affectsConfiguration('altium365.activeEnvironment')`. The listener's disposable is pushed into `context.subscriptions`. `npm run compile` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| globalState/configuration → TreeView UI | `altium365.activeEnvironment` value rendered as `treeView.description` is sourced from VS Code configuration (already shown in `Output → Altium 365` status bar today via the same value); same trust posture as status bar text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Information Disclosure | TreeView.description displaying env name | accept | Environment name is user-chosen / config-defined; not a secret. Same source already rendered in status bar (Phase 02-05) — no new injection sink. |
| T-04-02-02 | Tampering | Codicon swaps | accept | Built-in VS Code codicons only (`project`, `file-code`, `circle-filled`, `cloud`); no `contributes.icons` additions; no external asset surface. |

ASVS L1: no auth surface (D-15); no GraphQL changes; reuses existing globalState (D-16).
</threat_model>

<verification>
- `npm run compile` exits 0
- After F5: tree header shows `WORKSPACES <env-name>` (env name dimmed); run `Altium 365: Select Environment` → pick a different env → header description updates without restart
- Expand a workspace → Projects category shows the `$(project)` codicon glyph; Scripts category shows `$(file-code)`
- The currently-selected workspace renders with a solid filled circle to the left of its name (and no `(active)` text suffix); other workspaces render with the cloud glyph as before
</verification>

<success_criteria>
ROADMAP §Phase 4 SC #4 (distinct category codicons), SC #5 (env name in tree header via `treeView.description`), SC #6 (active workspace visually distinguishable by more than dimmed color). D-06, D-07, D-08, D-09, D-10 fully covered.
</success_criteria>

<output>
Create `.planning/phases/04-ui-polish/04-02-SUMMARY.md` when done.
</output>
