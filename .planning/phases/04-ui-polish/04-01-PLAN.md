---
id: 04-01
phase: 04-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: true
requirements_addressed: [D-01, D-02, D-03, D-04, D-16]
must_haves:
  truths:
    - "Command Palette filter 'Altium 365:' lists exactly 6 entries (D-02, D-03, D-04)"
    - "Context menu entries render without a redundant 'Altium 365:' prefix (D-01)"
    - "Removed commands still execute correctly when invoked from context menus / view-title (D-03, D-04)"
    - "Existing command IDs and globalState keys are unchanged (backwards compat per D-16)"
  artifacts:
    - path: package.json
      provides: "Trimmed contributes.commands[] (6 entries) + cleaned title strings"
      contains: "altium365.signIn"
  key_links:
    - from: package.json contributes.menus
      to: contributes.commands
      via: "menu entries reference command IDs that are now only registered via registerCommand (no longer in contributes.commands[])"
      pattern: "command.*altium365.script|altium365.tree|altium365.workspace.openInBrowser|altium365.project.openInBrowser|altium365.statusBar"
---

<objective>
Strip the redundant `Altium 365: ` prefix from every `title` in `package.json` `contributes.commands[]` (D-01), and trim `contributes.commands[]` to the 6-command user-actionable whitelist (D-02..D-04). Non-whitelisted command IDs remain wired via `vscode.commands.registerCommand` in `extension.ts` / `treeCommands.ts` / `scriptCommands.ts`, so context menus, view-title actions, and editor-title actions continue to resolve them.

Purpose: Fix the double-prefix bug Phase 3 UAT surfaced and de-clutter the Command Palette so only commands a user can sensibly invoke without a tree node selected appear there.

Output: `package.json` with 6 entries in `contributes.commands[]`, each titled without the `Altium 365: ` prefix.
</objective>

<execution_context>
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-ui-polish/04-CONTEXT.md
@AGENTS.md
@.planning/codebase/CONVENTIONS.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip "Altium 365: " prefix from every command title</name>
  <files>package.json</files>
  <read_first>
    - package.json (current contributes.commands[] block, lines 45-128)
    - .planning/phases/04-ui-polish/04-CONTEXT.md (D-01)
  </read_first>
  <action>
    For every entry in `contributes.commands[]`, replace `"title": "Altium 365: <X>"` with `"title": "<X>"`. Keep `"category": "Altium 365"` untouched on every entry — VS Code prepends category in the palette automatically (yielding `Altium 365: <X>` in the palette while context menus show `<X>` only). Targets (all 17 current entries) including: `altium365.runScript → "Run Python Script"`, `altium365.debugScript → "Debug Python Script"`, `altium365.signIn → "Sign In"`, `altium365.signOut → "Sign Out"`, `altium365.selectWorkspace → "Select Workspace"`, `altium365.selectEnvironment → "Select Environment"`, `altium365.tree.refresh → "Refresh Workspaces"`, `altium365.tree.retryNode → "Retry Tree Node"`, `altium365.statusBar.click → "Status Bar Menu"`, `altium365.script.runLocal → "Run Script (Local)"`, `altium365.script.edit → "Edit Script"`, `altium365.script.executeRemote → "Execute Script Remotely"`, `altium365.script.publish → "Publish Script"`, `altium365.tree.copyId → "Copy ID"`, `altium365.workspace.openInBrowser → "Open Workspace in Browser"`, `altium365.project.openInBrowser → "Open Project in Browser"`. Do not modify `category`, `icon`, or `command` fields. Per D-01 — applies before Task 2 trims the list, so every entry that survives Task 2 inherits the cleaned title.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');const bad=p.contributes.commands.filter(c=>/^Altium 365:/.test(c.title));process.exit(bad.length===0?0:1)"</automated>
  </verify>
  <done>Every entry in `contributes.commands[]` has a `title` that does NOT start with `Altium 365:`; every entry still has `category: "Altium 365"`.</done>
</task>

<task type="auto">
  <name>Task 2: Reduce contributes.commands[] to 6-entry palette whitelist</name>
  <files>package.json, src/extension.ts (read-only), src/treeCommands.ts (read-only), src/scriptCommands.ts (read-only)</files>
  <read_first>
    - package.json (full file — both contributes.commands[] and contributes.menus.* — to confirm menu references survive the trim)
    - src/extension.ts (verify `vscode.commands.registerCommand` calls exist for every removed command: altium365.tree.refresh, altium365.tree.retryNode, altium365.statusBar.click)
    - src/treeCommands.ts (verify registerCommand calls for altium365.tree.copyId, altium365.workspace.openInBrowser, altium365.project.openInBrowser)
    - src/scriptCommands.ts (verify registerCommand calls for altium365.script.runLocal, altium365.script.edit, altium365.script.executeRemote, altium365.script.publish)
    - .planning/phases/04-ui-polish/04-CONTEXT.md (D-02, D-03, D-04, D-16)
  </read_first>
  <action>
    Reduce `contributes.commands[]` to exactly these 6 entries (post-prefix-strip from Task 1), preserving each entry's `category` and `icon`:
      1. `altium365.signIn` → "Sign In"
      2. `altium365.signOut` → "Sign Out"
      3. `altium365.selectWorkspace` → "Select Workspace"
      4. `altium365.selectEnvironment` → "Select Environment" (keep `icon: $(globe)` — referenced by `view/title` menu entry)
      5. `altium365.runScript` → "Run Python Script"
      6. `altium365.debugScript` → "Debug Python Script"
    Remove these 11 entries from `contributes.commands[]` entirely (D-04 chose option a, NOT `menus.commandPalette: when: false`): `altium365.tree.refresh`, `altium365.tree.retryNode`, `altium365.statusBar.click`, `altium365.script.runLocal`, `altium365.script.edit`, `altium365.script.executeRemote`, `altium365.script.publish`, `altium365.tree.copyId`, `altium365.workspace.openInBrowser`, `altium365.project.openInBrowser`. Do NOT touch `contributes.menus` — every existing menu entry remains and continues to reference the removed command IDs (this is the supported pattern; menus can reference commands registered only via `registerCommand`). Do NOT modify any `vscode.commands.registerCommand(...)` calls in TS sources — all 11 removed commands stay registered there (verified in read_first). Per D-02/D-03/D-04. Backwards compat (D-16): command IDs and globalState keys (`altium365.selectedWorkspace`, `altium365.activeWorkspaceId`) remain unchanged.

    Note for executor: `contributes.commands[]` order may be reshuffled to match the whitelist order above for readability — VS Code does not depend on ordering. The `view/title` entry for `altium365.tree.refresh` at line 286-291 will continue to resolve via the `registerCommand` call in `extension.ts:80`; the `$(refresh)` icon is supplied inline on the menu entry already, so removing it from `contributes.commands[]` does not lose iconography. Same logic for `altium365.tree.retryNode` (referenced from `error` TreeItem command binding at `sidePanel.ts:188`).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');const ids=p.contributes.commands.map(c=>c.command).sort();const want=['altium365.debugScript','altium365.runScript','altium365.selectEnvironment','altium365.selectWorkspace','altium365.signIn','altium365.signOut'];process.exit(JSON.stringify(ids)===JSON.stringify(want)?0:1)"</automated>
    <automated>node -e "const ids=['altium365.tree.refresh','altium365.tree.retryNode','altium365.tree.copyId','altium365.tree.openInBrowser','altium365.statusBar.click','altium365.script.runLocal','altium365.script.edit','altium365.script.executeRemote','altium365.script.publish','altium365.workspace.openInBrowser','altium365.project.openInBrowser'];const fs=require('fs');const files=['src/extension.ts','src/treeCommands.ts','src/scriptCommands.ts'].filter(f=>fs.existsSync(f));const src=files.map(f=>fs.readFileSync(f,'utf8')).join('\n');const missing=ids.filter(id=>!src.includes(\`registerCommand('\${id}'\`)&&!src.includes(\`registerCommand(\\\"\${id}\\\"\`));if(missing.length){console.error('Missing registerCommand:',missing);process.exit(1)}else{console.log('All',ids.length,'removed IDs still registered')}"</automated>
    <automated>npm run compile</automated>
  </verify>
  <done>
    `contributes.commands[]` contains exactly 6 entries with the IDs listed above; `contributes.menus` is unchanged; `npm run compile` exits 0; every removed command ID still appears in at least one `vscode.commands.registerCommand(...)` call across `src/extension.ts`, `src/treeCommands.ts`, `src/scriptCommands.ts` (verified visually during read_first).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VS Code Marketplace → user install | `package.json` manifest is the install-time contract; trusted because it ships in the signed VSIX |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | `contributes.commands[]` trim | accept | No new privilege boundary — removing entries from `contributes.commands[]` only hides them from the palette; the underlying `registerCommand` registrations (which are the actual privilege grants) are unchanged. No new commands are added; no auth/token surface touched. |
| T-04-01-02 | Information Disclosure | command titles | accept | Titles contain no secrets or PII — only static UI strings. |

ASVS L1: no auth surface touched (D-15); reuses fully-audited command registration paths from Phase 02/02.1/02.3/03.
</threat_model>

<verification>
- `npm run compile` exits 0
- `node -e "process.exit(require('./package.json').contributes.commands.length===6?0:1)"` exits 0
- After F5 (Extension Development Host), Command Palette filter `Altium 365:` lists exactly 6 entries: Sign In, Sign Out, Select Workspace, Select Environment, Run Python Script, Debug Python Script
- Right-click a script tree node → context menu shows "Run Script (Local)", "Execute Script Remotely", "Edit Script", "Publish Script", "Copy ID" — NONE prefixed with "Altium 365:"
- Click view-title refresh icon → tree refreshes (proves `altium365.tree.refresh` still resolves despite being removed from `contributes.commands[]`)
</verification>

<success_criteria>
ROADMAP §Phase 4 SC #1 (palette/context menu titles) and SC #3 (6-command whitelist) are satisfied by this plan. D-01..D-04 and D-16 fully covered.
</success_criteria>

<output>
Create `.planning/phases/04-ui-polish/04-01-SUMMARY.md` when done.
</output>
