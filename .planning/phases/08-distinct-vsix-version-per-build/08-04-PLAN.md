---
phase: 08-distinct-vsix-version-per-build
plan: 04
type: execute
wave: 1
depends_on: [08-03]
files_modified:
  - src/updater.ts
  - package.json
autonomous: true
gap_closure: true
requirements: [UPD-02, UPD-05]
must_haves:
  truths:
    - "Updater hits the correct GitHub repo (AltiumDeveloper/a365-vscode-extension) and no longer 404s"
    - "Every user-visible 'Altium 365' brand surface in package.json (command categories, view container title, submenu label) displays as 'Altium Developer'"
    - "All altium365.* command IDs, view IDs, config keys remain unchanged (Part 3 scope fence preserved)"
  artifacts:
    - path: "src/updater.ts"
      provides: "RELEASES_URL constant pointing at AltiumDeveloper/a365-vscode-extension; matching doc comment"
      contains: "api.github.com/repos/AltiumDeveloper/a365-vscode-extension/releases"
    - path: "package.json"
      provides: "Rebranded display strings — 21 command categories + 1 view container title + 1 submenu label"
      contains: '"category": "Altium Developer"'
  key_links:
    - from: "src/updater.ts:31 RELEASES_URL"
      to: "GitHub Releases API for AltiumDeveloper/a365-vscode-extension"
      via: "https.get in getJson()"
      pattern: "AltiumDeveloper/a365-vscode-extension"
    - from: "package.json contributes.commands[*].category"
      to: "VS Code command palette grouping"
      via: "VS Code extension manifest"
      pattern: '"category":\s*"Altium Developer"'
---

<objective>
Close the two gaps identified in 08-UAT.md:

1. **Gap 1 (major):** `src/updater.ts` hardcodes the wrong GitHub owner (`altium/a365-vscode-extension`) — actual repo is `AltiumDeveloper/a365-vscode-extension`. Result: every update check returns HTTP 404.
2. **Gap 2 (major):** Phase 8 Part 3 rebrand was incomplete in `package.json` — 21 command `category` fields, 1 view container `title`, and 1 submenu `label` still display "Altium 365" instead of "Altium Developer".

Both gaps are mechanical string replacements with file:line precision pre-diagnosed in UAT.md. No investigation needed.

Purpose: First public release builds under the rebranded identity (`altium.developer`) MUST present a consistent "Altium Developer" surface in command palette / Activity Bar AND MUST be able to reach the real Releases endpoint so the self-updater works on day one.

Output: Updated `src/updater.ts` and `package.json` with all display-string fixes; `altium365.*` identifiers preserved (Part 3 scope fence per CONTEXT.md lines 94-100).
</objective>

<execution_context>
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/08-distinct-vsix-version-per-build/08-UAT.md
@.planning/phases/08-distinct-vsix-version-per-build/08-CONTEXT.md
@src/updater.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix RELEASES_URL owner segment in src/updater.ts (Gap 1)</name>
  <files>src/updater.ts</files>
  <action>Fix Gap 1 from 08-UAT.md. Two edits in src/updater.ts, both changing the GitHub owner segment from `altium` to `AltiumDeveloper`:

1. **Line 11 (module header doc comment):** `altium/a365-vscode-extension` → `AltiumDeveloper/a365-vscode-extension`.
2. **Line 31 (RELEASES_URL constant):** `https://api.github.com/repos/altium/a365-vscode-extension/releases` → `https://api.github.com/repos/AltiumDeveloper/a365-vscode-extension/releases`.

Do NOT extract to a constant or read from package.json `repository.url` — CONTEXT.md Part 3 (line 99) explicitly notes the repo URL/owner is unchanged at the GitHub level (rebrand of the repo is a separate, optional, future operation; this is purely a typo-class fix where the code's `altium` segment never matched the actual GitHub owner `AltiumDeveloper`).

Do NOT touch `package.json:14-17` `repository.url` — that lives under `altium/a365-vscode-extension` legacy URL and GitHub redirects handle it. Out of scope for this gap.

Do NOT touch any other constant in src/updater.ts (EXTENSION_ID, GLOBAL_LAST_CHECK_KEY, USER_AGENT, LOG_PREFIX) — all correct per CONTEXT.md Part 3.</action>
  <verify>
    <automated>grep -n "altium/a365-vscode-extension" src/updater.ts | grep -v "^#" ; test $(grep -c "AltiumDeveloper/a365-vscode-extension" src/updater.ts) -eq 2 && npm run compile</automated>
  </verify>
  <done>
    - `grep "altium/a365-vscode-extension" src/updater.ts` returns zero matches
    - `grep -c "AltiumDeveloper/a365-vscode-extension" src/updater.ts` returns exactly 2 (doc comment + RELEASES_URL)
    - `npm run compile` succeeds (no tsc errors)
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace "Altium 365" display strings with "Altium Developer" in package.json (Gap 2)</name>
  <files>package.json</files>
  <action>Fix Gap 2 from 08-UAT.md. In `package.json`, replace 23 user-visible "Altium 365" display strings with "Altium Developer":

1. **21 command category fields** (lines 49, 55, 60, 65, 70, 75, 80, 85, 90, 96, 101, 106, 112, 117, 122, 127, 132, 137, 142, 147, 152, 157 — note UAT.md lists 22 indices because line 162 is the already-correct `altium365.checkForUpdates` entry; the 21 to change are all `"category": "Altium 365"` occurrences):
   `"category": "Altium 365"` → `"category": "Altium Developer"`
2. **1 view container title** at package.json:25 (`contributes.viewsContainers.activitybar[0].title`):
   `"title": "Altium 365"` → `"title": "Altium Developer"`
3. **1 submenu label** at package.json:289 (`contributes.submenus[0].label` for `altium365.editorTitle`):
   `"label": "Altium 365"` → `"label": "Altium Developer"`

**Implementation:** Easiest path is a single `Edit` call with `replaceAll: true` on the exact string `"category": "Altium 365"` → `"category": "Altium Developer"` (handles all 21 categories in one shot since they're identical), then two separate `Edit` calls for the title (line 25) and label (line 289) — those need unique context because the bare strings `"title": "Altium 365"` and `"label": "Altium 365"` each appear only once.

**SCOPE FENCE — DO NOT TOUCH** (per CONTEXT.md Part 3 lines 94-100, reconfirmed by UAT.md Gap 2 `missing[3]`):
- All `altium365.*` command IDs (`altium365.signIn`, `altium365.runScript`, etc.) — these are stable user-facing contracts (keybindings, task definitions, palette muscle memory).
- All `altium365.*` config keys (`altium365.graphqlEndpoint`, `altium365.checkForUpdates`, etc.).
- All `altium365.*` view IDs / view container IDs (`altium365`, `altium365.tree`, `altium365.editorTitle`).
- The string `"Altium 365"` appearing inside the `viewsWelcome` body at line 41 (it's a link label for the marketing site `altium.com/altium-365` — NOT a brand surface to rename; this is the actual A365 platform, not the extension name).
- The string `"Altium 365"` in the `description` field at line 4 (mentions the "Altium 365" PLATFORM the extension talks to — not the extension's own brand; CONTEXT.md line 97 explicitly excludes `description` from this rebrand).
- The `altium365` activity bar container `id` at line 24 (`"id": "altium365"`) — view container ID, not display.
- The setting description at line 259 ("Use 'Altium 365: Select Environment' to switch") — this is doc copy referencing palette grouping; it's an orthogonal copy edit that may regress when display category changes. Update this one too: `'Altium 365: Select Environment'` → `'Altium Developer: Select Environment'` since the command's new palette grouping is "Altium Developer".

After all edits, verify the JSON is still valid (`node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"`) and that no `altium365.*` identifier was accidentally renamed.</action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))" && test $(grep -c '"category": "Altium 365"' package.json) -eq 0 && test $(grep -c '"category": "Altium Developer"' package.json) -eq 22 && test $(grep -c '"title": "Altium 365"' package.json) -eq 0 && test $(grep -c '"label": "Altium 365"' package.json) -eq 0 && grep -q '"id": "altium365"' package.json && grep -q '"altium365.signIn"' package.json && grep -q '"altium365.graphqlEndpoint"' package.json && npm run compile</automated>
  </verify>
  <done>
    - `package.json` parses as valid JSON
    - Zero `"category": "Altium 365"` occurrences remain; exactly 22 `"category": "Altium Developer"` (21 rebranded + the already-correct `altium365.checkForUpdates` at line 162)
    - Zero `"title": "Altium 365"` occurrences remain
    - Zero `"label": "Altium 365"` occurrences remain
    - All `altium365.*` command IDs / config keys / view IDs / submenu IDs intact (`altium365.signIn`, `altium365.graphqlEndpoint`, `"id": "altium365"`, `"id": "altium365.editorTitle"`, etc. all still present)
    - The "Altium 365" link text in `viewsWelcome` (line 41 region, the marketing-site label) is intentionally untouched
    - The setting description at line ~259 references "Altium Developer: Select Environment" not "Altium 365: Select Environment"
    - `npm run compile` succeeds
  </done>
</task>

</tasks>

<verification>
End-to-end gap-closure verification (run from project root):

1. **Gap 1 closed:**
   - `grep "altium/a365-vscode-extension" src/updater.ts` → no results
   - `grep "AltiumDeveloper/a365-vscode-extension" src/updater.ts` → 2 matches (line ~11 doc comment + line ~31 RELEASES_URL)

2. **Gap 2 closed:**
   - `grep -c '"category": "Altium 365"' package.json` → 0
   - `grep -c '"category": "Altium Developer"' package.json` → 22
   - `grep -c '"title": "Altium 365"' package.json` → 0
   - `grep -c '"label": "Altium 365"' package.json` → 0

3. **Scope fence intact (no identifier rename leaked):**
   - `grep -E '"altium365\.(signIn|signOut|runScript|debugScript|tree|script|workspace|project|testEvents|checkForUpdates|graphqlEndpoint|activeEnvironment|environments|editorTitle)' package.json` returns plenty of matches (all `altium365.*` IDs preserved)

4. **Build still green:** `npm run compile` exits 0.

5. **Manual re-run of UAT Tests 1, 2, 3** (deferred to executor / phase verification — gap-closure plan only needs the automated gates above; full UAT re-test happens after Gap 2 in the next verify cycle, per gap_closure workflow).
</verification>

<success_criteria>
- Both gaps in 08-UAT.md `## Gaps` section are mechanically closed
- All automated `<verify>` gates pass for both tasks
- No `altium365.*` identifier was renamed (Part 3 scope fence honored)
- `npm run compile` clean
- Repo state ready to re-run UAT Tests 1, 2, 3 (executor invokes Test 1 by manually running `altium365.checkForUpdates` once and confirming the output line no longer shows `HTTP 404`)
</success_criteria>

<output>
Create `.planning/phases/08-distinct-vsix-version-per-build/08-04-SUMMARY.md` when done.

Include in summary:
- Confirmation both gaps closed with the automated gate outputs
- Note that this plan is `gap_closure: true` (does not introduce new requirements beyond UPD-02 / UPD-05 alignment)
- Recommend re-running UAT Tests 1, 2, 3 against the rebranded VSIX after this lands
</output>
