---
phase: 02.2-auth-hardening
plan: 06
type: execute
wave: 2
depends_on:
  - 02.2-02
files_modified:
  - src/sidePanel.ts
  - src/workspace.ts
autonomous: true
requirements:
  - AUTH-WR-02
  - AUTH-WR-05-CARRY
tags:
  - call-sites
  - refresh-aware
must_haves:
  truths:
    - "sidePanel.ts loadWorkspaces fetches its token via getBaseAccessToken (refresh-aware), not getStoredTokens(...).access_token"
    - "extension.ts pickAndExchangeWorkspace (in workspace.ts) fetches its listWorkspaces token via getBaseAccessToken"
    - "Neither call site receives a workspace-scoped token for the listWorkspaces call (WR-05 carry-forward closed)"
    - "An expired base token triggers a refresh transparently before the listWorkspaces call instead of returning a 401"
  artifacts:
    - path: "src/sidePanel.ts"
      provides: "loadWorkspaces uses getBaseAccessToken (D-09)"
      contains: "getBaseAccessToken"
    - path: "src/workspace.ts"
      provides: "pickAndExchangeWorkspace uses getBaseAccessToken for the list, ensureWorkspaceToken for the exchange (D-10)"
      contains: "getBaseAccessToken"
  key_links:
    - from: "sidePanel.ts loadWorkspaces"
      to: "auth.ts getBaseAccessToken"
      via: "import update + call swap"
      pattern: "getBaseAccessToken\\(this.ctx"
    - from: "workspace.ts pickAndExchangeWorkspace"
      to: "auth.ts getBaseAccessToken + ensureWorkspaceToken"
      via: "two-step list-then-exchange"
      pattern: "getBaseAccessToken\\(context"
---

<objective>
Switch the two base-scope GraphQL call sites to consume `getBaseAccessToken` from plan 02.2-02. This closes WR-02 (refresh-on-expiry was missing from `loadWorkspaces`) and the WR-05 carry-forward (the naive Option 1 from `02.1-REVIEW-FIX.md` would have leaked a workspace-scoped token to `listWorkspaces`; D-03's surgical fix means call sites now ask for exactly the token they need).

Purpose: every base-scope GraphQL call (`listWorkspaces`) now uses a refresh-aware base token; every workspace-scope GraphQL call (`listProjects`, `listScripts`, future Phase-3 script ops) continues to use `ensureWorkspaceToken`. The two-step `pickAndExchangeWorkspace` flow remains explicit per D-10.
Output: two call-site swaps with no behavioural changes beyond the refresh-on-expiry improvement.
</objective>

<execution_context>
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.2-auth-hardening/02.2-CONTEXT.md
@.planning/phases/02.1-side-panel-ux/02.1-REVIEW.md
@.planning/phases/02.1-side-panel-ux/02.1-REVIEW-FIX.md
@src/auth.ts
@src/sidePanel.ts
@src/workspace.ts

<interfaces>
<!-- After plan 02.2-02 lands, auth.ts exports getBaseAccessToken(context, cfg): Promise<string | undefined> — refresh-aware, base scope only.
     Current sidePanel.ts loadWorkspaces (lines 225-253):
       - Line 5: imports getStoredTokens from './auth'
       - Line 231: const base = await getStoredTokens(this.ctx);
       - Line 232: if (!base?.access_token) return [];
       - Line 236: await listWorkspaces(endpoint, base.access_token);
     Current workspace.ts pickAndExchangeWorkspace (lines 63-99):
       - Line 6: imports getStoredTokens from './auth' (used in the helper)
       - Line 68: const base = await getStoredTokens(context);
       - Line 69-72: early-return with sign-in prompt if !base
       - Line 75: workspaces = await listWorkspaces(graphqlEndpoint, base.access_token);
       - Line 96: const tok = await exchangeWorkspaceToken(context, cfg, pick.ws.authId);  // workspace-scope exchange, stays as-is per D-10's two-step intent
     Other getStoredTokens callers (must NOT be touched — they need the raw TokenSet for id_token claims, not just the access_token):
       - src/extension.ts line 25, 240 (claims decoding / status)
       - src/statusBar.ts line 36 (truthy check for signed-in state)
       - src/auth.ts internal callers (getActiveUserLabel, refreshTokens, exchangeWorkspaceToken, getBaseAccessToken itself)
     These stay on getStoredTokens. Only the two access_token-extraction call sites swap to getBaseAccessToken. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Swap sidePanel.ts loadWorkspaces to getBaseAccessToken</name>
  <read_first>
    - src/sidePanel.ts (imports at lines 1-16, loadWorkspaces at lines 225-253)
    - .planning/phases/02.2-auth-hardening/02.2-CONTEXT.md (D-09)
  </read_first>
  <action>
    In `src/sidePanel.ts`:

    1. Update the import from `'./auth'` to add `getBaseAccessToken` and remove `getStoredTokens` IF no other code in sidePanel.ts uses it. Run `grep -n "getStoredTokens" src/sidePanel.ts` first — current state shows only the single use at line 231, so removing the import is safe. If a stray use exists, keep `getStoredTokens` in the import alongside the new `getBaseAccessToken`.

    2. Inside `loadWorkspaces`, replace the three-line block:
         `const base = await getStoredTokens(this.ctx);`
         `if (!base?.access_token) { return []; }`
         `... await listWorkspaces(endpoint, base.access_token);`
       with:
         `const baseToken = await getBaseAccessToken(this.ctx, cfg);`
         `if (!baseToken) { return []; }`
         `... await listWorkspaces(endpoint, baseToken);`

       Note that the existing `void cfg;` placeholder at line 230 of the current code (which currently exists because `cfg` was unused) MUST be removed — `cfg` is now consumed by `getBaseAccessToken`.

    3. Keep the empty-list info node fallback (lines 237-243) and the workspaceUrl derivation (line 244) and the node mapping (lines 245-252) and the cache assignment (line 251) unchanged.

    No other change to sidePanel.ts in this plan.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `grep -n "getBaseAccessToken(this.ctx" src/sidePanel.ts` matches exactly once inside loadWorkspaces
    - `grep -n "getStoredTokens(this.ctx)" src/sidePanel.ts` returns NO matches
    - `grep -n "void cfg" src/sidePanel.ts` returns NO matches (placeholder removed)
    - `grep -n "listWorkspaces(endpoint" src/sidePanel.ts` still matches — the GraphQL call is unchanged, only its token argument changes
  </acceptance_criteria>
  <done>
    loadWorkspaces is refresh-aware. An expired base token now triggers a refresh transparently before the listWorkspaces call. WR-02 is closed at this call site.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Swap workspace.ts pickAndExchangeWorkspace to getBaseAccessToken for the list step</name>
  <read_first>
    - src/workspace.ts (imports at lines 1-11, pickAndExchangeWorkspace at lines 63-99)
    - .planning/phases/02.2-auth-hardening/02.2-CONTEXT.md (D-10 — "Two-step is intentional and explicit")
    - .planning/phases/02.1-side-panel-ux/02.1-REVIEW-FIX.md WR-05 row (rationale: naive swap of getActiveAccessToken would leak a workspace token to listWorkspaces — D-10 prevents that by keeping the two calls separate and typed)
  </read_first>
  <action>
    In `src/workspace.ts`:

    1. Update the `'./auth'` import: add `getBaseAccessToken`. Decide whether to remove `getStoredTokens` from the import based on `grep -n "getStoredTokens" src/workspace.ts` — current state shows only the single use at line 68 inside pickAndExchangeWorkspace, so the import can be removed.

    2. Inside `pickAndExchangeWorkspace`, replace:
         `const base = await getStoredTokens(context);`
         `if (!base) { vscode.window.showErrorMessage(...); return undefined; }`
         `workspaces = await listWorkspaces(graphqlEndpoint, base.access_token);`
       with:
         `const baseToken = await getBaseAccessToken(context, cfg);`
         `if (!baseToken) { vscode.window.showErrorMessage('Sign in first (Altium 365: Sign In).'); return undefined; }`
         `workspaces = await listWorkspaces(graphqlEndpoint, baseToken);`

    3. Leave `exchangeWorkspaceToken(context, cfg, pick.ws.authId)` at line 96 EXACTLY as-is. D-10 calls out the two-step explicitly: base-scope list, then workspace-scope exchange. The two calls do different things and use different tokens; do not collapse them. (Optional refinement: per D-10's wording "ensureWorkspaceToken to acquire/refresh the workspace-scoped token", the executor MAY swap `exchangeWorkspaceToken` for `ensureWorkspaceToken` here so this path benefits from cache-hit dedup and refresh-aware behaviour. If swapping, change the signature accordingly: `ensureWorkspaceToken(context, cfg, { workspaceId: pick.ws.workspaceId, authId: pick.ws.authId })` returns `string`, not `TokenSet`, so the return type and downstream `tok` handling in extension.ts:160 must accept that. If the executor is uncertain about ripple effects, keep `exchangeWorkspaceToken` for this plan and defer the ensure-swap to a follow-up. Document the choice in SUMMARY.)

    4. Verify no other code in `workspace.ts` depends on the removed `getStoredTokens` import.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `grep -n "getBaseAccessToken(context" src/workspace.ts` matches exactly once inside pickAndExchangeWorkspace
    - `grep -n "getStoredTokens(context)" src/workspace.ts` returns NO matches
    - `grep -n "listWorkspaces(graphqlEndpoint" src/workspace.ts` still matches in pickAndExchangeWorkspace (call preserved, token argument changed)
    - The two-step structure is preserved: the function still contains BOTH a listWorkspaces call AND an exchangeWorkspaceToken (or ensureWorkspaceToken) call — verified by `grep -nE "listWorkspaces|exchangeWorkspaceToken|ensureWorkspaceToken" src/workspace.ts` showing both within the pickAndExchangeWorkspace function body
  </acceptance_criteria>
  <done>
    pickAndExchangeWorkspace uses a refresh-aware base token for the list step and continues to use the workspace-scope exchange for the second step. WR-05 carry-forward closed at this call site.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean
- Repo-wide grep gate: `grep -rn "getStoredTokens(.*).access_token" src/` returns NO matches — proves no remaining caller is doing the unsafe "grab the raw stored token and use its access_token without refresh-awareness" pattern.
- Manual smoke (deferred to UAT plan 02.2-07 scenarios 3 + 4): expired base token still loads the tree without 401; with a workspace selected, refresh tree → network log shows listWorkspaces called with the BASE token, not the workspace-scoped one.
</verification>

<success_criteria>
WR-02 + 02.1 WR-05 carry-forward closed. Every base-scope GraphQL call uses the refresh-aware base accessor; every workspace-scope call uses the workspace-scope accessor. The naive Option 1 rejected in 02.1-REVIEW-FIX is now structurally impossible because the two accessors exist as distinct exported functions with distinct names.
</success_criteria>

<output>
Create `.planning/phases/02.2-auth-hardening/02.2-06-SUMMARY.md` when done.
</output>
