---
phase: 02.2-auth-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - 02.2-02
files_modified:
  - src/auth.ts
autonomous: true
requirements:
  - AUTH-CR-02
tags:
  - auth
  - sign-in
  - emitter
must_haves:
  truths:
    - "signIn calls clearAllTokens at the top, before the OAuth dance, so no prior identity's per-workspace cache survives"
    - "The pre-drain inside signIn does NOT emit a transient signedIn:false event"
    - "The explicit sign-out command path (doSignOut) still results in a signedIn:false event reaching subscribers"
    - "The sign-in-failure cleanup path (if/when added) still results in a signedIn:false event reaching subscribers"
    - "signIn still emits signedIn:true after successful token acquisition"
  artifacts:
    - path: "src/auth.ts"
      provides: "signIn pre-drain + silent emitter refactor (D-05, D-06)"
      contains: "clearAllTokens"
  key_links:
    - from: "signIn (entry)"
      to: "clearAllTokens (silent variant)"
      via: "direct call before OAuth"
      pattern: "clearAllTokens\\("
---

<objective>
Per D-05, drain any prior identity's token caches at the top of `signIn` so an account switch without explicit sign-out cannot leave stale per-workspace tokens around. Per D-06, suppress the transient `signedIn:false` event the drain would otherwise fire mid-sign-in (subscribers like the status bar / side panel would briefly flicker to the signed-out state).

Purpose: closes CR-02 (workspace-cache pollution across identities) without introducing UI flicker.
Output: a `signIn` that drains-then-authenticates, and a `clearAllTokens` that supports a silent variant OR a refactor that moves the emitter fire to the two legitimate sign-out call sites — planner picks the smaller diff.
</objective>

<execution_context>
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02.2-auth-hardening/02.2-CONTEXT.md
@.planning/phases/02-side-panel/02-REVIEW.md
@.planning/codebase/CONVENTIONS.md
@src/auth.ts
@src/extension.ts

<interfaces>
<!-- Current state after plan 02.2-02 lands:
       - clearAllTokens(context) deletes SECRET_TOKENS, enumerates GLOBAL_WS_TOKEN_INDEX_KEY, deletes each SECRET_WS_TOKEN_PREFIX+id, clears the index, and fires authStateEmitter.fire({ signedIn: false }).
       - signIn does NOT currently call clearAllTokens.
       - The only external caller of clearAllTokens is extension.ts doSignOut at line ~144 (verified by grep at planning time).
     Two valid implementation shapes for D-06 (planner picks the SMALLER DIFF):
       Shape A (parameter): Change clearAllTokens signature to clearAllTokens(context, options?: { silent?: boolean }). When silent === true, skip the emitter fire. signIn calls clearAllTokens(context, { silent: true }) at entry. doSignOut keeps its existing zero-arg call. Diff: ~3 lines.
       Shape B (refactor): Remove the emitter fire from clearAllTokens entirely. Move authStateEmitter.fire({ signedIn: false }) into doSignOut (extension.ts) AFTER the await clearAllTokens(...) call. signIn calls plain clearAllTokens(context) at entry without firing anything. Diff: ~4 lines across two files BUT requires importing fireAuthStateChanged in extension.ts (already exported from auth.ts? check — fireAuthStateChanged IS exported per line 40 of current auth.ts).
     Decision rule for planner discretion: prefer Shape A (smaller diff, single file touched, mirrors the existing try/catch around the fire). If executor finds Shape A introduces an awkward boolean parameter that VS Code TS lint complains about, fall back to Shape B and document the choice in the SUMMARY. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add silent flag to clearAllTokens and call it from signIn entry</name>
  <read_first>
    - src/auth.ts (current signIn at lines 182-236, clearAllTokens at lines 331-344, authStateEmitter usage at lines 38-42 and 231 and 340)
    - .planning/phases/02.2-auth-hardening/02.2-CONTEXT.md (D-05, D-06, "Claude's Discretion" — smaller refactor wins)
    - src/extension.ts lines 143-148 (doSignOut — must still see signedIn:false after the change)
  </read_first>
  <action>
    Implement Shape A from the interfaces note (parameter on `clearAllTokens`) unless TypeScript surfaces an issue, in which case fall back to Shape B and document.

    Shape A steps:

    1. In `src/auth.ts`, change `clearAllTokens` signature from `(context: vscode.ExtensionContext)` to `(context: vscode.ExtensionContext, options?: { silent?: boolean })`. The drain body (delete SECRET_TOKENS, loop over index, delete per-workspace secrets, clear index) runs unchanged. Wrap the `authStateEmitter.fire({ signedIn: false })` call in `if (!options?.silent) { ... }` so a silent caller skips the broadcast. Keep the existing try/catch around the fire.

    2. In `src/auth.ts` `signIn`, add `await clearAllTokens(context, { silent: true });` as the FIRST line of the function body — before `pkcePair()`, before the redirect setup, before `awaitCallback`. This guarantees any prior identity's per-workspace cache is gone before a new sign-in can populate one.

    3. Verify that `extension.ts doSignOut` (line 144) still calls `clearAllTokens(context)` with no options arg. Since `options` is optional, this remains a valid call and still fires `signedIn:false` to subscribers (status bar, side panel) — no change required in extension.ts.

    4. Sanity check: confirm there are no other callers of `clearAllTokens` in the repo that need updating. Run `grep -rn "clearAllTokens" src/` to enumerate; if any other call site exists, evaluate whether it should be silent (it should NOT — only signIn's pre-drain is silent; the existing call in doSignOut is correctly loud).

    If TypeScript complains about Shape A (e.g., strict-mode optional-parameter handling), pivot to Shape B:
      - Remove the `authStateEmitter.fire({ signedIn: false })` from `clearAllTokens` (lines 339-343).
      - In `extension.ts doSignOut`, import `fireAuthStateChanged` from `./auth` (already exported per line 40 of current auth.ts) and call `fireAuthStateChanged({ signedIn: false })` AFTER `await clearAllTokens(context);`.
      - `signIn` still adds `await clearAllTokens(context);` at the top — without the fire inside clearAllTokens, signIn's pre-drain is silent by construction.
      - Document the Shape B pivot in the SUMMARY's Decisions section.

    Do NOT modify any other behaviour of signIn (PKCE, redirect, token storage, post-success emitter fire at line 231 stays exactly as-is). Do NOT touch token-routing (plan 02.2-02 territory). Do NOT touch the mutex wiring (plan 02.2-04). Do NOT touch the OAuth error HTML (plan 02.2-05).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - Shape A: `grep -n "options?: { silent" src/auth.ts` matches the new clearAllTokens signature; `grep -n "clearAllTokens(context, { silent: true })" src/auth.ts` matches the call in signIn
    - Shape B (only if used): `grep -n "fireAuthStateChanged({ signedIn: false })" src/extension.ts` matches in doSignOut; `grep -c "authStateEmitter.fire({ signedIn: false })" src/auth.ts` returns 0
    - `grep -n "await clearAllTokens" src/auth.ts | head -1` matches as the first executable line of signIn (verify by reading the surrounding context, not just the grep)
    - `grep -n "clearAllTokens" src/extension.ts` still matches in doSignOut — the explicit sign-out path is unchanged in its effect on subscribers
    - signIn still contains `authStateEmitter.fire({ signedIn: true, user: ... })` after the token-acquisition success path (preserved from the existing code at line 231)
  </acceptance_criteria>
  <done>
    A second sign-in (account switch without explicit sign-out) atomically drains the prior identity's per-workspace cache and base token before issuing the new OAuth request, without emitting a misleading transient signedIn:false event. Explicit sign-out via the command palette is unchanged for subscribers.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean
- Manual smoke (deferred to UAT plan 02.2-07 scenario 1): sign in as A, expand a workspace, sign in as B without signing out, expand the same workspace — must trigger a NEW exchange (not return A's cached token).
</verification>

<success_criteria>
CR-02 closed: the account-switch leak is impossible because the cache is unconditionally drained at signIn entry. No UI flicker (no transient signedIn:false event reaches subscribers during the drain).
</success_criteria>

<output>
Create `.planning/phases/02.2-auth-hardening/02.2-03-SUMMARY.md` when done.
</output>
