---
phase: 02.2-auth-hardening
plan: 07
subsystem: docs/uat
tags: [uat, docs, auth]
dependency_graph:
  requires: []
  provides: ["Manual UAT script for phase 02.2"]
  affects: []
tech_stack:
  added: []
  patterns: ["Per-scenario PASS/FAIL checkbox blocks (preconditions / steps / expected)"]
key_files:
  created:
    - .planning/phases/02.2-auth-hardening/02.2-UAT.md
  modified: []
decisions:
  - "Followed the structure prescribed in the plan (Scenario / Preconditions / Steps / Expected / PASS-FAIL) rather than the legacy 02.1-UAT.md test-list format — the plan explicitly required Scenario sections with discrete PASS/FAIL checkboxes."
  - "Added an 'Out of scope' footer documenting that D-12 (token-endpoint error-body truncation) is intentionally not scripted, matching the planner's note."
metrics:
  duration_minutes: 4
  completed: 2026-05-19
---

# Phase 02.2 Plan 07: Manual UAT Script Summary

Authored the manual UAT script `02.2-UAT.md` that the developer runs at phase-end to validate the
six in-scope findings (CR-01, CR-02, WR-01, WR-02, WR-04, WR-05) before phase 02.2 is marked
complete. Exactly five scenarios from D-14, each with preconditions, numbered steps, expected
observable outcome, and a PASS/FAIL checkbox pair; no automated logging/telemetry per D-13.

## Tasks Completed

| Task | Name                                            | Commit | Files                                                  |
| ---- | ----------------------------------------------- | ------ | ------------------------------------------------------ |
| 1    | Write 02.2-UAT.md with five scenarios from D-14 | (see final commit) | `.planning/phases/02.2-auth-hardening/02.2-UAT.md` |

## What Was Built

A single new file: `.planning/phases/02.2-auth-hardening/02.2-UAT.md`.

Structure:

- Frontmatter (`phase`, `date`, `status: ready-to-run`)
- Intro + one-time setup block (Extension Host, OutputChannel, Network filter)
- Five `## Scenario N` sections, in D-14 order:
  1. Account switch without explicit sign-out (CR-02 / D-05)
  2. Parallel workspace expansion (WR-05 race / D-07 dedup)
  3. Expired base token triggers refresh (WR-02 / D-09)
  4. listWorkspaces always uses the base token (WR-05 carry-forward / D-03, D-09, D-10)
  5. OAuth error XSS hardening (WR-01 / D-11)
- "Overall result" section with aggregate PASS / FAIL checkboxes and a notes block
- Footer noting D-12 (error-body truncation) is intentionally out of UAT scope

Each scenario specifies how to observe the outcome (OutputChannel, Network tab, SecretStorage via
Application panel, or JWT decoding for scope-claim inspection in Scenario 4).

## Verification

Automated gate from the plan (all green):

```
exists
scenarios: 5
PASS: 5
Pre: 5
Exp: 5
```

`grep -c "^## Scenario"` returns exactly `5`; `grep -c "^- \[ \] PASS"`, `grep -c "^\*\*Preconditions\*\*"`,
`grep -c "^\*\*Expected\*\*"` each return `5`. The five scenarios map 1:1 to D-14 in the prescribed
order. No placeholder TODOs remain.

## Deviations from Plan

None — plan executed exactly as written. The single judgment call was choosing the plan-prescribed
Scenario/Preconditions/Steps/Expected/PASS-FAIL structure over the legacy 02.1-UAT.md test-list
format; this is what the plan explicitly required and is documented in `decisions` above rather
than as a deviation.

## Self-Check: PASSED

- File exists at `.planning/phases/02.2-auth-hardening/02.2-UAT.md` — verified via `test -f`.
- All four grep gates from `<acceptance_criteria>` return the required counts.
- No files outside `<files_modified>` were touched.
