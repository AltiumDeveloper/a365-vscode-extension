---
phase: 02.3-actionwait-auth
verified: 2026-05-20T00:00:00Z
status: human_needed
score: 5/5 requirements + 16/16 decisions structurally closed
re_verification: null
human_verification:
  - test: "D-14 UAT scenarios 1.a/1.b/1.c (happy path against dev/uat/prod)"
    expected: "Sign-in completes; tokens stored; signedIn:true emitted"
    why_human: "Requires real ActionWait service + real browser auth; per D-14 manual-only by design"
  - test: "D-14 UAT scenario 2 (cancel via progress notification)"
    expected: "Silent abort, no token stored, no error toast"
    why_human: "Requires live notification UI interaction"
  - test: "D-14 UAT scenario 3 (browser tab closed → 180s overall timeout)"
    expected: "User-facing 'timed out after 3 minutes' message"
    why_human: "Requires real browser tab + 3-minute wall-clock"
  - test: "D-14 UAT scenario 4 (CSRF state mismatch)"
    expected: "'state mismatch (possible CSRF)' error; no token exchange"
    why_human: "Requires debugger-injected mutated state or local intercept"
  - test: "D-14 UAT scenario 5 (network failure mid-poll)"
    expected: "'cannot reach ActionWait service (<host>)' error"
    why_human: "Requires live network disable mid-flow"
  - test: "D-14 UAT scenario 6 (Phase 02.2 regression: workspace expansion + sign-out)"
    expected: "Per-workspace token exchange + clearAllTokens both still work post-AW sign-in"
    why_human: "End-to-end UX flow validation"
  - test: "D-05 empirical: ActionWait 200 body field names are exactly `code` and `state`"
    expected: "Parser at src/auth.ts:194-201 succeeds against real response"
    why_human: "CONTEXT D-05 flags this as TBD; only first real sign-in confirms"
---

# Phase 02.3 — ActionWait Auth Flow — VERIFICATION

**Phase Goal (ROADMAP.md):** Replace loopback HTTP-server OAuth callback with Altium's ActionWait long-poll service per user-provided flow. Loopback removed entirely. Env-specific redirect_uri. Cancellable. Phase 02.2 hardening surface preserved.

**Status:** ✅ STRUCTURALLY PASSED — all code-verifiable closure confirmed. ⚠️ HUMAN UAT (D-14, 6 scenarios) required to close functionally per phase design (manual-only — no automated tests).

---

## 1. Files-Changed Boundary

`git diff --stat dc9c89f..HEAD -- src/ package.json`:

| File | Lines | Verdict |
|---|---|---|
| `src/auth.ts` | +/- 216 | ✓ expected |
| `src/extension.ts` | +/- 82 | ✓ expected |
| `package.json` | +/- 17 | ✓ expected |

No other `src/` files touched (`git diff --name-only dc9c89f..HEAD -- src/` returns exactly two). ✓

---

## 2. Requirements (AUTH-AW-01..05)

| Req | Description | Evidence | Status |
|---|---|---|---|
| **AUTH-AW-01** | Replace loopback HTTP with ActionWait long-poll | `src/auth.ts:136-220` `pollActionWait`: POST JSON `{token}` to endpoint; 200 → parse `{code,state}`; 408 → reconnect (`continue`); 410 → cancelled; other → fatal. Invoked from `signIn` at `src/auth.ts:258-263` BEFORE `openExternal` (line 265) per D-09 race-free comment. | ✓ VERIFIED |
| **AUTH-AW-02** | Fixed env-specific `redirect_uri`; no dynamic port | `src/auth.ts:242` (authorize URL) and `src/auth.ts:278` (token exchange) both use the **same** `cfg.redirectUri`. `OAuthConfig` (lines 6-14) carries `redirectUri: string`. Old `redirectPort`/`redirectPath` removed everywhere (Section 4 below). | ✓ VERIFIED |
| **AUTH-AW-03** | `connection_token` (GUID) as OAuth state and AW key | `src/auth.ts:237` `const connectionToken = crypto.randomUUID()`. Used as OAuth `state` at line 246, as AW poll body at line 168, and CSRF-checked at line 271 with exact message `'State mismatch (possible CSRF).'` matching D-07. | ✓ VERIFIED |
| **AUTH-AW-04** | Honour env-specific `actionWaitEndpoint` | `readOAuthConfig` reads `actionWaitEndpoint` (`src/auth.ts:510`). `EnvironmentSpec` includes it (`src/extension.ts:240`). Env-switch propagates it (`src/extension.ts:297-299`). `package.json` ships defaults for all three envs (lines 199, 208, 217) + schema entry (line 230). | ✓ VERIFIED |
| **AUTH-AW-05** | All loopback code removed (no fallback) | Section 4 below — zero loopback artifacts in `src/` or `package.json`; only comments in `src/auth.ts:125, 236` reference the removed flow descriptively. | ✓ VERIFIED |

---

## 3. CONTEXT Decisions (D-01..D-16)

| Dec | Locked Outcome | File:Line Evidence | Status |
|---|---|---|---|
| **D-01** | Env-specific redirect_uri (auth host + `/authCompleted`) | `package.json:200` dev1, `:209` uat1, `:218` prod — all `https://auth.<env>.altium.com/authCompleted` ✓ | ✓ |
| **D-02** | Add `redirectUri` to `EnvironmentSpec` + schema | `src/extension.ts:241`, `package.json:231` | ✓ |
| **D-03** | Remove `altium365.redirectPort`/`redirectPath` settings | `grep -n redirectPort package.json` → 0 matches; same for `redirectPath` | ✓ |
| **D-04** | Drop port/path, add `actionWaitEndpoint`+`redirectUri` on `OAuthConfig` | `src/auth.ts:6-14` `OAuthConfig` declares both new fields, neither old field present; `readOAuthConfig` (lines 502-512) reads them | ✓ |
| **D-05** | POST JSON `{token}`; 200=done, 408=reconnect, 410=cancel; body `{code,state}` | `src/auth.ts:165-168` (request), `:184-202` (200), `:205-207` (408), `:210-212` (410), `:214-215` (fatal). 500-char body truncation present on all error paths. | ✓ |
| **D-06** | `crypto.randomUUID()` for connection_token | `src/auth.ts:237` | ✓ |
| **D-07** | connection_token doubles as state; CSRF mismatch error | `src/auth.ts:246, 271-273` exact message `'State mismatch (possible CSRF).'` | ✓ |
| **D-08** | clientId stays global (out-of-scope deferral) | `package.json` retains single `altium365.clientId`; no per-env client field added. | ✓ |
| **D-09** | `withProgress` cancellable; AbortController wired; silent abort | `src/extension.ts:125-141`: `cancellable: true`, `controller.abort()` on cancel, signal passed to `signIn`. `pollActionWait` honours signal at `src/auth.ts:146-150, 159-161, 173-176`. `signIn` invokes `clearAllTokens(ctx,{silent:true})` at line 232 (per D-13). | ✓ |
| **D-10** | `awaitCallback` + `escapeHtml` removed | `grep -rn "awaitCallback\|escapeHtml" src/` → 0 matches | ✓ |
| **D-11** | New private `pollActionWait(endpoint, connectionToken, signal, timeoutMs)` in auth.ts | `src/auth.ts:136-141` — signature exact match to D-11 spec | ✓ |
| **D-12** | Five distinct error mappings at extension.ts call site | `src/extension.ts:155-203`: silent-cancel (155), CSRF (160), wall-clock timeout (168), network (176), AW non-2xx (193), token-endpoint fallthrough (203). All messages match D-12 verbatim. | ✓ |
| **D-13** | Phase 02.2 surface invariant | `git diff dc9c89f..HEAD -- src/auth.ts` shows **no removed lines** matching `getBaseAccessToken\|getActiveAccessToken\|ensureWorkspaceToken\|workspaceTokenMutex\|AsyncMutex\|SECRET_TOKENS\|decodeIdTokenClaims\|userLabelFromClaims\|getStoredTokens\|clearAllTokens`. All exports still present at `src/auth.ts:46, 324, 397, 422, 453, 482`. `clearAllTokens(context,{silent:true})` at top of `signIn` preserved (`:232`). | ✓ |
| **D-14** | Manual UAT only — ≥5 scenarios | `02.3-UAT.md` 397 lines covering scenarios 1.a/1.b/1.c, 2, 3, 4, 5, 6 (= 6 distinct + 2 sub-runs of 1). Sign-off table at line 374. | ✓ scaffolded; ⚠️ not executed |
| **D-15** | actionWaitEndpoint URLs as-is | `package.json:199, 208, 217` — Dev/Uat/Prod values present, untouched | ✓ |
| **D-16** | `globalThis.fetch` (no new dep) | `src/auth.ts:165` `fetch(endpoint, ...)` — same pattern as `postForm` at `:97`. No new deps in `package.json`. | ✓ |

---

## 4. Loopback Residue Scan

`grep -rnE "http\.createServer|server\.listen|127\.0\.0\.1|loopback|redirectPort|redirectPath|awaitCallback|escapeHtml" src/ package.json`:

| Pattern | Hits | Verdict |
|---|---|---|
| `http.createServer` | 0 | ✓ |
| `server.listen` | 0 | ✓ |
| `127.0.0.1` | 0 | ✓ |
| `loopback` | 2 (both in `src/auth.ts:125, 236` — descriptive comments only, no code) | ✓ |
| `redirectPort` / `redirectPath` | 0 in src/ and package.json | ✓ |
| `awaitCallback` / `escapeHtml` | 0 | ✓ |

No `http` module import remains (`grep "from 'http'\|require('http')" src/` → 0).

---

## 5. redirect_uri Equivalence (AUTH-AW-02 Spot Check)

Within a single `signIn` invocation (`src/auth.ts:222-292`), `cfg.redirectUri` is a **closed-over const** for the duration of the call. Both uses reference the same value:

- Authorize URL params: `src/auth.ts:242` → `redirect_uri: cfg.redirectUri`
- Token exchange form: `src/auth.ts:278` → `redirect_uri: cfg.redirectUri`

No mutation, no recomputation between them. ✓

---

## 6. Type-Check

`npx tsc --noEmit` → exit 0, no output. ✓

---

## 7. UAT.md Sufficiency for D-14

| D-14 Scenario | Covered in UAT.md | Notes |
|---|---|---|
| 1. Happy path dev/uat/prod (×3) | §1.a (line 34), §1.b (82), §1.c (118) | Each has port-listener `lsof` check confirming loopback gone (e.g. line 105) |
| 2. Cancel via progress notification | §2 (line 154) | |
| 3. Browser closed → 180s timeout | §3 (line 188) | |
| 4. State mismatch / CSRF | §4 (line 225) | Two methods offered (debugger / local intercept) |
| 5. Network failure mid-poll | §5 (line 282) | |
| 6. Phase 02.2 regression sanity | §6 (line 325) | Workspace expansion + sign-out coverage |

Sign-off table present (line 374). UAT.md is sufficient for D-14 closure once executed by a human. ✓

---

## 8. Gaps

None blocking. The only outstanding item is **human execution of D-14 UAT** — by phase design (D-14 explicitly: "Manual UAT only — no automated tests for the poll loop"). The verifier cannot run a real browser/ActionWait dance.

Additionally, the CONTEXT.md research-items §1-3 (200-body field names, error envelope shapes, token TTL) were planner-flagged as TBD pending first real sign-in. The parser at `src/auth.ts:194-201` assumes `{code, state}` per D-05 working assumption; first happy-path UAT run will confirm or trigger a documented fix-forward.

---

## 9. Verdict

**STRUCTURAL PASSED.** Phase goal is achieved at the code layer. All 5 requirements, all 16 decisions, no loopback residue, type-clean, Phase 02.2 invariants preserved (zero deletions of D-13 surface).

**FUNCTIONAL closure** requires the 6 manual UAT scenarios in `02.3-UAT.md` to be executed and signed off (D-14). That is the next gate before the phase can be marked done in `STATE.md`.

_Verified: 2026-05-20 — the agent (gsd-verifier)_
