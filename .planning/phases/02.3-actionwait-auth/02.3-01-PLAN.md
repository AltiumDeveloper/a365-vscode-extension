---
phase: 02.3-actionwait-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: true
requirements:
  - AUTH-AW-02
  - AUTH-AW-04
  - AUTH-AW-05
tags:
  - config
  - schema
  - breaking-settings
must_haves:
  truths:
    - "Each env (Dev/Uat/Prod) default in altium365.environments has both actionWaitEndpoint AND redirectUri populated with the D-01 URLs"
    - "altium365.redirectPort and altium365.redirectPath setting blocks are entirely removed from contributes.configuration"
    - "altium365.environments.additionalProperties schema lists actionWaitEndpoint and redirectUri as known properties"
    - "package.json remains valid JSON and the existing description block for altium365.environments is updated to mention the two new fields"
  artifacts:
    - path: "package.json"
      provides: "Updated env schema (D-01, D-02, D-03)"
      contains: "redirectUri"
  key_links:
    - from: "altium365.environments.default.Dev"
      to: "https://auth.dev1.altium.com/authCompleted"
      via: "redirectUri field"
      pattern: "\"redirectUri\":\\s*\"https://auth\\.dev1\\.altium\\.com/authCompleted\""
    - from: "altium365.environments.default.Uat"
      to: "https://auth.uat1.altium.com/authCompleted"
      via: "redirectUri field"
      pattern: "\"redirectUri\":\\s*\"https://auth\\.uat1\\.altium\\.com/authCompleted\""
    - from: "altium365.environments.default.Prod"
      to: "https://auth.altium.com/authCompleted"
      via: "redirectUri field"
      pattern: "\"redirectUri\":\\s*\"https://auth\\.altium\\.com/authCompleted\""
---

<objective>
Align package.json with the ActionWait flow's settings surface: per-env defaults gain a `redirectUri` field (D-01, D-02), the obsolete loopback settings (`altium365.redirectPort`, `altium365.redirectPath`) are deleted entirely (D-03), and the env-schema's `additionalProperties` lists the two ActionWait fields that landed in commit `e445252` plus the new redirect.

Purpose: this is the user-visible config contract. Plans 02 + 04 + 05 depend on these settings being declared so VS Code populates them in user `settings.json` and `readOAuthConfig` finds non-empty values for new installs.
Output: a single coherent diff to `package.json` — three env defaults updated, two setting blocks removed, one schema fragment extended.
</objective>

<execution_context>
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.3-actionwait-auth/02.3-CONTEXT.md
@AGENTS.md
@package.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update env defaults, delete loopback settings, extend additionalProperties schema</name>
  <read_first>
    - package.json (lines 155-242 — the contributes.configuration.properties block; in particular the redirectPort/redirectPath blocks at 162-171, the environments default at 202-241, and the additionalProperties schema at 231-240)
    - .planning/phases/02.3-actionwait-auth/02.3-CONTEXT.md (D-01 for exact redirectUri URLs per env; D-02 for the schema field add; D-03 for the unconditional removal — no deprecation period; D-15 confirms existing actionWaitEndpoint defaults are taken as-is)
  </read_first>
  <action>
    Make exactly three edits to `package.json`:

    1. **Delete** the two setting blocks per D-03:
       - `altium365.redirectPort` (currently at lines 162-166)
       - `altium365.redirectPath` (currently at lines 167-171)
       Remove the entire JSON property entries including the closing `,`. Verify the surrounding JSON (the preceding `altium365.audience` block at 157-161 and the following `altium365.pythonPath` block) remains comma-correct.

    2. **Extend each env default in `altium365.environments.default`** (currently lines 204-228) with a `redirectUri` field per D-01. Exact values, no abbreviation:
       - `Dev.redirectUri = "https://auth.dev1.altium.com/authCompleted"`
       - `Uat.redirectUri = "https://auth.uat1.altium.com/authCompleted"`
       - `Prod.redirectUri = "https://auth.altium.com/authCompleted"`
       Place `redirectUri` immediately after the existing `actionWaitEndpoint` line in each env block to keep ActionWait-related fields adjacent. Leave the existing `actionWaitEndpoint` values untouched (D-15).

    3. **Extend the `additionalProperties.properties` schema** at lines 231-240 to declare BOTH new ActionWait-era fields. Currently it lists graphqlEndpoint, authEndpoint, tokenEndpoint, scopes, audience. Add:
       - `"actionWaitEndpoint": { "type": "string" }` (this is missing today even though the defaults use it — fix while we're here per D-15's "fix forward" stance)
       - `"redirectUri": { "type": "string" }`
       Both added as siblings to the existing five.

    Also update the `description` string on `altium365.environments` (currently line 230) so it mentions the two new fields: change "Each entry can override graphqlEndpoint, authEndpoint, tokenEndpoint, scopes, audience." to "Each entry can override graphqlEndpoint, authEndpoint, tokenEndpoint, actionWaitEndpoint, redirectUri, scopes, audience." — keep the trailing "Use 'Altium 365: Select Environment' to switch." sentence intact.

    Do NOT add a top-level `altium365.actionWaitEndpoint` or `altium365.redirectUri` setting — these are env-scoped only (the env-switch command in `doSelectEnvironment` is what propagates them to the flat `altium365.*` settings consumed by `readOAuthConfig`; that propagation is plan 02's job). Do NOT touch any other property block.
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))" &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` exits 0 (file is valid JSON)
    - `npx tsc --noEmit` exits 0 (no TypeScript caller broke — readOAuthConfig still compiles since `cfg.get` returns undefined for the deleted settings, falling back to its `||` defaults until plan 04 lands)
    - `grep -c "altium365.redirectPort" package.json` returns `0`
    - `grep -c "altium365.redirectPath" package.json` returns `0`
    - `grep -c '"redirectUri"' package.json` returns at least `4` (3 env defaults + 1 in additionalProperties.properties)
    - `grep -c "authCompleted" package.json` returns exactly `3` (the three D-01 URLs)
    - `grep -c "actionWaitEndpoint" package.json` returns at least `4` (3 existing defaults + 1 new entry in additionalProperties.properties)
    - The new `redirectUri` values match D-01 exactly — no trailing slash, no path mismatch (`auth.dev1.altium.com/authCompleted`, `auth.uat1.altium.com/authCompleted`, `auth.altium.com/authCompleted`)
  </acceptance_criteria>
  <done>
    package.json contributes the ActionWait-era settings contract: redirectUri is declared per env with the LOCKED D-01 URLs, actionWaitEndpoint is properly schema-typed, and the loopback settings are gone. The file parses as JSON and tsc is green.
  </done>
</task>

</tasks>

<verification>
- `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` clean
- `npx tsc --noEmit` clean across the whole repo (no caller depended on the removed settings being declared — `readOAuthConfig` reads via `cfg.get<T>()` which returns undefined for missing keys; the `||` defaults handle it until plan 04 rewrites readOAuthConfig)
- All grep gates above pass
</verification>

<success_criteria>
AUTH-AW-02 (fixed redirect_uri per env) and AUTH-AW-04 (honour env-specific actionWaitEndpoint) are settled at the schema layer. AUTH-AW-05 (remove loopback) is closed at the settings-contract layer (the two loopback settings no longer exist in the public schema). Plans 02 and 04 can now propagate / consume these fields with confidence the defaults will land in user `settings.json`.
</success_criteria>

<output>
Create `.planning/phases/02.3-actionwait-auth/02.3-01-SUMMARY.md` when done.
</output>
