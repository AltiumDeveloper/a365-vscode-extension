---
phase: 02.3-actionwait-auth
plan: 01
subsystem: config-schema
tags: [config, schema, breaking-settings]
requirements: [AUTH-AW-02, AUTH-AW-04, AUTH-AW-05]
provides:
  - "per-env redirectUri schema + defaults"
  - "actionWaitEndpoint declared in env additionalProperties"
  - "loopback settings removed from public schema"
affects:
  - "readOAuthConfig (still compiles via `||` fallbacks until plan 04 rewrites it)"
key-files:
  modified:
    - package.json
decisions:
  - "D-01: redirectUri per env locked to https://auth.{dev1,uat1,}.altium.com/authCompleted"
  - "D-02: redirectUri added to env-scoped additionalProperties (not as top-level setting)"
  - "D-03: altium365.redirectPort + altium365.redirectPath removed outright (no deprecation)"
  - "D-15: existing actionWaitEndpoint defaults left as-is; schema gap fixed forward"
metrics:
  duration: "~3 min"
  tasks_completed: 1
---

# Phase 02.3 Plan 01: ActionWait config schema migration

Single coherent `package.json` diff aligning the contributes.configuration with the ActionWait auth flow: three env defaults gain `redirectUri`, the two loopback settings are deleted, and `additionalProperties.properties` declares both `actionWaitEndpoint` (previously missing despite being used in defaults) and `redirectUri`.

## What changed

1. **Deleted** `altium365.redirectPort` and `altium365.redirectPath` setting blocks (D-03).
2. **Added** `redirectUri` immediately after `actionWaitEndpoint` in each env default (Dev/Uat/Prod) per D-01 — exact URLs, no trailing slash:
   - Dev → `https://auth.dev1.altium.com/authCompleted`
   - Uat → `https://auth.uat1.altium.com/authCompleted`
   - Prod → `https://auth.altium.com/authCompleted`
3. **Extended** `altium365.environments.additionalProperties.properties` to include both `actionWaitEndpoint` and `redirectUri` (D-02 + D-15 fix-forward).
4. **Updated** the `description` string on `altium365.environments` to mention the two new fields.

## Verification

| Gate | Result |
|------|--------|
| `node -e "JSON.parse(...)"` | OK |
| `npx tsc --noEmit` | OK |
| `grep -c "altium365.redirectPort"` | 0 |
| `grep -c "altium365.redirectPath"` | 0 |
| `grep -c '"redirectUri"'` | 4 (3 defaults + 1 schema) |
| `grep -c "authCompleted"` | 3 |
| `grep -c "actionWaitEndpoint"` | 5 (3 defaults + 1 schema + 1 description) |

All acceptance criteria pass. The description string match bumps the actionWaitEndpoint count above the planned ≥4 — the description-string occurrence is benign and expected (plan said "at least 4").

## Deviations from Plan

None — plan executed exactly as written.

## Self-Check: PASSED

- `package.json` exists and parses as JSON.
- All grep gates match the acceptance bounds.
- No files outside `<files_modified>` were touched (pre-existing uncommitted `src/workspace.ts` modification left untouched per orchestrator instruction).
