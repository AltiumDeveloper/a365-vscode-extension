---
phase: 02.3-actionwait-auth
plan: 02
subsystem: auth/env-switch
tags: [env-switch, config-propagation]
requires: [package.json schema fields actionWaitEndpoint + redirectUri (plan 01)]
provides: [env-switch propagation of actionWaitEndpoint + redirectUri to flat settings]
affects: [src/extension.ts EnvironmentSpec, doSelectEnvironment]
tech_stack_added: []
tech_stack_patterns: [conditional cfg.update('!== undefined') for optional env fields]
key_files_created: []
key_files_modified:
  - src/extension.ts
decisions:
  - "Used conditional `!== undefined` propagation (mirroring scopes/audience) rather than the unconditional `?? ''` pattern used for the three core endpoints — leaves prior value in place when an env spec omits the field, surfacing misconfig later in plan 04's readOAuthConfig with a debuggable value."
  - "Did NOT extend the validation warning at lines 209-225 — graphql/auth/token remain the only unrecoverable trio; missing actionWait/redirect will be caught downstream by plan 04 + 05's pre-flight."
metrics:
  duration_minutes: 2
  completed: 2026-05-20
  tasks_completed: 1
  files_changed: 1
---

# Phase 02.3 Plan 02: Env-switch propagation of ActionWait + redirect fields — Summary

Extended `EnvironmentSpec` with two optional fields (`actionWaitEndpoint?`, `redirectUri?`) and taught `doSelectEnvironment` to push both into the flat `altium365.*` settings via `cfg.update` (conditional on `!== undefined`), plus surfaced them in the post-switch diagnostic log.

## What was built

- `EnvironmentSpec` (src/extension.ts:172) now declares `actionWaitEndpoint?: string` and `redirectUri?: string` between `tokenEndpoint` and `scopes`, mirroring the package.json schema field order from plan 01.
- `doSelectEnvironment` propagation block (src/extension.ts:230-236) gained two conditional `cfg.update` calls for the new fields, using the same `!== undefined` guard as scopes/audience.
- Diagnostic logging (src/extension.ts:249-253) now emits `actionWait:` and `redirect:   ` lines alongside the existing graphql/auth/token triplet, giving UAT D-14.1 a one-glance confirmation of the active env.

## Tasks Completed

| Task | Name                                                                   | Commit  | Files            |
| ---- | ---------------------------------------------------------------------- | ------- | ---------------- |
| 1    | Extend EnvironmentSpec and propagate two new fields in doSelectEnvironment | _below_ | src/extension.ts |

## Verification

- `npx tsc --noEmit` → exit 0
- `grep -n "actionWaitEndpoint?: string" src/extension.ts` → 1 match (interface)
- `grep -n "redirectUri?: string" src/extension.ts` → 1 match (interface)
- `grep -c "cfg.update('actionWaitEndpoint'" src/extension.ts` → 1
- `grep -c "cfg.update('redirectUri'" src/extension.ts` → 1
- `grep -c "altium365.redirectPort\|altium365.redirectPath" src/extension.ts` → 0
- `grep -c "actionWait:" src/extension.ts` → 1
- `grep -c "redirect:   " src/extension.ts` → 1 (three-space alignment preserved)

## Deviations from Plan

None — plan executed exactly as written. Note: the plan-checker had flagged the `grep -c "redirect:   "` gate as brittle (depends on column-aligned trailing spaces), but the exact three-space alignment matched on first run and the gate returned `1` as specified, so no deviation was needed.

## Self-Check: PASSED

- `src/extension.ts` modified — confirmed present
- Commit hash recorded below in final commit step
