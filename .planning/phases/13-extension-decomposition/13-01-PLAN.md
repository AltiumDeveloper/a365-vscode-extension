---
phase: "13"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/runner/index.ts
  - src/ux/commands.ts
  - src/extension.ts
  - src/scripts/commands.ts
  - src/ux/treeCommands.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "src/extension.ts contains only activate(), deactivate(), updateActiveRemoteContext()"
    - "src/runner/index.ts contains all Python subprocess logic"
    - "src/ux/commands.ts contains all auth/workspace/run command handlers"
    - "No circular imports remain between extension.ts and its consumers"
    - "npm run compile exits 0"
    - "npm test exits 0"
---

<objective>
Decompose src/extension.ts (988 lines) into three focused modules with zero logic changes.

Target layout after this plan:
  src/runner/index.ts   — RunPrep interface, prepareRun, runScriptAtPath, debugScriptAtPath,
                          resolvePythonPath, resolveWorkspaceForScript
  src/ux/commands.ts    — doSignIn, doSignOut, doSelectWorkspace, doSelectEnvironment,
                          applyWorkspaceSelection, onStatusBarClick, updateSignedInContext,
                          runScript, debugScript, EnvironmentSpec
  src/extension.ts      — activate(), deactivate(), updateActiveRemoteContext() only (~230 lines)

Side effects (both desirable):
  - Circular import scripts/commands.ts ↔ extension.ts eliminated
    (scripts/commands.ts imports runScriptAtPath/debugScriptAtPath from '../runner' instead)
  - Circular import ux/treeCommands.ts ↔ extension.ts eliminated
    (ux/treeCommands.ts imports applyWorkspaceSelection from './commands' instead)
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create src/runner/index.ts — extract subprocess logic</name>
  <files>src/runner/index.ts, src/scripts/commands.ts, src/extension.ts</files>
  <action>
    Create src/runner/index.ts with the following content extracted verbatim from extension.ts
    (adjust import paths only — zero logic changes):

    Exports: runScriptAtPath, debugScriptAtPath
    Internal: RunPrep interface, prepareRun, resolvePythonPath, resolveWorkspaceForScript

    Import paths in runner/index.ts:
      vscode, path, fs, os, spawn  — unchanged
      './auth'         → '../auth'
      './workspace'    → '../workspace'
      './runner/sandbox' → './sandbox'           (already in runner/)
      './scripts/localCache' → '../scripts/localCache'
      './testEvents/identity' → '../testEvents/identity'
      './testEvents/resolver' → '../testEvents/resolver'

    ONE logic change required to break circular dep:
      In resolveWorkspaceForScript(), replace the direct call:
        await doSelectWorkspace(context);
      with:
        await vscode.commands.executeCommand('altium365.selectWorkspace');
      This is semantically equivalent — it IS a VS Code command dispatch.
      Remove the import of doSelectWorkspace (which doesn't exist yet; it was
      in extension.ts but will move to ux/commands.ts). The executeCommand
      approach is the correct decoupling: runner/ must not import ux/.

    Fix scripts/commands.ts:
      Change: import { runScriptAtPath, debugScriptAtPath, updateActiveRemoteContext } from '../extension';
      To:     import { runScriptAtPath, debugScriptAtPath } from '../runner';
              import { updateActiveRemoteContext } from '../extension';
      Note: updateActiveRemoteContext stays in extension.ts — keep that import.

    Remove from extension.ts:
      RunPrep interface, prepareRun, runScriptAtPath, debugScriptAtPath,
      resolvePythonPath, resolveWorkspaceForScript
      Also remove: path, fs, os, spawn imports (if no longer used)
      Also remove: direct imports now only needed by runner/index.ts:
        getBaseAccessToken, ensureWorkspaceToken (still needed by commands — keep)
        getSandboxPythonPath (keep for installScriptDependencies command)
        resolveScriptIdentity, resolveScriptParameters (now in runner — remove from extension.ts)
        listWorkspaces (still needed by prepareRun — now in runner — remove from extension.ts import)

    Add to extension.ts imports:
      import { runScriptAtPath, debugScriptAtPath } from './runner';

    After: npm run compile — must pass.
  </action>
  <verify>
    <automated>npm run compile</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Create src/ux/commands.ts — extract command handlers</name>
  <files>src/ux/commands.ts, src/ux/treeCommands.ts, src/extension.ts</files>
  <action>
    Create src/ux/commands.ts with the following content extracted verbatim from extension.ts
    (adjust import paths only — zero logic changes):

    Exports: doSignIn, doSignOut, doSelectWorkspace, doSelectEnvironment,
             applyWorkspaceSelection, updateSignedInContext
    Internal: onStatusBarClick, runScript, debugScript, EnvironmentSpec interface

    These are NOT exported but called from extension.ts activate():
      onStatusBarClick, runScript, debugScript

    Since extension.ts needs to register runScript/debugScript as command handlers in
    activate(), either:
      (a) export them from ux/commands.ts and import in extension.ts, OR
      (b) expose a registerRunCommands(context) factory from ux/commands.ts

    Use approach (a): export runScript and debugScript so extension.ts can import
    and register them inline.

    Import paths in ux/commands.ts:
      vscode               — unchanged
      '../auth'            — clearAllTokens, ensureWorkspaceToken, fireAuthStateChanged,
                             getBaseAccessToken, getStoredTokens, readOAuthConfig, signIn
      '../workspace'       — getSelectedWorkspace, getWorkspaceApiUrl, listProjects,
                             listWorkspaces, WorkspaceInfo, resolveWorkspaceFromAuthId,
                             pickWorkspace
      '../runner'          — runScriptAtPath, debugScriptAtPath (for runScript/debugScript handlers)

    Fix ux/treeCommands.ts:
      Change: import { applyWorkspaceSelection } from '../extension';
      To:     import { applyWorkspaceSelection } from './commands';

    Remove from extension.ts:
      doSignIn, doSignOut, doSelectWorkspace, doSelectEnvironment,
      applyWorkspaceSelection, updateSignedInContext, onStatusBarClick,
      runScript, debugScript, EnvironmentSpec

    Add to extension.ts imports from './ux/commands':
      doSignIn, doSignOut, doSelectWorkspace, doSelectEnvironment,
      updateSignedInContext, onStatusBarClick, runScript, debugScript

    Verify: extension.ts should now contain only:
      - imports (~20 lines)
      - outputChannel singleton (1 line)
      - updateActiveRemoteContext() (~14 lines)
      - activate() (~190 lines)
      - deactivate() (1 line)

    After: npm run compile — must pass.
    Then: npm test — all 142 tests must pass.
  </action>
  <verify>
    <automated>npm run compile && npm test</automated>
  </verify>
</task>

</tasks>

<verification>
- npm run compile exits 0
- npm test exits 0 (142 tests)
- wc -l src/extension.ts shows ≤ 250 lines
- No import of '../extension' or './extension' remains in scripts/commands.ts for run/debug functions
- No import of '../extension' remains in ux/treeCommands.ts
</verification>

<success_criteria>
- extension.ts: activate() + deactivate() + updateActiveRemoteContext() only
- runner/index.ts: all Python subprocess preparation and launch logic
- ux/commands.ts: all auth/workspace/environment/run command handlers
- Zero circular imports
</success_criteria>

<output>
Create `.planning/phases/13-extension-decomposition/13-01-SUMMARY.md` when done
</output>
