---
phase: quick-260601-dv8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md, AGENTS.md]
autonomous: true
requirements: []

must_haves:
  truths:
    - README.md reflects current extension name "Altium Developer" (not "Altium 365 Developer Tools")
    - README.md documents all current commands from package.json
    - README.md explains global vs workspace token concepts
    - README.md explains OAuth scopes (openid, profile, offline_access, workspace:scripts.manage, workspace:scripts.execute)
    - README.md does NOT contain internal Dev/Uat/Prod environment URLs or appId GRIDs
    - AGENTS.md accurately reflects current architecture (test events, remote script FSP, identity module)
  artifacts:
    - path: "README.md"
      provides: "User-facing documentation with current state"
      min_lines: 100
    - path: "AGENTS.md"
      provides: "Agent-facing project context"
      min_lines: 40
  key_links:
    - from: "README.md"
      to: "package.json commands"
      via: "command name consistency"
      pattern: "altium365\\."
---

<objective>
Refresh README.md and AGENTS.md to reflect the current state of the extension after 13 completed phases.

Purpose: README.md is outdated (still references "Altium 365 Developer Tools" instead of "Altium Developer", missing test events documentation, contains internal environment details). AGENTS.md needs updates to reflect current architecture (test events, remote script FSP, identity module, extension points).

Output: Updated user-facing README.md and agent-facing AGENTS.md that accurately document the current extension.
</objective>

<execution_context>
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/dmitrykolomiets/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@package.json
@README.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md to current extension state</name>
  <files>README.md</files>
  <action>
Update README.md to reflect current extension state:

1. **Title and branding**: Change title to "Altium Developer" (not "Altium 365 Developer Tools") to match package.json displayName.

2. **Commands reference**: Update Commands Reference table to include all current commands from package.json, including test event commands (Pick/Create/Edit/Delete/Set Default Test Event).

3. **Remove internal environment details**: 
   - Remove specific Dev/Uat/Prod URLs (usw2.dev-365.altium.com, eur.uat-365.altium.com, eur.365.altium.com)
   - Replace with generic language: "Multiple environments can be configured via the `altium365.environments` setting"
   - Remove appId GRID references entirely (internal implementation detail)

4. **Document OAuth scopes**: Add a new section "Authentication & Scopes" explaining:
   - `openid profile` — basic user identity
   - `offline_access` — enables refresh_token for silent sign-in on VS Code restart
   - `workspace:scripts.manage` — required for editing and publishing scripts
   - `workspace:scripts.execute` — required for remote script execution

5. **Document token architecture**: Add section "Global vs Workspace Tokens" explaining:
   - Global token obtained via browser OAuth flow, stored in VS Code secrets
   - Workspace token obtained automatically via token exchange when first interacting with a workspace
   - Workspace tokens are workspace-scoped and cached per workspace ID
   - Sign out clears all tokens (global + all workspace tokens)

6. **Update script execution context documentation**: Clarify that `context.auth_token` is:
   - The global token when no workspace is selected
   - A workspace-scoped token when a workspace has been selected
   - The script execution context section should note this difference

7. **Test events section**: Already present in current README — verify it's accurate and complete per STATE.md decisions (status bar indicator, unified picker, storage in globalState, 25-event bloat warning).

8. **Configuration section**: Update to reflect current settings:
   - Remove deprecated settings (inputParametersPath, promptForProjectId were removed per STATE.md Phase 999.3-06)
   - Mark `altium365.checkForUpdates` as deprecated (per package.json line 290-293)

9. **Remote scripts section**: Verify accuracy against current implementation (edit via `altium365:` scheme, save publishes, Execute Remotely uses test events).

Keep tone concise and user-focused. Remove any "will be added" or "planned" language — document only what exists now.
  </action>
  <verify>
    <automated>grep -q "Altium Developer" README.md && grep -q "Global vs Workspace Tokens" README.md && grep -q "offline_access" README.md && ! grep -q "usw2.dev-365.altium.com" README.md && ! grep -q "appId" README.md</automated>
  </verify>
  <done>README.md updated with current extension name, OAuth scopes, token architecture, complete command list, no internal environment URLs or appId GRIDs</done>
</task>

<task type="auto">
  <name>Task 2: Update AGENTS.md to current architecture</name>
  <files>AGENTS.md</files>
  <action>
Update AGENTS.md to reflect current architecture and phase progress:

1. **Update Phase Execution Order section**: 
   - Note that Phases 1-13 are complete (per STATE.md)
   - Add Phase 4 (UI polish), Phase 6 (test events), Phase 7 (vitest), Phase 8 (versioning), Phase 9 (IntelliSense), Phase 10 (extension points rework) to the list
   - Mark that the extension has shipped test events, remote script FSP, identity module

2. **Update Architecture Summary**:
   - Add `src/identity.ts` — script identity resolution (local path vs remote workspace+scriptId)
   - Add `src/testEvents.ts` — test event storage and resolution
   - Add `src/remoteScriptFs.ts` — `altium365:` FileSystemProvider for remote scripts
   - Add `src/testEventFs.ts` — `altium365-event:` FileSystemProvider for test event editing
   - Update `extension.ts` line count if significantly changed (was 484 lines)

3. **Update Key constraint section**: Add new constraints discovered during execution:
   - Test events stored in `context.globalState` under `altium365.scriptParams.<identity>` — NOT synced via Settings Sync
   - Script identity = local absolute path OR `(workspaceId, scriptId)` for remote scripts
   - Status bar indicator shows active Python editor's default test event

4. **Update Tech Stack**: Add vitest (Phase 7 added unit testing)

5. **Update Code Conventions**: Add:
   - FileSystemProvider pattern for virtual documents (`altium365:`, `altium365-event:` schemes)
   - Identity resolution via `buildIdentity()`/`parseIdentity()` helpers in identity.ts
   - Test event commands prefixed `altium365.testEvents.*`

6. **Remove outdated "will be added" language**: vsce was added in Phase 1, side panel was added in Phase 2, remote script operations were added in Phase 3.

Keep the AGENTS.md concise — this is a quick-reference for AI agents, not exhaustive documentation. Focus on architecture patterns and key constraints that affect new work.
  </action>
  <verify>
    <automated>grep -q "src/identity.ts" AGENTS.md && grep -q "src/testEvents.ts" AGENTS.md && grep -q "context.globalState" AGENTS.md && grep -q "altium365-event:" AGENTS.md</automated>
  </verify>
  <done>AGENTS.md reflects current architecture with identity module, test events, FSP patterns, vitest, and updated phase completion status</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — documentation-only changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260601-dv8-01 | Information Disclosure | README.md | mitigate | Remove internal environment URLs and appId GRIDs (completed in Task 1) |
</threat_model>

<verification>
- README.md grep confirms no Dev/Uat/Prod URLs or appId GRIDs remain
- README.md grep confirms "Altium Developer" title and "offline_access" scope documented
- AGENTS.md grep confirms identity.ts, testEvents.ts, FSP schemes documented
</verification>

<success_criteria>
- README.md uses "Altium Developer" (not "Altium 365 Developer Tools")
- README.md documents OAuth scopes and global vs workspace tokens
- README.md contains zero internal environment URLs or appId GRIDs
- AGENTS.md reflects current architecture (identity, test events, FSP, vitest)
- Both files are accurate to the current extension state (Phase 13 complete)
</success_criteria>

<output>
Create `.planning/quick/260601-dv8-refresh-readme-md-and-agents-md-to-refle/260601-dv8-SUMMARY.md` when done
</output>
